# 任务拆分（tasks.md）— 师范生考核与教师职业能力证书管理平台

> 配套 `plan.md`（含 §15 增补）。本文件把计划拆成可被 codex **逐条领取执行**的原子任务。
> 任务格式：
> ```
> T-编号 · [层] 标题   ← 依赖：T-xxx
> 产出：要交付的类/文件/接口/页面/表
> 验收：可验证的完成标准（含 AT 引用）
> ```
> 层：`Infra`=工程/部署 · `BE`=后端 · `FE`=前端 · `DB`=数据库迁移。
> 全局完成定义（DoD）：编译通过 + 单元/集成测试 + Swagger 更新 + 不破坏既有用例 + 关键路径有 `audit_log`。
> 执行顺序：按 Phase 升序；同 Phase 内按编号；标 `∥` 者可并行。优先级：Phase 0–10、13 = P0；11、12 = P1；14(M14 预留) = P2。

---

## Phase 0 · 工程脚手架与基础设施

**T-001 · [Infra] 创建 Maven 父工程与 8 子模块骨架**  ← 依赖：—
产出：`platform-parent`（dependencyManagement 锁定 Spring Boot 3.2.x / MyBatis-Plus 3.5.x / FastExcel / MinIO / JWT / Hutool 版本）+ §2.4 八子模块 POM 与目录。
验收：`mvn -pl platform-boot -am package` 生成可运行 jar。

**T-002 · [BE] platform-common 基础设施**  ← 依赖：T-001
产出：`Result<T>`/`PageResult<T>`、`ResultCode` 枚举、`BizException`、`GlobalExceptionHandler`、`BaseEntity`(id/创建更新人时间/deleted)、通用枚举基类、常量类、`@AuditLog`/`@DataScope` 注解定义。
验收：抛 `BizException` 返回统一 JSON；参数校验失败返回字段级错误。

**T-003 · [BE] platform-boot 启动与全局配置**  ← 依赖：T-002
产出：启动类、`application-{dev,prod}.yml`、MyBatis-Plus 配置（分页插件/逻辑删除/字段自动填充/乐观锁）、Jackson 配置（`Long`→`String`、日期文本不转序列）、Knife4j、CORS。
验收：`/doc.html` 可访问；Long 主键序列化为字符串。

**T-004 · [DB] Flyway 接入 + 基础表 V1**  ← 依赖：T-003
产出：Flyway 配置；`V1__base.sql` 建 `audit_log`、`file_object`、`sys_param`（含 §11/§15 默认参数种子）。
验收：启动自动建表；`sys_param` 含 `cert.seq.scope`、`video.passLine`、`video.diffThreshold` 等默认值。

**T-005 · [Infra] 本地依赖 docker-compose**  ← 依赖：—  ∥
产出：`docker-compose.dev.yml`（mysql8/redis7/minio）+ `.env` + 初始化脚本（建库 utf8mb4、建 MinIO bucket）。
验收：`docker compose up` 后三服务健康，后端可连通。

**T-006 · [BE] platform-file MinIO 文件服务**  ← 依赖：T-004,T-005
产出：MinIO 配置、`FileService`(上传/下载流/预签名URL/删除/秒传校验MD5)、`file_object` CRUD、`@FileBiz` 标记业务归属。
验收：上传返回 fileId；预签名 URL 限时可访问，过期失效。

**T-007 · [BE] 审计切面 + 数据权限切面骨架**  ← 依赖：T-002,T-004
产出：`@AuditLog` AOP（落 `audit_log`：操作人/时间/前后状态/IP）、`@DataScope` AOP（注入 SQL 范围条件，按 college_id/授权专业/本人）。
验收：标注方法自动写日志；`@DataScope` 单测验证 SQL 拼接正确。

**T-008 · [FE] 前端工程脚手架**  ← 依赖：—  ∥
产出：Vite+Vue3+TS+Naive UI+Pinia+Vue Router；Axios 封装（token 注入/统一错误/loading/401 刷新）；基础布局（侧栏/顶栏/面包屑/标签页）、主题、`.env`。
验收：`pnpm dev` 启动；mock 接口走通封装层。

**T-009 · [FE] 路由权限框架与按钮指令**  ← 依赖：T-008
产出：路由守卫（登录态/菜单权限）、动态菜单、`v-perm` 指令（按钮级权限）、403/404 页。
验收：无权限路由被拦截；无权限按钮不渲染。

**T-010 · [Infra] CI 与代码规范**  ← 依赖：T-001,T-008  ∥
产出：后端 Spotless/Checkstyle，前端 ESLint+Prettier，统一 commit 规范；`make`/脚本一键启动。
验收：lint 通过；格式化无差异。

---

## Phase 1 · 基础数据与字典管理（M01）

**T-011 · [DB] 字典/区划/学科/组织表**  ← 依赖：T-004
产出：`V2__dict.sql`：`sys_dict_type`、`sys_dict_item`(parent_code/year_version/ext_json)、`sys_region`、`teaching_subject`、`sys_college`、`sys_major`、`major_training_goal`、`training_goal_config`。
验收：表与索引创建成功（plan §5.2/§15.3）。

**T-012 · [BE] 字典管理 CRUD + Redis 缓存**  ← 依赖：T-011,T-007
产出：`DictService`（类型/项 CRUD、启用停用、年度版本）、按 type 取项接口、Redis 缓存与变更失效、`@Dict` 翻译注解。
验收：取字典走缓存；改动后缓存刷新。

**T-013 · [DB] 字典标准值种子**  ← 依赖：T-011
产出：`V3__dict_seed.sql`：plan §5.2 全部字典（identity_type/id_card_type/education_level/training_goal/internship_*/teaching_segment/interview_org_mode/gender/material_category/ability_test_conclusion/exemption_subject/exemption_basis/cert_issuer/video_score_dimension）+ 学校 10588 + 省码 44。
验收：各字典标准值与需求逐字一致。

**T-014 · [BE] 行政区划三级联动**  ← 依赖：T-011
产出：`RegionService`（按 parent 查子级、按 code 反查全路径、拼接完整文本）。
验收：省→市→区县级联查询正确；区县→完整文本（生源地）拼接正确。

**T-015 · [DB] 行政区划种子**  ← 依赖：T-011  ∥
产出：导入国标 GB/T 2260 省/市/区县数据（脚本或资源文件）。
验收：广东省三级数据完整可联动。

**T-016 · [BE] 任教学科标准库服务 + 导入器**  ← 依赖：T-011,T-006
产出：`TeachingSubjectService`（按学段查、关键词搜索、按类别筛选、`is_category` 不可选过滤）、Excel 导入器（年度版本）、最近使用记录接口。
验收：中职专业课类别节点不可被选为任教学科（§6.5/AT-05）。

**T-017 · [DB] 任教学科种子（示例库）**  ← 依赖：T-011
产出：`V5__subject_seed.sql`：幼儿园1、小学23、初中28、高中/中职文化课27 完整项 + 中职专业课示例及类别节点（预留完整 358 项以模板导入）。
验收：四学段标准项可下拉；幼儿园仅"幼儿园"。

**T-018 · [BE] 学校/学院/专业 + 多培养目标 + 联动配置**  ← 依赖：T-011
产出：`college`/`major` CRUD（试点标识、年度版本）、`major_training_goal` 维护、`training_goal_config` 维护（默认/可选 学段与实习地点）。
验收：一个专业可配多培养目标；培养目标可查默认/可选范围。

**T-019 · [FE] 字典管理页**  ← 依赖：T-012,T-009
产出：字典类型与项维护、年度版本、启用停用。
验收：增删改即时生效（缓存刷新）。

**T-020 · [FE] 行政区划维护 + 级联组件**  ← 依赖：T-014
产出：区划树维护页 + 可复用 `RegionCascader` 组件（生源地选择）。
验收：三级级联选择并回显完整文本。

**T-021 · [FE] 任教学科库页 + 选择组件**  ← 依赖：T-016
产出：学科库维护/导入页 + `SubjectSelect` 组件（按学段过滤、关键词搜索、分类、最近使用、类别禁选）。
验收：切换学段后候选学科同步刷新。

**T-022 · [FE] 学校/学院/专业维护页**  ← 依赖：T-018
产出：组织与专业维护，专业多培养目标与联动配置编辑。
验收：配置后在 M04 表单生效。

---

## Phase 2 · 账号、角色与权限（M02）

**T-023 · [DB] RBAC 表**  ← 依赖：T-004
产出：`V5__rbac.sql`：`sys_user`(student_id)、`sys_role`、`sys_user_role`、`sys_permission`、`sys_role_permission`、`sys_user_data_scope`。
验收：表结构符合 plan §5.1。

**T-024 · [BE] 认证：登录/JWT/验证码/锁定**  ← 依赖：T-023
产出：Spring Security 配置、JWT 签发与刷新、图形验证码、BCrypt、登录失败次数锁定、强制首次改密。
验收：错误密码累计触发锁定；token 过期可刷新。

**T-025 · [BE] 授权：RBAC + 数据范围落地**  ← 依赖：T-024,T-007
产出：`@PreAuthorize` 权限点鉴权、当前用户上下文、`@DataScope` 与用户授权范围联动、菜单/按钮权限查询接口。
验收：越权接口 403（AT-13）。

**T-026 · [DB] 角色与权限点种子（§15.1 矩阵）**  ← 依赖：T-023
产出：`V6__rbac_seed.sql`：7 角色 + §15.1 全部权限点 + 角色-权限映射 + 超管账号。
验收：各角色权限与 §15.1 矩阵逐项一致。

**T-027 · [BE] 用户/角色/权限/数据范围 管理接口**  ← 依赖：T-025
产出：用户 CRUD、重置密码、分配角色、角色分配权限、用户分配学院/专业数据范围。
验收：分配后即时影响菜单与数据范围。

**T-028 · [FE] 登录页**  ← 依赖：T-024,T-008
产出：账号密码+验证码登录、首次强制改密、记住入口。
验收：登录后按角色加载菜单。

**T-029 · [FE] 用户/角色/权限/数据范围管理页**  ← 依赖：T-027,T-009
产出：用户管理、角色与权限分配（树）、数据范围分配。
验收：操作后权限实时生效。

---

## Phase 3 · 基本信息管理（M03）

**T-030 · [DB] student 表**  ← 依赖：T-004
产出：`V7__student.sql`：plan §5.3 `student`（证件类型/号码、出生日期文本、身份类型、生源地三级+完整、状态、locked）+ 唯一/普通索引。
验收：身份证号、学号字段为 VARCHAR。

**T-031 · [BE] 证件号码校验器（4 类）**  ← 依赖：T-002
产出：`IdCardValidator`：按 §15.4 四类正则；可选身份证 MOD11-2 校验码（参数开关）。
验收：四类证件正确放行/拦截，提示"证件类型与号码不匹配"（V-05/AT-03）。

**T-032 · [BE] 出生日期一致性校验器**  ← 依赖：T-031
产出：`BirthDateValidator`：身份证/港澳台居住证取第7-14位 YYYYMMDD 与出生日期归一比对。
验收：不一致拦截并提示（V-06/AT-03）。

**T-033 · [BE] 姓名格式校验器**  ← 依赖：T-002
产出：`NameValidator`：默认/放宽两模式（§15.4）。
验收：含数字/字母/空格/非法符号被拦（V-04）。

**T-034 · [BE] 学生 CRUD + 本人确认/补充 + 字段锁定**  ← 依赖：T-030,T-031,T-032,T-033
产出：学生增删改查、学生本人确认/补充接口、生源地三级维护、关键字段锁定（证书生成后/审核通过后）。
验收：锁定后关键字段不可改（§6.2）。

**T-035 · [BE] 学生信息两级审核（状态机A）**  ← 依赖：T-034,T-007
产出：基本信息 提交/初审(通过/退回/不通过)/复审(通过/退回/不通过) 接口，按 §15.2-A 流转并留痕。
验收：状态流转与留痕符合 §15.2-A（含不合格终止态）。

**T-036 · [BE] 敏感信息脱敏**  ← 依赖：T-034
产出：`@Desensitize` 注解/序列化器：身份证默认前6后4；明文查看/导出需 `export:sensitive` 权限。
验收：普通页面脱敏；越权取明文被拒。

**T-037 · [FE] 学生信息管理页（教务员）**  ← 依赖：T-034,T-035
产出：列表（数据范围）、编辑、初审/复审操作（退回填原因）、脱敏展示。
验收：退回必填原因；状态可视。

**T-038 · [FE] 学生本人信息页（学生端）**  ← 依赖：T-034
产出：本人信息确认/补充、生源地级联、查看审核意见与状态。
验收：锁定字段禁编辑。

---

## Phase 4 · 专业培养信息（M04）

**T-039 · [DB] training_profile 表**  ← 依赖：T-030
产出：`V8__training.sql`：plan §5.3 `training_profile`（按考核年度一对多）。
验收：含培养目标/学段/学科/面试方式/测试结论字段。

**T-040 · [BE] 专业代码校验器**  ← 依赖：T-002
产出：`MajorCodeValidator`：教育类研究生二级学科代码前四位 ∈ {0401,0451,0453}；其他类身份与试点专业匹配。
验收：非法前缀提示异常（V-08/AT-04）。

**T-041 · [BE] 培养目标→实习地点/学段/学科 联动校验**  ← 依赖：T-018,T-016
产出：基于 `training_goal_config`：培养目标变更联动默认与可选范围；实习地点限制（企业=职教、海外=汉语国际教育）；任教学科必须来自对应学段库。
验收：V-09/V-10/AT-05 通过。

**T-042 · [BE] 培养信息 CRUD + 多培养目标选择 + 两级审核**  ← 依赖：T-039,T-040,T-041,T-035
产出：培养信息维护、学生/教务员选定唯一培养目标、状态机A 两级审核、关键字段锁定。
验收：培养目标必选其一；联动与审核正确。

**T-043 · [FE] 专业培养信息表单**  ← 依赖：T-042
产出：联动表单（培养目标驱动实习地点/学段/学科）、专业代码校验提示、审核操作。
验收：切换培养目标即时联动；非法专业代码即时提示。

---

## Phase 5 · 文件服务 + 过程性考核材料（M05）

**T-044 · [BE] 通用附件上传（PDF/图片）+ 大小限制**  ← 依赖：T-006
产出：受限上传接口（类型 PDF/JPG/JPEG/PNG、单附件大小参数化）、在线预览预签名（PDF/图片）。
验收：超限/非法类型被拒；预览鉴权有效。

**T-045 · [DB] process_material 表**  ← 依赖：T-030
产出：`V9__material.sql`：plan §5.3 `process_material`（四类、初/复审字段、状态）。
验收：category 受 `material_category` 字典约束。

**T-046 · [BE] 材料上传/替换/删除（含锁规则）**  ← 依赖：T-045,T-044
产出：单/多文件上传、通过前可替换、通过后须退回再替换、逻辑删除。
验收：已通过材料直接替换被拒。

**T-047 · [BE] 材料两级审核（通过/退回/不通过）**  ← 依赖：T-046,T-007
产出：初审/复审接口（三种结论，退回/不通过必填原因）、状态机A、历史版本留存。
验收：结论与留痕符合 §15.2-A。

**T-048 · [BE] 过程性考核聚合"合格"判定**  ← 依赖：T-047
产出：四类材料各自复审通过 → 过程性考核状态置"合格"的聚合逻辑。
验收：缺任一类不合格则不能"合格"（§6.9/AT-06）。

**T-049 · [BE] 材料批量下载 + 清单元数据**  ← 依赖：T-046,T-006
产出：按学院/专业/班级/学生打包下载；返回文件名/材料状态/审核人/审核时间。
验收：打包内容与权限范围一致。

**T-050 · [FE] 材料上传页（学生端）**  ← 依赖：T-046
产出：四类材料分区上传、预览、替换、查看审核意见。
验收：通过后替换入口禁用并提示退回。

**T-051 · [FE] 材料审核页（教务员/副院长）**  ← 依赖：T-047,T-049
产出：审核列表、预览、初/复审操作、批量下载、退回原因。
验收：四类聚合状态正确显示。

---

## Phase 6 · 免考信息管理（M06）

**T-052 · [DB] exemption_request / exemption_material 表**  ← 依赖：T-030
产出：`V10__exemption.sql`：免考申请（多科多行）+ 每科佐证材料。
验收：一名学生可多科免考。

**T-053 · [BE] 免考申请（多科 + 每科佐证 + 依据字典）**  ← 依赖：T-052,T-044
产出：申请/修改、按学段配置可免科目、每科独立佐证上传、依据字典+文本说明。
验收：每科必须独立佐证（AT-07）。

**T-054 · [BE] 免考两级审核（每科独立）+ 应考科目联动**  ← 依赖：T-053,T-007
产出：每科 初/复审（状态机A）、通过科目从应考科目移除、不覆盖过程性结论。
验收：通过科目联动 `ability_test_result.exam_subjects`（§6.10）。

**T-055 · [FE] 免考申请页（学生端）**  ← 依赖：T-053
产出：多科选择、每科佐证上传、依据填写。
验收：提交校验每科佐证齐全。

**T-056 · [FE] 免考审核页（教务员/副院长）**  ← 依赖：T-054
产出：按科审核、退回原因、状态展示。
验收：每科可独立通过/退回/不通过。

---

## Phase 7 · 教学能力视频评审（M07）

**T-057 · [BE] 分片上传服务（init/chunk/merge）**  ← 依赖：T-006
产出：MinIO 分片上传：初始化、分片上传、合并、MD5 秒传、断点续传、进度查询、失败重传；单文件阈值参数化（默认 2GB）。
验收：2GB MP4 断网续传成功。

**T-058 · [BE] 视频校验（格式/大小/时长±容差）**  ← 依赖：T-057
产出：合并后校验 MP4、大小、时长（15min±`video.durationTolerance`），失败置"校验失败"。
验收：超时长容差被拒并提示（§6.11）。

**T-059 · [DB] video_review / video_review_task 表**  ← 依赖：T-030
产出：`V11__video.sql`：视频主表 + 评审任务表（每教师一行、维度分 JSON、是否提交、提交时间）。
验收：唯一约束 `(video_review_id,reviewer_id)`。

**T-060 · [BE] 评审任务分配（默认2，可配多专家）**  ← 依赖：T-059
产出：分配评审教师接口（`video.reviewerCount`）、任务下发、进入"评审中"。
验收：分配数符合参数；非分配教师无任务。

**T-061 · [BE] 独立评分（提交前互不可见）**  ← 依赖：T-060
产出：维度评分+意见+结论提交；未全部提交前互相屏蔽分数/意见。
验收：教师A提交后教师B仍看不到A的分（§6.6/AT-08）。

**T-062 · [BE] 分差判定 + 结算（状态机B + §15.5）**  ← 依赖：T-061
产出：全部提交后按 §15.5 计算：一致→均分；分差>阈值或结论冲突→"需复评"。
验收：阈值/合格线参数化生效。

**T-063 · [BE] 复评/仲裁（第三专家 or 学院仲裁）**  ← 依赖：T-062
产出：`thirdExpert`/`collegeArbitrate` 两模式结算终分与结论；学院负责人确认。
验收：复评后产生唯一终分与结论并留痕（AT-08）。

**T-064 · [BE] 视频鉴权播放 + 水印数据下发**  ← 依赖：T-057,T-025
产出：鉴权后下发限时预签名播放地址；返回水印信息（用户名/工号/时间）。
验收：未登录/越权无法获取播放地址。

**T-065 · [FE] 视频上传页（学生端，分片进度）**  ← 依赖：T-057,T-058
产出：分片上传、进度条、断点续传、失败重传、校验结果提示。
验收：大文件上传体验完整。

**T-066 · [FE] 视频评审页（评审教师）**  ← 依赖：T-061,T-064
产出：鉴权播放器+动态水印浮层、维度评分表单、意见、提交（提交前不可见他人）。
验收：水印随播放显示；提交后锁定本人评分。

**T-067 · [FE] 视频评审管理页（学院）**  ← 依赖：T-060,T-063
产出：分配、进度看板、复评/仲裁、结果确认。
验收：需复评任务可流转到第三专家/仲裁。

---

## Phase 8 · 教师职业能力测试结果（M08）

**T-068 · [DB] ability_test_result 表**  ← 依赖：T-030
产出：`V12__test.sql`：应考科目、成绩(文本)、结论、免考关系、确认状态、locked。
验收：成绩字段 VARCHAR。

**T-069 · [BE] 测试结果录入/导入 + 免考联动**  ← 依赖：T-068,T-054
产出：手工录入与 Excel 导入、结论（合格/不合格/免考/待确认）、应考科目随免考结果联动。
验收：免考通过科目不在应考清单（§6.7）。

**T-070 · [BE] 测试结果教务处确认与锁定**  ← 依赖：T-069,T-007
产出：最终确认置 locked，后续修改需审批并留痕。
验收：确认后修改走审批+日志。

**T-071 · [FE] 测试结果管理页**  ← 依赖：T-069,T-070
产出：录入/导入、结论维护、确认锁定、免考联动展示。
验收：联动与锁定可视。

---

## Phase 9 · 证书编号与证书管理（M09）

**T-072 · [DB] certificate / cert_sequence 表**  ← 依赖：T-030
产出：`V13__certificate.sql`：证书表（编号唯一、关键字段、状态、作废/重开字段）+ 序列表（scope_key+current_seq）。
验收：`cert_no` 唯一索引。

**T-073 · [BE] 证书编号生成器（18位 + 作用域 + 锁）**  ← 依赖：T-072
产出：按 §6.6/§15.10 生成：年+校码+层次+省码+学段+序号；作用域参数 `cert.seq.scope`；Redis 分布式锁/行锁保证连续不重；查重。
验收：同年同校（同学段）序号连续不重复；示例可复现（AT-10）。

**T-074 · [BE] 有效期计算器**  ← 依赖：T-002
产出：`ValidityCalculator`：上半年→签发年+3 的 6/30；下半年→+3 的 12/31；允许权限更正。
验收：示例 2022 上/下半年 → 2025/6/30、2025/12/31（AT-11）。

**T-075 · [BE] 证书生成前置条件校验**  ← 依赖：T-035,T-042,T-048,T-054,T-063,T-070
产出：聚合校验 §6.8（信息/培养/过程性/测试免考/视频/复审/教务处确认；视频是否必须通过受参数控制）。
验收：任一前置不满足则拒绝生成（AT-09）。

**T-076 · [BE] 证书状态机（生成/签发/导出/归档/作废/重开）**  ← 依赖：T-073,T-074,T-075,T-007
产出：状态机C 全流转 + 关键字段锁定 + 作废/重开留痕（原因/操作人/时间/原证书号）。
验收：流转与留痕符合 §15.2-C。

**T-077 · [BE] 证书编号导入/更正（权限受控）**  ← 依赖：T-076,T-025
产出：教务处在 `cert:correct` 权限下导入或更正编号，强制留痕。
验收：更正记审计日志。

**T-078 · [FE] 证书管理页（教务处）**  ← 依赖：T-076
产出：生成（前置校验提示缺项）、清单、作废/重开、更正。
验收：缺项时阻断并列出原因。

**T-079 · [FE] 证书签发页（签发人）**  ← 依赖：T-076
产出：签发确认（签发人/日期）、有效期回显、导出清单入口。
验收：签发后关键字段锁定。

---

## Phase 10 · 教育部标准数据交换中心（M10）

**T-080 · [BE] 26列 Excel 模型 + 文本格式策略**  ← 依赖：T-003
产出：A–Z `@ExcelProperty` 模型（全 String）、文本单元格写出处理器（格式 `@`）、列顺序与名称固定、H 列表头"身份证件号码"。
验收：导出重开无科学计数法/日期序列/前导零丢失（AT-01/AT-02）。

**T-081 · [BE] 模板下载（年度/学院/专业 + 内置下拉）**  ← 依赖：T-080,T-012,T-016
产出：POI `DataValidationHelper` 内置字典与学科库下拉、隐藏数据 sheet、全列文本格式。
验收：下拉项与系统字典一致。

**T-082 · [BE] 预校验中心 V-01~V-13（不入库）**  ← 依赖：T-031,T-032,T-033,T-040,T-041,T-073,T-074
产出：`PreValidator` 逐行执行 13 条规则，产出 {总数/成功预览/失败明细}。
验收：13 条规则逐条可触发并定位行号/字段/原因。

**T-083 · [BE] 异常报告生成**  ← 依赖：T-082
产出：异常数据表导出（批次号/行号/学号姓名/字段/错误值/原因/建议）。
验收：内容符合 §15.6/AT-14。

**T-084 · [DB] import_export_batch / import_error_detail / import_record_ref**  ← 依赖：T-004
产出：`V14__exchange.sql`：批次、异常明细、回滚追溯表。
验收：表结构符合 plan §5.3/§15.3。

**T-085 · [BE] 确认导入（策略 + 批次 + 事务）**  ← 依赖：T-082,T-084
产出：策略（新增/覆盖/跳过重复/仅更新空字段）、批次号、成功/失败计数、写 `import_record_ref`。
验收：四策略行为正确；批次可查。

**T-086 · [BE] 导入回滚**  ← 依赖：T-085
产出：按 batch_id 反向应用（INSERT→逻辑删除/UPDATE→还原 before_json），冲突跳过提示。
验收：回滚后数据恢复到导入前。

**T-087 · [BE] 标准导出（按范围筛选 + 保留条件）**  ← 依赖：T-080,T-007
产出：标准上报表导出，范围筛选（全校/学院/专业/班级/培养目标/学段/审核状态/证书状态），导出审计，敏感导出鉴权。
验收：导出记录含操作人/时间/范围/文件（AT-01）。

**T-088 · [BE] 完整审核表 / 汇总表 / 附件清单表 导出**  ← 依赖：T-087,T-049
产出：§15.6 三类子表导出（含各环节审核明细/证书汇总/附件清单）。
验收：列与 §15.6 定义一致。

**T-089 · [BE] 附件与视频批量打包导出**  ← 依赖：T-049,T-064
产出：附件/视频材料打包，Excel 保留文件名/类型/状态/下载路径。
验收：包内容与清单一致。

**T-090 · [FE] 导入中心页**  ← 依赖：T-081,T-085,T-086
产出：模板下载、上传、预校验结果预览（成功/失败）、异常报告下载、策略选择、确认导入、批次列表、回滚。
验收：两步导入 + 回滚闭环可用。

**T-091 · [FE] 导出中心页**  ← 依赖：T-087,T-088,T-089
产出：范围筛选、多种导出类型、敏感导出权限提示、导出记录。
验收：筛选条件随导出留存。

---

## Phase 11 · 统计查询与报表（M11，P1）

**T-092 · [BE] 学院提交进度统计**  ← 依赖：T-035  ∥
产出：按学院/专业/班级/状态聚合 + 导出。 验收：数据与明细对账一致。

**T-093 · [BE] 材料完成率统计**  ← 依赖：T-048  ∥
产出：四类材料/初复审状态/退回原因聚合 + 异常清单。 验收：完成率口径正确。

**T-094 · [BE] 免考统计**  ← 依赖：T-054  ∥
产出：科目/学院/专业/通过-不通过人数 + 明细。 验收：人数与明细一致。

**T-095 · [BE] 视频评审进度统计**  ← 依赖：T-063  ∥
产出：已/未上传、待评审、已评审、需复评 + 专家任务表。 验收：看板数据准确。

**T-096 · [BE] 证书生成统计**  ← 依赖：T-076  ∥
产出：待生成/已生成/已签发/已作废/已导出 + 证书清单。 验收：状态计数正确。

**T-097 · [BE] 任教学段/学科 交叉统计**  ← 依赖：T-042  ∥
产出：学段/学科/身份类型/学历层次交叉表。 验收：交叉维度正确。

**T-098 · [BE] 异常数据统计**  ← 依赖：T-082  ∥
产出：字段缺失/证件异常/专业代码异常/证书编号异常/学段学科不匹配 + 整改清单。 验收：异常可定位。

**T-099 · [BE] 导入导出日志统计**  ← 依赖：T-085,T-087  ∥
产出：批次/操作人/时间/成功失败数/范围 审计报表。 验收：与批次记录一致。

**T-100 · [FE] 统计报表页（ECharts + 导出）**  ← 依赖：T-092..T-099
产出：八类统计图表/表格 + Excel 导出。 验收：图表与数据一致、可导出。

---

## Phase 12 · 通知提醒（M12，P1）

**T-101 · [DB] notification 表**  ← 依赖：T-004
产出：`V15__notification.sql`。 验收：建表成功。

**T-102 · [BE] 站内信服务 + 触发点接入**  ← 依赖：T-101
产出：通知服务 + 在 提交/退回/评审分配/导出完成 等节点发通知；预留邮件/短信通道接口。
验收：四类触发均产生通知。

**T-103 · [FE] 通知中心 + 未读角标**  ← 依赖：T-102
产出：通知列表、已读/未读、顶栏角标。 验收：实时未读计数。

---

## Phase 13 · 系统管理与审计/安全（M13，P0）

**T-104 · [BE] 系统参数管理（§11/§15 全量）**  ← 依赖：T-004
产出：参数 CRUD（分组）、热加载、类型校验；覆盖证书序列作用域、视频阈值/合格线、文件大小、自动建号开关等。
验收：改参数无需改代码即生效（NFR 可配置性）。

**T-105 · [BE] 审计日志查询（多维）**  ← 依赖：T-007
产出：按学生/批次/学院/时间/操作类型查询；普通管理员不可删除（仅系统管理员且留痕）。
验收：全审核操作可查（AT-12）。

**T-106 · [BE] 全局水印与脱敏治理**  ← 依赖：T-036,T-064
产出：敏感页面统一脱敏、文件/视频访问统一鉴权、播放水印组件化。
验收：复制链接未登录不可访问（NFR 文件安全）。

**T-107 · [BE] 备份与可恢复策略**  ← 依赖：T-004,T-006
产出：MySQL binlog/定时全备脚本、MinIO 版本化/同步、逻辑删除（不物理删）；恢复说明文档。
验收：可恢复指定时间点（NFR 可靠性）。

**T-108 · [FE] 系统参数/审计日志/备份 管理页**  ← 依赖：T-104,T-105
产出：参数维护、日志检索、备份记录查看。 验收：操作闭环。

---

## Phase 14 · 非功能收口、部署与验收（含 M14 预留，P2）

**T-109 · [BE] M14 外部接口扩展点（仅预留）**  ← 依赖：T-024
产出：统一身份认证(CAS/OAuth2)开关与适配接口、教务/学籍/电子签章/上级平台 SPI 占位，不实现具体对接。
验收：开关关闭时不影响一期功能。

**T-110 · [Infra] 后端/前端 Dockerfile**  ← 依赖：T-003,T-008
产出：后端多阶段构建（maven→jre）、前端（node 构建→nginx，反代 `/api`）。
验收：镜像可独立运行。

**T-111 · [Infra] 生产 docker-compose + 初始化**  ← 依赖：T-110,T-004
产出：`docker-compose.yml`（mysql/redis/minio/backend/frontend）、`.env`、数据卷、健康检查、Flyway 自动迁移、种子初始化、README。
验收：一键起站可登录可跑通主流程。

**T-112 · [Infra] 兼容性与文本一致性测试**  ← 依赖：T-080,T-087
产出：导出文件在 Microsoft Excel 与 WPS 打开一致性核对、主流浏览器回归。
验收：文本字段不被转换（NFR 兼容性/AT-01）。

**T-113 · [QA] AT-01~AT-14 验收用例归档**  ← 依赖：所有前置
产出：14 条验收标准的测试用例与执行记录（plan §10 映射）。
验收：14 项全部通过。

**T-114 · [QA] 主业务流程端到端走查**  ← 依赖：T-113
产出：导入→确认→培养→材料→初复审→免考→视频→测试→教务处确认→证书生成→签发→导出/归档 全链路 E2E。
验收：全流程贯通、状态/留痕正确。

---

## 附：任务依赖关键路径（里程碑）
1. **M0 可启动**：T-001→T-004→T-008（前后端骨架 + 建表）。
2. **M1 字典就绪**：T-011→T-013/T-015/T-017→T-016/T-018（联动与学科库）。
3. **M2 登录鉴权**：T-023→T-024→T-025→T-026（RBAC 矩阵）。
4. **M3 数据采集**：T-030→T-031/T-032→T-034→T-035（基本信息两级审核）。
5. **M4 联动校验**：T-039→T-040/T-041→T-042。
6. **M5–M8 业务闭环**：材料 T-044→T-048；免考 T-052→T-054；视频 T-057→T-063；测试 T-068→T-070。
7. **M9 证书**：T-072→T-073/T-074→T-075→T-076（前置依赖 M3–M8 全部）。
8. **M10 数据交换**：T-080→T-082→T-085→T-087/T-088（依赖全部校验器与业务表）。
9. **收口**：T-104→T-105（审计/参数）→ T-111（部署）→ T-113/T-114（验收）。

> 关键阻塞点：T-073（编号生成）与 T-082（预校验）分别依赖 §6 全部校验器；务必在进入 Phase 9/10 前完成 T-031/032/033/040/041/074。
