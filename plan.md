# 师范生教育教学能力考核与教师职业能力证书管理平台 — 开发执行计划（plan.md）

> 本文件供 codex 逐阶段执行。来源：《师范生教育教学能力考核与教师职业能力证书管理平台 需求分析报告 V2.0》（广东技术师范大学，2026年6月）。
> 原则：**需求零遗漏**。文档中出现的每一条字段、字典、校验规则、状态、流程、验收项均落地为可执行任务。
> 技术栈（强制）：Java 17 + Maven + Spring Boot 3.x + MySQL 8.x + Docker；前端 Vue 3 + Vite + TypeScript + Naive UI。

---

## 0. 文档约定与全局原则

1. **文本化第一原则（贯穿全栈）**：`学校代码、学号、身份证件号码、出生日期、证书编号、有效期限` 等字段必须从「数据库存储 → 模板 → 导入解析 → 导出写入」三层全部按文本处理。
   - 数据库：一律 `VARCHAR`，禁止 `DATE/BIGINT` 存储上述字段（出生日期、有效期限以字符串 `YYYY/M/D` 或 `YYYY/MM/DD` 存储与导出）。
   - 导出：单元格格式强制 `@`（文本），禁止科学计数法、禁止日期序列号、禁止前导零丢失。
   - 验收对应 **AT-01**。
2. **标识符规范**：表名/列名/接口路径用英文 snake/kebab；业务枚举值、提示文案、字典值用中文。所有表加注释（COMMENT）。
3. **配置化第一原则**：字典、证书规则、视频阈值、评分维度、合格线、导入模板版本、文件大小上限均通过参数/字典配置，不写死代码。
4. **留痕第一原则**：所有审核（初审/复审/确认/退回/作废/重开/导出）写 `audit_log`，记录操作人、时间、意见、前状态、后状态、IP。
5. **逻辑删除**：业务数据与附件元数据逻辑删除（`deleted` 标志），不物理删除；保留历史版本。
6. 平台正式名称：**师范生教育教学能力考核与教师职业能力证书管理平台**。学校：广东技术师范大学，学校代码 `10588`，省级行政区划代码 `44`（广东）。

---

## 1. 系统目标与建设边界

### 1.1 目标（来自 1.2）
- 建立统一师范生考核基础数据库：学生基本信息、专业培养信息、培养目标、任教学段、任教学科、证书信息。
- 过程性考核四类材料的在线上传、预览、退回、初审、复审、归档。
- 免考申请、免考科目、佐证材料、二级审核、结果确认的独立管理。
- 教学能力视频独立入口、分片上传、双教师评审、分差复评、最终结果确认。
- 教师职业能力测试结果、免考结果与证书生成条件的业务联动。
- 18 位证书编号一键生成、查重、签发、作废、重开、有效期自动计算。
- 符合教育部标准模板的导入、预校验、异常报告、导出（防止身份证号、证书编号、日期格式转换错误）。

### 1.2 边界（来自 1.3）
| 范围类型 | 纳入范围 | 处理 |
|---|---|---|
| 业务管理 | 学生信息、培养信息、材料审核、免考、视频评审、测试结果、证书、导入导出、统计查询 | 一期全部纳入 |
| 数据交换 | 教育部标准 Excel 模板、内部导入模板、异常报告、证书清单、完整审核表 | 一期以 Excel 导入导出为主，接口对接为二期 |
| 外部系统 | 教务系统、学籍系统、统一身份认证、电子签章/电子证照、上级平台接口 | **预留扩展接口（M14），一期不强制实现** |
| 不纳入/后置 | 视频内容质量自动识别、AI 评分、师德材料真实性自动判定 | 不做 |

---

## 2. 技术栈与架构决策

### 2.1 后端
- **Java 17 + Spring Boot 3.2.x + Maven**（多模块）。
- **MyBatis-Plus 3.5.x** 作为 ORM（字典/CRUD/复杂报表密集，配合分页插件与逻辑删除）。
- **Spring Security + JWT** 认证授权；预留 CAS/OAuth2 对接统一身份认证（M14）。
- **MySQL 8.x**，字符集 `utf8mb4`，排序 `utf8mb4_0900_ai_ci`。
- **Redis 7.x**：字典缓存、验证码、登录态、限流、序列号分布式锁。
- **EasyExcel（FastExcel）**：Excel 导入导出（核心，保障文本格式）。
- **MinIO**（S3 兼容）：附件与视频对象存储，支持分片/断点续传/预签名鉴权 URL。
- **Flyway**：数据库版本迁移（`src/main/resources/db/migration`）。
- **Knife4j + SpringDoc OpenAPI**：接口文档。
- **Hutool / commons**：工具类。**Logback**：日志。**Spring Validation**：参数校验。
- 全局异常处理 + 统一响应体 `Result<T>{code,msg,data}`。

### 2.2 前端
- **Vue 3 + Vite + TypeScript + Naive UI**。
- **Pinia**（状态）、**Vue Router**（路由+权限守卫）、**Axios**（封装拦截器、统一错误、token 刷新）。
- **@vueuse/core**；图表用 **ECharts**（统计报表）。
- 大文件分片上传组件（自研，基于 `Blob.slice` + 并发 + MD5 秒传 + 断点续传）。
- 视频在线播放加动态用户水印（用户名+工号+时间戳浮层）。
- 角色化菜单与按钮级权限指令 `v-perm`。

### 2.3 部署
- Docker + docker-compose：`mysql`、`redis`、`minio`、`backend`、`frontend(nginx)`。
- 前端 nginx 反代 `/api` 到 backend；视频/附件经后端鉴权后由 MinIO 预签名 URL 下发。

### 2.4 模块划分（Maven）
```
platform-parent/
├── platform-common         # 通用：Result、异常、枚举、常量、工具、注解(@AuditLog,@DataScope)
├── platform-security       # 认证授权、JWT、RBAC、数据权限切面
├── platform-system         # 字典、行政区划、任教学科库、学校/学院/专业、参数、用户角色权限、审计日志、通知
├── platform-business       # 学生、培养、材料、免考、视频、测试结果、证书
├── platform-exchange       # 教育部标准导入导出与预校验中心
├── platform-statistics     # 统计查询与报表
├── platform-file           # MinIO 文件服务、分片上传
└── platform-boot           # 启动模块（聚合、配置、Swagger、跨域、全局异常）
```

---

## 3. 用户角色与权限（来自 三 / 十）

### 3.1 角色定义（7 个）
| 角色编码 | 角色 | 主要权限 | 数据范围 | 关键控制点 |
|---|---|---|---|---|
| `STUDENT` | 学生 | 查看本人信息、补充/确认基本信息、上传过程性材料、提交免考申请、上传教学能力视频、查看审核意见与结果 | 仅本人 | 审核通过或证书生成后关键字段不可自行修改 |
| `COLLEGE_CLERK` | 学院教务员 | 批量导入学生信息、维护本学院学生数据、初审基本信息/培养信息/材料/免考、导出学院数据 | 本学院或授权专业 | 所有初审留痕；退回须填原因 |
| `COLLEGE_AUDITOR` | 副院长/学院审核负责人 | 复审初审通过数据、确认学院考核结论、退回材料、确认视频评审结果 | 本学院或授权专业 | 复审通过后进入教务处确认/证书前置 |
| `REVIEW_TEACHER` | 专业教师评审人 | 查看被分配视频、独立评分、填写评审意见、提交结果 | 仅分配任务 | 两位教师提交前互不可见评分与意见 |
| `ACADEMIC_ADMIN` | 教务处管理员 | 全校数据管理、字段字典维护、模板维护、最终确认、证书编号生成、全校导出、异常数据处理 | 全校 | 最终确认与证书生成严格留痕 |
| `CERT_ISSUER` | 证书签发人/校级确认人 | 查看证书清单、确认签发信息、签发证书、查看导出清单 | 全校或授权范围 | 签发后证书状态变更、关键字段锁定 |
| `SYS_ADMIN` | 系统管理员 | 用户、角色、权限、日志、存储、参数、备份维护 | 系统级 | 不直接改业务结论；超管更正必须留痕 |

### 3.2 数据权限
- 通过 `@DataScope` 切面 + 用户绑定 `college_id`/授权专业列表实现：学生=本人；学院角色=本学院/授权专业；教务处/签发=全校；系统管理员=系统级。
- 验收对应 **AT-13**：学生只能查看本人数据；学院用户只能查看授权范围；敏感导出有权限控制与日志。

### 3.3 权限粒度
- 菜单权限 + 按钮（操作）权限 + 数据范围权限三层。
- 权限点编码示例：`student:import`、`material:firstReview`、`material:secondReview`、`exemption:audit`、`video:assign`、`video:score`、`cert:generate`、`cert:issue`、`cert:void`、`export:standard`、`export:sensitive`、`dict:manage` 等。

---

## 4. 总体业务流程与状态机（来自 四 / 十）

### 4.1 主流程
```
学生名单导入 → 学生确认/补充信息 → 专业培养信息确认 → 上传过程性考核材料
→ 教务员初审 → 副院长复审 → 免考信息审核 → 视频上传与双教师评审
→ 教师职业能力测试结果确认 → 教务处最终确认 → 证书生成 → 证书签发 → 导出/归档
```

### 4.2 业务状态（信息/材料/免考通用 + 视频 + 证书前置）
实现为状态枚举 `BizStatus`，每次流转写 `audit_log`。

| 状态 | 适用阶段 | 触发条件 | 下一步 |
|---|---|---|---|
| 草稿 | 填报/材料上传 | 学生未提交或教务员导入后待确认 | 学生/教务员提交 |
| 待初审 | 信息/材料/免考 | 学生或教务员提交后 | 教务员初审 |
| 初审退回 | 信息/材料/免考 | 教务员退回 | 修改后再提交 |
| 待复审 | 信息/材料/免考 | 初审通过 | 副院长复审 |
| 复审退回 | 信息/材料/免考 | 复审退回 | 修改后重提初审或复审 |
| 复审通过 | 信息/材料/免考 | 学院负责人确认 | 进入教务处确认/证书前置校验 |
| 待评审 | 视频 | 视频上传并校验通过 | 分配专业教师评审 |
| 需复评 | 视频 | 两教师分差超阈值或结论冲突 | 第三专家或学院仲裁 |
| 待教务处确认 | 证书前置 | 复审/测试免考/视频条件满足 | 教务处最终确认 |
| 已生成证书 | 证书 | 证书编号生成成功 | 签发/导出/作废重开 |
| 已签发 | 证书 | 签发人与签发日期确认 | 归档导出 |
| 已归档 | 最终 | 完成导出或归档 | 流程结束 |

### 4.3 证书状态机（来自 九.3）
`待生成 → 已生成 → 已签发 → 已导出 → 已归档`；旁支：`已作废`（记录原因）、`已重开`（关联原证书）。
- 已作废：必须记录作废原因、操作人、时间、关联原证书编号。
- 已重开：作废后重新生成或更正，关联原证书记录。
- 已生成后：姓名、证件号码、任教学段、任教学科、培养目标、证书编号、有效期限锁定。

### 4.4 审核机制（来自 十）
| 审核对象 | 初审人 | 复审人/确认人 | 可退回 | 备注 |
|---|---|---|---|---|
| 基本信息 | 学院教务员 | 副院长/学院负责人 | 是 | 关键身份信息变更需留痕 |
| 专业培养信息 | 学院教务员 | 副院长/学院负责人 | 是 | 培养目标、任教学段、任教学科联动校验 |
| 过程性考核材料 | 学院教务员 | 副院长/学院负责人 | 是 | 四项全部复审通过才可合格 |
| 免考申请材料 | 学院教务员 | 副院长/学院负责人 | 是 | 每个免考科目独立审核 |
| 视频评审结果 | 专业教师评审 | 学院负责人确认 | 是 | 分差超阈值进入复评/仲裁 |
| 教师职业能力测试结果 | 学院或教务处授权人员 | 教务处管理员 | 是 | 作为证书生成条件 |
| 证书生成 | 教务处管理员 | 证书签发人/校级确认 | 是 | 生成后关键字段锁定 |

留痕规则：记录审核人、时间、意见、前状态、后状态；退回必填原因并保留历史版本；超管对已生成证书关键字段更正单独记录原因并形成审计日志；日志支持按学生/批次/学院/时间查询。验收对应 **AT-12**。

---

## 5. 数据库设计（覆盖 八 全部 10 个核心对象 + 系统支撑表）

> 全部表 `utf8mb4`；含 `id BIGINT PK auto`、`created_by/created_at/updated_by/updated_at`、`deleted TINYINT default 0`。文本化字段一律 `VARCHAR`。用 Flyway 管理 DDL。

### 5.1 认证与组织
- `sys_user`(id, username, password_hash, real_name, work_no, email, phone, status, user_type, college_id, student_id 可空, last_login_at)
- `sys_role`(id, code, name, description)
- `sys_user_role`(user_id, role_id)
- `sys_permission`(id, code, name, type[menu/button/data], parent_id, path, sort)
- `sys_role_permission`(role_id, permission_id)
- `sys_user_data_scope`(user_id, college_id, major_id) — 授权专业/学院范围
- `sys_college`(id, code, name, status) — 学院
- `sys_major`(id, college_id, internal_major_code 校内专业代码, internal_major_name, second_discipline_code 二级学科代码, second_discipline_name, pilot_scope_flag 试点范围标识, year_version, status) — 支持年度版本与专业多培养目标

### 5.2 字典与标准库（来自 6.1 / 附录B）
- `sys_dict_type`(id, type_code, type_name)
- `sys_dict_item`(id, type_code, item_code, item_value, parent_code 可空, sort, status, year_version, ext_json) — 支持层级与年度版本
- `sys_region`(id, code, name, parent_code, level[1省/2市/3区县]) — 省/市/区县三级联动
- `teaching_subject`(id, segment_code 学段编码, category_node 类别节点, subject_code, subject_name, is_category 是否类别节点, keyword 检索词, year_version, status) — **任教学科标准库独立表**

**必须预置的字典（type_code）**：
| type_code | 含义 | 标准值 |
|---|---|---|
| `school` | 学校 | 代码 10588 / 广东技术师范大学（导出按文本） |
| `identity_type` | 身份类型 | 教育类研究生、公费师范生、普通师范生、优师计划师范生、国优计划师范生 |
| `id_card_type` | 身份证件类型 | 居民身份证、港澳台居民居住证、港澳居民来往内地通行证、五年有效期台湾居民来往大陆通行证 |
| `education_level` | 学历层次 | 博士研究生、硕士研究生、本科、专科 |
| `training_goal` | 专业培养目标 | 幼儿园教师、小学教师、初级中学教师、高级中学教师、中等职业学校教师 |
| `internship_org_mode` | 教育实习实践组织方式 | 学校组织、个人联系 |
| `internship_location` | 教育实习实践地点 | 幼儿园、中小学、企业（职业技术教育专业）、海外（汉语国际教育专业）、其他 |
| `teaching_segment` | 任教学段 | 幼儿园、小学、初级中学、高级中学、中等职业学校 |
| `interview_org_mode` | 面试考试组织方式 | 结合教育实习实践环节一并考核、单独面试 |
| `gender` | 性别 | 男、女（按上级平台允许值配置） |
| `material_category` | 材料类别 | 思想品德及师德素养、教师教育课程学业、教育实习实践、专业能力及技能培训 |
| `ability_test_conclusion` | 测试结论 | 合格、不合格、免考、待确认 |
| `exemption_subject` | 免考科目 | 按学段配置（幼儿园：综合素质（幼儿园）、保教知识与能力；其他学段按学校规则） |
| `exemption_basis` | 免考依据/原因 | 学校维护 |
| `cert_issuer` | 证书签发人 | 校（院）长姓名，学校维护 |
| `video_score_dimension` | 视频评分维度 | 说课设计、教学目标、重难点、教学实施、课堂组织、学科素养、语言表达、课件/板书、教学反思（可配置） |

**任教学科标准库（teaching_subject）规模（附录B，须以模板导入，不写死）**：
| 学段/类别 | 数量 | 规则 |
|---|---|---|
| 幼儿园 | 1 项（仅"幼儿园"） | 幼儿园学段任教学科只能填"幼儿园" |
| 小学 | 23 项 | 下拉选择（语文、数学、英语、音乐、美术、书法、舞蹈、心理健康教育、科学、体育与健康等） |
| 初级中学 | 28 项 | 下拉选择（语文、数学、英语、物理、化学、生物、历史、地理、道德与法治等） |
| 高级中学/中职文化课 | 27 项 | 中职文化课可从该库选（语文、数学…思想政治、通用技术等） |
| 中职专业课 | 358 项，含 107 个类别节点 | **只能选具体学科，不能选类别节点**；前端支持关键词搜索、分类筛选、最近使用 |

> 实施：任教学科库作为"标准字典版本"导入系统，支持年度版本启用；codex 先建表与导入器，并用文档示例做种子数据，预留完整库 Excel 导入入口。

### 5.3 业务表（八）
- `student` 学生基本信息：student_no 学号, name 姓名, gender, id_card_type, id_card_no 证件号码, birth_date 出生日期(文本), identity_type 身份类型, source_province/source_city/source_county 生源地三级, source_full 生源地完整文本, college_id, grade 年级, class_name 班级, status, locked 是否锁定
- `training_profile` 专业培养信息：student_id, assessment_year 考核年度, second_discipline_code/name, internal_major_code/name, education_level 学历层次, training_goal 培养目标, internship_org_mode, internship_location, teaching_segment 任教学段, teaching_subject_id 任教学科, interview_org_mode 面试组织方式, ability_test_conclusion 测试结论, status（按年度一对多）
- `process_material` 过程性材料：student_id, assessment_year, category 材料类别, file_id, file_name, file_path, uploader_id, upload_time, first_review_status/first_reviewer/first_review_time/first_review_comment, second_review_status/second_reviewer/second_review_time/second_review_comment, status
- `exemption_request` 免考申请：student_id, assessment_year, subject 免考科目, basis 依据, remark, first_review_*, second_review_*, final_status（一名学生多科 → 多行）
- `exemption_material` 免考佐证材料：exemption_request_id, file_id, file_name（每个免考科目独立上传）
- `video_review` 视频评审主：student_id, assessment_year, video_file_id, duration_seconds 时长, format_check, status, final_conclusion 复评/仲裁结论, arbitrate_reviewer
- `video_review_task` 视频评审任务（每教师一行）：video_review_id, reviewer_id, score, dimension_scores_json 各维度分, comment 意见, conclusion 合格/不合格, submitted 是否提交, submit_time（提交前互不可见）
- `ability_test_result` 测试结果：student_id, assessment_year, exam_org_mode 考试组织方式, exam_subjects 应考科目, score 成绩(文本), conclusion 结论, exemption_relation 免考关系, confirm_status 确认状态, locked
- `certificate` 证书：student_id, assessment_year, cert_no 18位证书编号, training_goal, teaching_segment, teaching_subject, education_level, issuer 签发人, issue_date 签发日期(文本), valid_until 有效期限(文本), status 证书状态, void_reason 作废原因, reissue_origin_cert_no 原证书编号, locked
- `import_export_batch` 导入/导出批次：type[import/export], file_name, batch_no, operator_id, operate_time, total, success_count, fail_count, error_report_file_id, scope_json 范围/筛选条件, strategy 覆盖策略, status
- `import_error_detail` 导入异常明细：batch_id, row_no 行号, field 字段, error_value 错误值, error_reason 错误原因, suggestion 建议处理方式
- `audit_log` 审核日志：biz_type 业务类型, biz_id, target 审核对象, operator_id, operate_time, comment 意见, old_status 原状态, new_status 新状态, operation 操作, ip 操作IP
- `file_object` 文件登记：original_name, stored_name, bucket, object_key, size, content_type, md5, biz_type, uploader_id, upload_time（MinIO 元数据）
- `notification` 通知：user_id, type, title, content, read_flag, created_at
- `cert_sequence` 证书序列：scope_key（school+year[+segment]）, current_seq（带分布式锁，保证连续不间断递增）

### 5.4 关系要点（八）
- 一名学生 → 一个或多个年度考核记录（`training_profile` 按年度一对多）。
- 每名学生四类材料至少各一项（`process_material` category 覆盖 4 类）。
- 一名学生可多个免考科目（`exemption_request` 多行）。
- 一个视频至少两名教师评审（`video_review_task` ≥2）。
- 证书生成后锁定关键字段。

---

## 6. 核心业务规则（必须精确实现，含伪代码）

### 6.1 证件号码校验（来自 6.2 / 附录A / V-05）
按 `id_card_type` 联动正则：
```
居民身份证               ：^\d{17}[\dXx]$        （18位，末位可X）
港澳台居民居住证          ：^\d{17}[\dXx]$              （18位数字）
港澳居民来往内地通行证     ：^[A-Za-z]\d{8}$       （1位字母+8位数字）
五年有效期台湾居民来往大陆通行证：^\d{8}$          （8位数字）
```
不匹配 → 提示"证件类型与号码不匹配"，进入异常清单。

### 6.2 出生日期一致性（6.2 / V-06）
```
if id_card_type in [居民身份证, 港澳台居民居住证]:
    born = id_card_no.substring(6, 14)            # 第7-14位 YYYYMMDD
    if normalize(birth_date) != normalize(born):  # 统一为 YYYYMMDD 比较
        reject("出生日期与证件号码不一致")          # 阻止提交或进入异常清单
```
出生日期对外格式 `1990/6/28`（文本），导入禁止转 Excel 日期序列。

### 6.3 专业代码校验（6.3 / V-08）
```
if identity_type == 教育类研究生:
    assert second_discipline_code[0:4] in {"0401","0451","0453"}  # 否则提示"专业代码不符合规则"
    # 校内专业代码/名称可为空
else:  # 普通师范生等
    # 校内专业代码/名称需与学校试点范围内专业匹配（pilot_scope_flag）
```

### 6.4 实习地点限制（6.1 / 6.3 / V-09）
```
企业  → 仅"职业技术教育专业"可选
海外  → 仅"汉语国际教育专业"可选
默认值：中职教师默认"其他/企业"；高级中学教师默认"中小学"；幼儿园教师默认"幼儿园"
```

### 6.5 任教学段—任教学科联动（6.1 / 6.3 / V-10 / AT-05）
- 任教学科必须来自对应学段标准库，**不得自由填写**。
- 幼儿园学段 → 任教学科只能"幼儿园"。
- 中职专业课 → 只能选具体学科，不能选类别节点（`is_category=1` 不可选）。
- 任教学段变化后，任教学科下拉列表同步刷新（前端联动 + 后端二次校验）。
- 任教学段必须与证书任教学段一致。

### 6.6 证书编号生成（18位，来自 九.1 / W列 / V-11 / AT-10）
```
certNo = year(4) + schoolCode(5) + levelCode(1) + provinceCode(2) + segmentCode(1) + seq(5)

year        = 颁发年份（与签发年份一致），如 2026
schoolCode  = "10588"                          # 学校字典维护
levelCode   = {博士研究生:"1", 硕士研究生:"2", 本科:"3", 专科:"4"}[education_level]
provinceCode= "44"                             # 广东，学校参数维护
segmentCode = {幼儿园:"1", 小学:"2", 初级中学:"3", 高级中学:"4", 中等职业学校:"5"}[teaching_segment]
seq         = 5位零填充，不间断递增。计数器存 cert_sequence 行，与证书写入【同一事务】内 SELECT ... FOR UPDATE 自增（事务回滚则序号回退，保证无空号）；Redis 锁仅作可选互斥，【禁止】用 Redis INCR 当计数器（回滚会留空号）

示例：2026 本科 广东 高级中学 第1号 = 202610588344400001
      2026 本科 广东 中等职业学校 第1号 = 202610588344500001
```
- **序列号作用域**：默认 `school+year+segment`（与示例一致），**作为系统参数可切换为 `school+year`**（见 14 待确认事项）。
- 必须支持一键生成、查重（同证书编号唯一）、锁定、作废重开；允许教务处在权限下导入或更正。

### 6.7 有效期限计算（九.2 / X列 / V-12 / AT-11）
```
issueYear = year(issueDate)
if month(issueDate) <= 6:   validUntil = f"{issueYear+3}/6/30"     # 上半年签发
else:                       validUntil = f"{issueYear+3}/12/31"    # 下半年签发
有效期 3 年；允许有权限者更正。
示例：2022 上半年 → 2025/6/30；2022 下半年 → 2025/12/31
```

### 6.8 证书生成前置条件（6.8 / AT-09）
生成前必须全部满足：基本信息复审通过 + 专业培养信息复审通过 + 过程性考核（四类材料全部复审通过=合格）+ 测试/免考结论有效 + 视频评审通过（是否必须通过由系统参数配置）+ 学院复审 + 教务处最终确认。

### 6.9 过程性考核合格判定（6.4 / AT-06）
四类材料（思想品德及师德素养、教师教育课程学业、教育实习实践、专业能力及技能培训）**全部复审通过**后，过程性考核状态才可为"合格"。

### 6.10 免考联动（6.5 / 6.7 / AT-07）
- 可任选一科或多科免考；每科独立上传佐证材料并二级审核。
- 免考审核通过的科目 → 不再列入应考科目（影响 `ability_test_result.exam_subjects`）。
- 免考 ≠ 过程性考核通过；不得直接覆盖过程性考核结论。

### 6.11 视频评审规则（6.6 / AT-08）
- 视频内容：5分钟说课 + 10分钟课堂教学展示，总时长约15分钟，容差 ±1 分钟（可配置）。
- 格式/大小：MP4，支持**至少 2GB 单文件**，分片上传、断点续传、进度显示、失败重传。
- 校验：上传后校验格式、大小、时长，不符自动提示重传。
- 安全播放：在线播放不强制下载，播放页加用户水印，访问需鉴权（预签名/鉴权代理），未登录复制链接不可访问。
- 评审：一个视频由**两位**专业教师独立评审（可配多专家），提交前互不可见评分与意见；100分制（合格线可配置）；维度见字典 `video_score_dimension`。
- 分差处理：两教师分差超阈值（建议 10-15 分，可配）或一合格一不合格 → 自动进入第三专家评审或学院仲裁（`需复评`）。

---

## 7. 教育部标准导入导出（七 / 附录A） — 26 列 A~Z 完整映射

> 模块 `platform-exchange`。标准上报表必须固定 A~Z 共 26 列，字段名称与顺序与模板一致。**修复 H 列表头为"身份证件号码"**（AT-02）。

| 列 | 字段 | 必填 | 标准规则 | 系统处理 |
|---|---|---|---|---|
| A | 序号 | 是 | 由1顺序编号 | 导入校验连续性；导出自动生成 |
| B | 学校代码 | 是 | 5位高校代码，10588 | 全程文本，禁科学计数法 |
| C | 学校名称 | 是 | 与代码对应 | 默认广东技术师范大学，字典维护 |
| D | 学号 | 建议必填 | 校内唯一 | 去重/查询/对账/学生绑定关键字段 |
| E | 姓名 | 是 | 不含空格、数字、字母及除"·"外特殊符号 | 姓名格式校验（V-04/AT-?） |
| F | 性别 | 是 | 男、女等 | 字典 `gender` |
| G | 身份证件类型 | 是 | 4 种标准值 | 联动号码校验 |
| H | **身份证件号码** | 是 | 模板表头为空，按填表说明第7项补为"身份证件号码" | **下载模板补齐H表头**，文本保存，禁丢前导零 |
| I | 出生日期 | 是 | 如 1990/6/28；身份证/港澳台居住证需与号码一致 | 文本，禁日期序列 |
| J | 身份类型 | 是 | 5 种标准值 | 字典 `identity_type` |
| K | 生源地 | 是 | 精确到区县 | 内部省/市/区县三级，导出拼接完整文本 |
| L | 二级学科（专业）代码 | 是 | 教育类研究生前四位 0401/0451/0453；其他填国内专业代码 | 按身份类型触发校验 |
| M | 二级学科（专业）名称 | 是 | 与代码对应 | 与字典匹配 |
| N | 校内专业代码 | 条件必填 | 普通师范生等须匹配试点；教育类研究生可空 | 按身份类型与试点范围校验 |
| O | 校内专业名称 | 条件必填 | 同上 | 与校内专业代码联动 |
| P | 学历层次 | 是 | 博士/硕士/本科/专科 | 证书编号第10位 |
| Q | 专业培养目标 | 是 | 5 种 | 联动任教学段/实习地点/任教学科 |
| R | 教育实习实践组织方式 | 是 | 学校组织/个人联系 | 字典下拉 |
| S | 教育实习实践地点 | 是 | 幼儿园/中小学/企业/海外/其他 | 按培养目标与专业类型限制 |
| T | 任教学段 | 是 | 5 种 | 任教学科筛选 + 证书编号第13位 |
| U | 任教学科 | 是 | 幼儿园只能幼儿园；其他按标准库 | 不得自由录入，按学段联动 |
| V | 面试考试组织方式 | 是 | 结合实习一并考核/单独面试 | 替换非标准项 |
| W | 证书编号 | 是 | 18位（见6.6） | 一键生成、查重、锁定、作废重开 |
| X | 有效期限 | 是 | 3年；上半年6/30、下半年12/31 | 签发日期自动计算，可权限更正 |
| Y | 证书签发人 | 是 | 校（院）长姓名 | 字典 `cert_issuer` |
| Z | 备注 | 否 | 可空 | 上报说明/异常备注 |

### 7.1 导入要求（7.2）
- 格式：Excel `.xlsx`，模板所有字段预设文本格式；导入后系统值与 Excel 显示值完全一致。
- 模板下载：按年度/学院/专业下载，内置字典下拉，下拉项与系统字典一致。
- 字段校验：必填项、字典项、重复数据、证件号码、出生日期、专业代码、培养目标与任教学段匹配关系。
- 覆盖规则：新增 / 覆盖 / 跳过重复 / 仅更新空字段（导入前管理员选策略 `strategy`）。
- 导入批次：每次形成批次号，记录导入人、时间、文件名、成功数、失败数，可追溯和回滚。
- 异常处理：异常数据不静默入库，可下载异常报告，修改后重导。

### 7.2 导出要求（7.3）
- 格式：标准 `.xlsx`，必要时 CSV；字段顺序与名称与模板一致。
- 文本保护：学校代码、学号、身份证件号码、出生日期、证书编号、有效期限全部文本导出（重开 Excel 无科学计数法/日期序列/前导零丢失）。
- 导出范围：全校/学院/专业/班级/培养目标/任教学段/审核状态/证书状态筛选；保留筛选条件。
- 导出内容：标准上报表、完整审核表、证书获得者汇总表、异常数据表、附件清单表（字段可配，标准上报表固定）。
- 附件导出：附件与视频材料批量打包；Excel 保留文件名、材料类型、审核状态、下载路径。
- 导出审计：记录导出人、时间、范围、文件；敏感数据导出须权限（`export:sensitive`）。

### 7.3 预校验中心 V-01~V-13（7.4，必须逐条实现）
| 编号 | 校验项 | 规则 | 不通过处理 |
|---|---|---|---|
| V-01 | 字段完整性 | 必填非空；条件必填按身份类型判断 | 阻止提交，进异常报告 |
| V-02 | 文本格式 | 关键字段按文本 | 导入转文本，导出设文本格式 |
| V-03 | 学校代码/名称 | 必须对应 | 提示字典不匹配 |
| V-04 | 姓名格式 | 不含空格/数字/字母/除"·"外特殊符号 | 提示姓名格式异常 |
| V-05 | 证件号码 | 按类型校验（6.1） | 提示类型与号码不匹配 |
| V-06 | 出生日期 | 身份证/港澳台居住证与号码一致 | 提示不一致 |
| V-07 | 身份类型 | 属标准字典 | 提示字典项错误 |
| V-08 | 专业代码 | 教育类研究生前4位 0401/0451/0453 | 提示不符合规则 |
| V-09 | 实习地点 | 企业仅职教专业、海外仅汉语国际教育 | 提示不匹配 |
| V-10 | 任教学科 | 来自对应学段库；中职专业课只能具体学科 | 提示学段学科不匹配 |
| V-11 | 证书编号 | 18位各段合法 | 阻止生成或导出 |
| V-12 | 有效期限 | 上/下半年规则、3年 | 提示计算错误 |
| V-13 | 重复数据 | 同证件号码/同证书编号不重复 | 提示重复记录 |

异常报告内容（AT-14）：行号、字段、错误值、错误原因、建议处理方式。

---

## 8. 详细功能模块（六，M01~M14）逐模块任务

> 每模块给出后端接口要点 + 前端页面要点 + 验收关联。完整 CRUD 不再赘述，重点列业务规则点。

### M01 基础数据与字典管理（P0，6.1）
- 后端：字典类型/项 CRUD、年度版本、启用停用；行政区划三级 CRUD + 联动查询；任教学科库导入/查询（关键词、按学段、分类、类别节点不可选）；学校/学院/专业字典（专业多培养目标、试点标识、年度版本）。
- 前端：字典管理、行政区划树、任教学科库（支持关键词搜索/分类筛选/最近使用）、学校学院专业维护。
- 缓存：字典 Redis 缓存，变更失效。

### M02 账号、角色与权限管理（P0）
- 后端：用户/角色/权限/数据范围 CRUD、JWT 登录、验证码、密码加密(BCrypt)、登录失败锁定、`@DataScope` 切面、`@PreAuthorize` 注解。
- 前端：登录页（账号密码+验证码）、用户管理、角色权限分配、数据范围分配；菜单与按钮级权限。
- 预留统一身份认证对接开关。验收 **AT-13**。

### M03 基本信息管理（P0，6.2）
- 批量导入（精确提示行号/字段/原因）、学生确认/补充、证件校验（6.1）、出生日期一致性（6.2）、生源地三级维护导出完整文本、敏感信息脱敏（默认前6后4，导出需权限）。
- 证书生成后关键字段锁定。验收 **AT-01/AT-03**。

### M04 专业培养信息管理（P0，6.3）
- 二级学科代码/名称、校内专业代码/名称、学历层次、培养目标（多选一）、实习地点联动、任教学段、任教学科（标准库联动）、面试组织方式、测试结论。
- 联动规则：6.3/6.4/6.5。培养目标变化自动联动实习地点/任教学段/任教学科。验收 **AT-04/AT-05**。

### M05 过程性考核材料管理（P0，6.4）
- 四类材料上传（PDF/JPG/JPEG/PNG，单附件大小可配）；单/多文件；PDF与图片在线预览；通过前可替换，通过后须退回再替换。
- 审核结论：通过/退回/不通过（退回与不通过必填原因）；初审=教务员，复审=副院长/学院负责人。
- 四项全部复审通过 → 过程性考核"合格"。
- 批量下载（按学院/专业/班级/学生），完整导出表显示文件名、材料状态、审核人、审核时间。验收 **AT-06**。

### M06 免考信息管理（P0，6.5）
- 免考科目按学段配置、可多科；每科独立佐证材料；免考依据字典+文本说明；二级审核（通过/退回/不通过留痕）。
- 结果影响应考科目，不覆盖过程性结论。验收 **AT-07**。

### M07 教学能力视频评审（P0，6.6）
- 分片上传/断点续传/进度/失败重传（≥2GB MP4）；格式大小时长校验；在线播放+水印+鉴权。
- 双教师独立评审（提交前互不可见）、100分制可配合格线、维度评分、意见。
- 分差超阈值/结论冲突 → 第三专家/学院仲裁（`需复评`）。学院负责人确认结果。验收 **AT-08**。

### M08 教师职业能力测试结果管理（P0，6.7）
- 考试组织方式（标准值）；测试成绩可导入/手工录入（文本导出）；测试结论（合格/不合格/免考/待确认）。
- 免考联动应考科目；教务处最终确认后锁定，修改需审批与日志。证书前置之一。

### M09 证书编号与证书管理（P0，6.8 / 九）
- 18位编号一键生成/查重/导入更正；前置条件校验（6.8）；生成后关键字段锁定；有效期自动计算（6.7）。
- 证书状态机（4.3）：待生成/已生成/已签发/已作废/已重开/已导出/已归档；作废重开记录原因/操作人/时间/关联原证书。验收 **AT-09/AT-10/AT-11**。

### M10 教育部标准数据交换中心（P0，七）
- 模板下载（补H表头、内置下拉）、导入（策略/批次/回滚）、预校验 V-01~V-13、异常报告、标准导出/完整审核表/汇总表/异常表/附件清单表。验收 **AT-01/AT-02/AT-14**。

### M11 统计查询与报表（P1，十一）
| 统计项 | 维度 | 输出 |
|---|---|---|
| 学院提交进度 | 学院/专业/班级/学生状态 | 进度表、柱状图、导出Excel |
| 材料完成率 | 四类材料/初复审状态/退回原因 | 统计表、异常清单 |
| 免考统计 | 免考科目/学院/专业/通过/不通过 | 统计表、明细表 |
| 视频评审进度 | 已上传/未上传/待评审/已评审/需复评 | 进度看板、专家任务表 |
| 证书生成统计 | 待生成/已生成/已签发/已作废/已导出 | 证书清单、汇总表 |
| 任教学段/学科统计 | 学段/学科/身份类型/学历层次 | 统计表、交叉表 |
| 异常数据统计 | 字段缺失/证件异常/专业代码异常/证书编号异常/学段学科不匹配 | 异常报告、整改清单 |
| 导入导出日志 | 批次/操作人/时间/成功失败数/范围 | 审计报表 |

### M12 通知提醒（P1）
- 提交提醒、退回提醒、评审提醒、导出完成提醒（站内信 `notification`，预留邮件/短信扩展）。

### M13 系统管理与审计日志（P0，十二）
- 系统参数、存储、备份、日志、导出控制；脱敏与水印；审计日志（登录/查看/下载/导出/审核/修改/作废/重开全留痕，普通管理员不可删除）。

### M14 外部接口扩展（P2）
- 预留：统一身份认证、教务/学籍系统、电子签章/电子证照、上级平台接口（一期仅留扩展点与配置，不实现）。

---

## 9. 非功能需求（十二）

| 类别 | 要求 | 验收口径 |
|---|---|---|
| 安全与权限 | 统一身份认证或账号密码+验证码；按角色/学院/专业/数据范围授权 | 普通用户不得越权访问其他学院/专业数据 |
| 个人信息保护 | 姓名、证件号码、出生日期、成绩、师德材料、视频按敏感数据管理；非必要页面脱敏 | 身份证默认前6后4；敏感导出需权限和日志 |
| 文件安全 | 附件视频访问鉴权，禁公开链接；播放页水印 | 复制链接未登录不可访问 |
| 性能 | 批量导入/导出/下载；视频分片断点续传 | 并发与容量学校确认 |
| 存储 | 支持大视频，单文件阈值（≥2GB）学校确认 | 上传稳定、失败可续传 |
| 可靠性 | 数据库与附件定期备份；逻辑删除 | 可恢复指定时间点 |
| 审计日志 | 全操作留痕 | 日志不可由普通管理员删除 |
| 兼容性 | 主流浏览器；Excel 在 WPS 与 Microsoft Excel 打开格式一致 | 文本字段不被转换 |
| 可配置性 | 字典/证书规则/视频阈值/评分维度/合格线/模板版本可配 | 无需改代码调整常用参数 |

---

## 10. 验收标准映射（十三，AT-01~AT-14）

| 编号 | 验收项 | 对应实现章节 |
|---|---|---|
| AT-01 | 文本字段导入导出一致（无科学计数法/日期序列/前导零丢失） | §0.1, §7.1, §7.2 |
| AT-02 | 标准导出 A-Z 26 列，名称顺序一致，修复 H 列表头 | §7, §7.2 |
| AT-03 | 证件校验 + 出生日期一致拦截 | §6.1, §6.2 |
| AT-04 | 教育类研究生专业代码非 0401/0451/0453 提示异常 | §6.3 |
| AT-05 | 任教学段变化任教学科联动，禁自由填写 | §6.5 |
| AT-06 | 四类材料未全复审通过不可"合格" | §6.9, §M05 |
| AT-07 | 任选一/多科免考，每科独立佐证+二级审核 | §6.10, §M06 |
| AT-08 | 视频≥两教师独立评审，分差超阈值进复评/仲裁 | §6.11, §M07 |
| AT-09 | 满足全部前置条件才允许生成证书 | §6.8, §M09 |
| AT-10 | 证书编号18位自动生成，同年同校序号连续不重复 | §6.6 |
| AT-11 | 有效期上半年+3年6/30、下半年+3年12/31 | §6.7 |
| AT-12 | 全审核操作记录操作人/时间/意见/前后状态 | §4.4, §M13 |
| AT-13 | 学生仅本人、学院仅授权范围、敏感导出有权限和日志 | §3.2 |
| AT-14 | 导入失败生成含行号/字段/错误值/原因/建议的异常报告 | §7.3 |

每阶段交付须附对应 AT 的自测说明或测试用例。

---

## 11. 待确认事项与默认决策（十四）

> codex 按默认决策实现，并将其做成可配置参数，便于后续学校确认调整。

| 事项 | 默认决策（可配置） |
|---|---|
| 统一身份认证 | 一期账号密码+验证码，预留 CAS/OAuth2 开关 |
| 数据来源 | 一期 Excel 导入为主，预留教务/学籍接口 |
| 学号是否必填 | 必填 |
| 证书编号序列作用域 | 默认 `school+year+segment`，参数可切 `school+year` |
| H 列表头 | 系统模板补"身份证件号码" |
| 视频最大文件 | 默认 2GB，参数可调 |
| 视频合格线 | 默认 60，参数可配 |
| 两评委分差阈值 | 默认 12（10-15 区间），参数可配 |
| 视频是否必须通过才能生成证书 | 系统参数，默认"必须通过" |
| 免考依据/可免科目 | 字典维护，初始置空待学校填 |
| 任教学科库维护 | 以模板为基准导入字典，支持年度版本 |
| 部署方式 | docker-compose（校内服务器/私有云均可） |

---

## 12. Docker 部署

`docker-compose.yml` 服务：`mysql:8`、`redis:7`、`minio`、`backend`(platform-boot jar)、`frontend`(nginx 静态 + 反代 `/api`)。
- 环境变量集中 `.env`；数据卷持久化 MySQL/MinIO。
- 后端 `Dockerfile` 多阶段构建（maven 构建 → jre 运行）；前端 `Dockerfile`（node 构建 → nginx）。
- 初始化：Flyway 自动建表 + 种子字典/角色/管理员账号。

---

## 13. 实施阶段与里程碑（codex 按序执行）

> 每个 Phase = 一组可独立提交的任务，含「后端 / 前端 / 验收」。先搭骨架，再按 P0→P1→P2 推进。

### Phase 0 — 工程脚手架与基础设施
1. 初始化 Maven 多模块（§2.4）与 Spring Boot 启动模块；统一 `Result`、全局异常、跨域、Knife4j。
2. 接入 MySQL/Redis/MinIO/Flyway/MyBatis-Plus/EasyExcel；`docker-compose` 起本地依赖。
3. 初始化 Vue3+Vite+TS+Naive UI 前端骨架：路由、Pinia、Axios 封装、布局、主题。
4. 建立 `audit_log`、`file_object`、统一枚举与常量。
- 验收：前后端可启动，Swagger 可访问，登录占位接口连通。

### Phase 1 — 字典与标准数据（M01）
1. `sys_dict_type/item`、`sys_region`、`teaching_subject`、`sys_college/major` 表与 CRUD。
2. Flyway 种子：§5.2 全部字典标准值 + 学校 10588 + 省码 44。
3. 任教学科库导入器（Excel）+ 文档示例种子；前端关键词/分类/类别不可选。
4. 行政区划三级联动接口与前端级联选择。
- 验收：字典/学科库可维护、可联动、可按年度版本启用。

### Phase 2 — 账号角色权限与认证（M02）
1. RBAC 表 + JWT 登录 + 验证码 + BCrypt + 失败锁定；`@DataScope` 切面、`@PreAuthorize`。
2. 7 角色与权限点初始化（§3）；前端登录、用户/角色/权限/数据范围管理、菜单与按钮权限。
- 验收：**AT-13** 越权访问被拦截。

### Phase 3 — 基本信息管理（M03）
1. `student` 表 + 批量导入（行号/字段/原因）、学生确认、证件校验（6.1）、出生日期一致（6.2）、生源地三级、脱敏（前6后4）、字段锁定。
- 验收：**AT-01 部分 / AT-03**。

### Phase 4 — 专业培养信息（M04）
1. `training_profile` 表 + 联动校验（6.3/6.4/6.5）、培养目标多选一、任教学科标准库联动、专业代码校验。
- 验收：**AT-04 / AT-05**。

### Phase 5 — 过程性材料（M05）+ 文件服务
1. `platform-file`：MinIO 上传/预签名/在线预览（PDF/图片）；附件大小可配。
2. `process_material` 四类材料、单/多文件、通过前替换/通过后退回再替换、初审复审（通过/退回/不通过+原因）、四项全通过=合格、批量下载、导出显示文件信息。
- 验收：**AT-06**。

### Phase 6 — 免考管理（M06）
1. `exemption_request/exemption_material`：多科免考、每科佐证、依据字典、二级审核、应考科目联动、不覆盖过程性结论。
- 验收：**AT-07**。

### Phase 7 — 视频评审（M07）
1. 分片上传/断点续传/秒传/进度/失败重传（≥2GB MP4，MinIO multipart）。
2. 格式大小时长校验；在线播放+动态水印+鉴权。
3. `video_review/video_review_task`：双教师独立评审（提交前不可见）、维度评分、合格线/阈值可配、分差→第三专家/仲裁、学院确认。
- 验收：**AT-08**。

### Phase 8 — 测试结果（M08）
1. `ability_test_result`：考试组织方式、成绩（文本）、结论、免考联动、最终确认锁定。
- 验收：证书前置联动正确。

### Phase 9 — 证书编号与证书管理（M09）
1. `certificate/cert_sequence`：18位生成（6.6，分布式锁连续序号）、查重、前置条件校验（6.8）、有效期计算（6.7）、状态机（4.3）、作废/重开留痕、关键字段锁定。
- 验收：**AT-09 / AT-10 / AT-11**。

### Phase 10 — 标准导入导出与预校验中心（M10）
1. 模板下载（补 H 表头、内置下拉、文本格式）。
2. 导入（策略：新增/覆盖/跳过/仅空字段，批次+回滚）+ 预校验 V-01~V-13 + 异常报告（行号/字段/错误值/原因/建议）。
3. 导出（标准上报表 A-Z 固定、完整审核表、汇总表、异常表、附件清单表，全文本保护，导出审计+敏感权限）。
- 验收：**AT-01 / AT-02 / AT-14**。

### Phase 11 — 统计查询与报表（M11）
1. §M11 八类统计 + ECharts 图表 + 导出 Excel。

### Phase 12 — 通知提醒（M12）
1. `notification` 站内信：提交/退回/评审/导出完成；预留邮件短信。

### Phase 13 — 系统管理与审计（M13）
1. 系统参数、备份策略、审计日志查询（按学生/批次/学院/时间）、日志防删、脱敏与水印统一治理。
- 验收：**AT-12**。

### Phase 14 — 非功能、部署、整体验收
1. 安全/性能/兼容性/可配置性收口；浏览器与 WPS/Excel 文本一致性测试。
2. 完整 `docker-compose` 部署、Flyway 初始化、健康检查、README。
3. 按 AT-01~AT-14 走查 + 测试用例归档。

---

## 14. 给 codex 的执行须知

1. **不得简化或跳过任一校验规则（6.1~6.11、V-01~V-13）与任一字段（A~Z 26 列）**；遗漏即不通过验收。
2. 所有"可配置"项落到 `sys_param`/字典表，提供管理界面；禁止硬编码阈值/合格线/学校代码/省码。
3. 文本化字段在实体、Excel 模型、DTO 全链路用 `String`；EasyExcel 单元格格式设 `@`。
4. 每个 Phase 完成后：补单元/集成测试、更新 Swagger、在 PR 描述里列出本阶段覆盖的 AT 项与自测结果。
5. 先交付 P0（M01-M10, M13），再 P1（M11, M12），最后 P2（M14 仅预留）。
6. 数据库变更一律走 Flyway 版本脚本，不手改库。
7. 遇到 §11 待确认事项，按默认决策实现并标注 `// TODO: 待学校确认`，集中在参数页可调。

---

## 15. 详细检查发现与设计增补（对 §1–§14 的精化，codex 须并入实现）

> 本节为对计划的二次详查结果，补齐 §1–§14 中"描述清楚但未落到可执行细节"的部分。**优先级等同正文**；与正文冲突时以本节为准。配套的逐条原子任务见 `tasks.md`。

### 15.0 检查发现清单（Gap List）
| 编号 | 发现 | 影响 | 处置 |
|---|---|---|---|
| G-01 | 状态模型缺"不通过/不合格"终止态（6.4/6.5 审核结论含"不通过"） | 材料/免考无法表达"不通过" | → 15.2 |
| G-02 | 缺角色×权限点完整矩阵 | RBAC 初始化无依据 | → 15.1 |
| G-03 | 数据模型缺 `sys_param`、`major_training_goal`、`training_goal_config`、考核年度、导入回滚追溯表 | 配置/联动/回滚无法落地 | → 15.3 |
| G-04 | 基本信息/培养信息未明确两级审核（正文 M03/M04 仅写"学生确认"） | 与 §4.4 审核表不一致 | → 15.2 / 15.9 |
| G-05 | 姓名格式、身份证校验码、出生日期归一未给精确规则 | 校验歧义 | → 15.4 |
| G-06 | 视频复评/仲裁结算规则未定义 | 终分/结论无法计算 | → 15.5 |
| G-07 | 导出 4 类子表（完整审核表/汇总表/异常表/附件清单表）列未定义 | 导出无法实现 | → 15.6 |
| G-08 | 导入"预校验→确认导入→回滚"两步与模板下拉实现未细化 | M10 实现歧义 | → 15.7 |
| G-09 | 学生账号开通与"当前考核年度"机制未定义 | 学生无法登录/跨年混淆 | → 15.8 |
| G-10 | 证书序列作用域：正文文字"校+年" 与示例"校+年+学段"冲突 | 编号生成歧义 | → 15.10 |

### 15.1 角色 × 权限点矩阵（RBAC 初始化依据）
> 单元格含义：`本`=仅本人 / `院`=本学院或授权专业 / `校`=全校 / `系`=系统级 / `✓`=所有登录用户无范围限制 / 空=无权限。数据范围在权限之上由 `@DataScope`（§3.2）二次过滤。
> 角色列：学=STUDENT 员=COLLEGE_CLERK 长=COLLEGE_AUDITOR 师=REVIEW_TEACHER 处=ACADEMIC_ADMIN 发=CERT_ISSUER 管=SYS_ADMIN。

| 权限点 | 学 | 员 | 长 | 师 | 处 | 发 | 管 |
|---|---|---|---|---|---|---|---|
| `system:user:manage` / `system:role:manage` / `system:perm:manage` | | | | | | | 系 |
| `system:param:manage` | | | | | 校 | | 系 |
| `system:backup` | | | | | | | 系 |
| `audit:view` | | 院 | 院 | | 校 | | 系 |
| `dict:view` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `dict:manage` / `region:manage` / `college:manage` / `major:manage` | | | | | 校 | | 系 |
| `subject:manage` / `subject:import` | | | | | 校 | | 系 |
| `student:view` | 本 | 院 | 院 | | 校 | 校 | |
| `student:import` / `student:edit` | | 院 | | | 校 | | |
| `student:confirm`（本人确认/补充） | 本 | | | | | | |
| `info:firstReview`（基本/培养信息初审） | | 院 | | | | | |
| `info:secondReview`（基本/培养信息复审） | | | 院 | | | | |
| `student:export` | | 院 | | | 校 | | |
| `training:edit` | | 院 | | | 校 | | |
| `training:confirm`（本人选择培养目标等） | 本 | | | | | | |
| `material:upload`（本人上传/替换） | 本 | | | | | | |
| `material:firstReview` | | 院 | | | | | |
| `material:secondReview` | | | 院 | | | | |
| `material:batchDownload` | | 院 | 院 | | 校 | | |
| `exemption:apply`（本人申请） | 本 | | | | | | |
| `exemption:firstReview` | | 院 | | | | | |
| `exemption:secondReview` | | | 院 | | | | |
| `video:upload`（本人上传） | 本 | | | | | | |
| `video:assign`（分配评审教师） | | 院 | 院 | | 校 | | |
| `video:score`（独立评分） | | | | 分配 | | | |
| `video:arbitrate`（第三专家/学院仲裁） | | | 院 | | 校 | | |
| `video:confirm`（学院负责人确认结果） | | | 院 | | | | |
| `video:play`（鉴权播放） | 本 | 院 | 院 | 分配 | 校 | | |
| `test:edit` / `test:import` | | 院 | | | 校 | | |
| `test:confirm`（最终确认锁定） | | | | | 校 | | |
| `cert:generate`（生成编号） | | | | | 校 | | |
| `cert:correct` / `cert:void` / `cert:reissue` | | | | | 校 | | |
| `cert:issue`（签发） | | | | | | 校 | |
| `cert:view` | | 院 | 院 | | 校 | 校 | |
| `exchange:template`（模板下载） | | 院 | | | 校 | | |
| `exchange:import` / `exchange:prevalidate` | | 院 | | | 校 | | |
| `exchange:export:standard` / `exchange:export:full` | | 院 | | | 校 | | |
| `exchange:export:sensitive`（含敏感明文导出） | | | | | 校 | | |
| `stats:view` | | 院 | 院 | | 校 | 校 | |
| `notice:view` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

### 15.2 状态机转换表（补 G-01/G-04，含"不合格"终止态）

**(A) 信息 / 材料 / 免考 通用审核状态机**（基本信息、专业培养信息、四类材料每类、免考每科 各持有一份）
| 当前态 | 事件(操作人) | 目标态 | 守卫 / 动作 |
|---|---|---|---|
| 草稿 | 提交(学生/教务员) | 待初审 | 必填校验通过 |
| 待初审 | 初审通过(教务员) | 待复审 | 留痕 |
| 待初审 | 初审退回(教务员,必填原因) | 初审退回 | 留痕 |
| 待初审 | 初审不通过(教务员,必填原因) | **不合格(终止)** | 留痕 |
| 初审退回 | 重新提交(学生/教务员) | 待初审 | 保留历史版本 |
| 待复审 | 复审通过(副院长) | 复审通过(合格) | 留痕 |
| 待复审 | 复审退回(副院长,必填原因) | 复审退回 | 留痕 |
| 待复审 | 复审不通过(副院长,必填原因) | **不合格(终止)** | 留痕 |
| 复审退回 | 重新提交(修改后) | 待初审 **或** 待复审 | 由参数 `review.return.target` 决定，默认 待初审 |
| 不合格(终止) | 教务处特批重置(可选) | 草稿 | 必须留痕+原因 |

- 材料类聚合规则：四类材料**各自**到达"合格"后，过程性考核聚合状态才置"合格"（§6.9 / AT-06）。
- 免考每科独立走本状态机（§6.10 / AT-07）。

**(B) 视频评审状态机**
| 当前态 | 事件 | 目标态 | 守卫 / 动作 |
|---|---|---|---|
| 待上传 | 学生上传完成 | 校验中 | 触发 格式/大小/时长 校验 |
| 校验中 | 校验通过 | 待评审 | — |
| 校验中 | 校验失败 | 校验失败 | 提示具体原因 |
| 校验失败 | 重新上传 | 校验中 | 断点续传 |
| 待评审 | 分配 N 位教师(默认2,`video.reviewerCount`) | 评审中 | 任务下发，互不可见 |
| 评审中 | 全部提交 且 分差≤阈值 且 结论一致 | 评审完成 | 按 §15.5 结算终分 |
| 评审中 | 全部提交 且 (分差>阈值 或 结论冲突) | 需复评 | — |
| 需复评 | 第三专家评分 / 学院仲裁裁定 | 评审完成 | 按 §15.5 结算 |
| 评审完成 | 学院负责人确认(`video:confirm`) | 已确认(合格/不合格) | 留痕，作为证书前置 |

**(C) 证书状态机**（精化 §4.3）
| 当前态 | 事件(操作人) | 目标态 | 守卫 / 动作 |
|---|---|---|---|
| 待生成 | 生成编号(教务处) | 已生成 | 前置条件全满足(§6.8)+编号查重 |
| 已生成 | 签发(签发人) | 已签发 | 锁定关键字段；记签发人/日期；算有效期(§6.7) |
| 已签发 | 导出(教务处/签发) | 已导出 | 记导出批次 |
| 已导出 | 归档 | 已归档 | 置只读 |
| 已生成 / 已签发 | 作废(教务处,必填原因) | 已作废 | 记原因/操作人/时间/关联原证书号 |
| 已作废 | 重开(教务处) | 已重开 → 新证书(待生成) | 关联原证书号 |

### 15.3 补充数据表（补 G-03）
- `sys_param`(id, param_key UNIQUE, param_value, param_type, param_group, description, editable) — 承载 §11 全部可配置项；Flyway 种子默认值。
- `major_training_goal`(id, major_id, training_goal_code) — 专业↔多培养目标（实现"同一专业可配置多个培养目标"）。
- `training_goal_config`(id, training_goal_code UNIQUE, default_segment, allowed_segments_json, default_internship_location, allowed_internship_locations_json) — 培养目标联动默认值与可选范围（驱动 §6.4/§6.5 前端联动与后端二次校验）。
- 考核年度：`sys_param.current_assessment_year`；如需多年度管理可建 `assessment_year`(year, name, status, start_date, end_date)。
- `import_record_ref`(id, batch_id, table_name, record_id, action[INSERT/UPDATE], before_json, created_at) — 导入回滚追溯（实现 §7.1"可回滚"）。
- `sys_user.student_id` 已预留，导入学生时按 §15.8 开通账号。
- 视频维度权重：`sys_dict_item.ext_json` 存 `{"weight":x,"maxScore":y}`；合格线 `sys_param: video.passLine`。
- 关键索引：`student(id_card_no)`、`student(student_no)` 唯一、`certificate(cert_no)` 唯一、`audit_log(biz_type,biz_id)`、`audit_log(operate_time)`、`import_export_batch(batch_no)`、`video_review_task(video_review_id,reviewer_id)` 唯一。

### 15.4 校验规则精化（补 G-05）
- **姓名**（V-04）：默认正则 `^[一-龥·]{2,}$`（仅汉字+间隔号"·"）；放宽模式（`validate.name.mode=loose`）= 禁止 空格/数字/`A-Za-z`/除"·"外 ASCII 标点，但允许其它 Unicode 文字（兼容少数民族/外文姓名）。
- **居民身份证**：`^\d{17}[\dXx]$`；可选 ISO 7064 MOD 11-2 校验码（`validate.idcard.checksum`，默认 false 仅做格式+出生日期一致）；末位 X 统一大写存储。
- **港澳台居民居住证**：`^\d{17}[\dXx]$`，出生日期取第 7–14 位 `YYYYMMDD`。
- **港澳居民来往内地通行证**：`^[A-Za-z]\d{8}$`（不做出生日期一致校验）。
- **五年有效期台湾居民来往大陆通行证**：`^\d{8}$`（不做出生日期一致校验）。
- **出生日期归一**：存储/导出文本 `YYYY/M/D`；比较时 zero-pad 为 `YYYYMMDD` 再与证件号比对（仅身份证、港澳台居住证，V-06）。

### 15.5 视频复评/仲裁结算（补 G-06，全部参数化）
- 一致区间：`|s1−s2| ≤ video.diffThreshold`（默认 12）且结论一致 → 终分 = `round((s1+s2)/2)`，结论取一致结论。
- 触发复评：`|s1−s2| > 阈值` 或 一合格一不合格（结论冲突）→ 进 `需复评`。
- 复评结算 `video.arbitrate.mode`：
  - `thirdExpert`：第三专家给 s3 → 终分 = 三者中"两两分差最小"的两者均值（默认）；
  - `collegeArbitrate`：学院仲裁人直接裁定终分与结论（留痕）。
- 合格判定：终分 `≥ video.passLine`（默认 60）→ 合格，否则不合格。

### 15.6 导出子表列定义（补 G-07，全部文本字段按 `@` 导出）
- **标准上报表**：A–Z 26 列（§7，固定不可改）。
- **完整审核表**：A–Z + 各环节审核明细：`基本信息[初审人/时间/状态][复审人/时间/状态]` + `培养信息[同上]` + `四类材料各[状态/审核人/时间]` + `免考各科[科目/状态/审核人/时间]` + `视频[教师1分/教师2分/复评分/终分/结论/确认人/时间]` + `测试[成绩/结论/确认人]` + `证书[状态/编号/签发人/签发日期/有效期限]`。
- **证书获得者汇总表**：学号、姓名、身份证件号码、身份类型、学历层次、培养目标、任教学段、任教学科、证书编号、签发人、签发日期、有效期限、证书状态。
- **异常数据表**：批次号、行号、学号/姓名、字段、错误值、错误原因、建议处理方式。
- **附件清单表**：学号、姓名、材料类别、文件名、材料状态、审核人、审核时间、下载链接。

### 15.7 导入两步流程与模板实现（补 G-08）
1. **预校验（不入库）**：上传 → EasyExcel/FastExcel 全部读为 `String` → 逐行执行 V-01~V-13 → 产出 `{总行数, 成功行预览, 失败行+异常报告}`，前端展示并可下载异常报告（§15.6 异常数据表）。
2. **确认导入**：选择策略（新增/覆盖/跳过重复/仅更新空字段）→ 事务批量入库 → 写 `import_export_batch` 与 `import_record_ref`。
3. **回滚**：按 `batch_id` 反向应用 `import_record_ref`（INSERT→逻辑删除，UPDATE→还原 `before_json`）；若记录已被后续操作修改则跳过并提示冲突。
4. **模板下拉**：Apache POI `DataValidationHelper`（显式/隐藏 sheet 承载字典与学科库选项）；所有列单元格格式预设 `@`；**H 列表头写"身份证件号码"**（AT-02）。

### 15.8 账号开通与考核年度（补 G-09）
- 导入学生时按参数 `student.autoCreateAccount`（默认开）创建 `sys_user`：`username=学号`，初始密码 = `student.defaultPwd`（或证件号后 6 位，参数可选），角色 `STUDENT`，绑定 `student_id`，标记强制首次改密。
- `current_assessment_year` 控制默认操作年度；查询、统计、导出默认按当前年度过滤；历史年度数据只读保留。

### 15.9 M03/M04 审核补充（补 G-04）
- 基本信息、专业培养信息与材料/免考一致，均走 **教务员初审 → 副院长复审**（§4.4 已列），状态机用 §15.2-(A)；正文 M03/M04 的"学生确认/补充"之后必须接入两级审核与状态流转。对应权限点 `info:firstReview` / `info:secondReview`。

### 15.10 证书序列作用域裁定（补 G-10）
- 冲突：§9.1 文字为"同一学校同一年度顺序不间断递增"（= 校+年），但两条示例（高中/中职均为 00001）暗示 校+年+学段。
- 处置：作用域用参数 `cert.seq.scope` ∈ `{SCHOOL_YEAR, SCHOOL_YEAR_SEGMENT}`，默认 `SCHOOL_YEAR_SEGMENT`（与示例一致）；`cert_sequence.scope_key` 据此拼装；**上线前向学校书面确认**。

---

（完）本计划已覆盖需求报告 第一~十四章 + 附录A/B 全部条目，并经二次详查补齐 10 项执行细节（§15），可直接作为 codex 分阶段开发依据。逐条原子任务见同目录 `tasks.md`。
