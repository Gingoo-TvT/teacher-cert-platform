<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>cn.edu.gpnu</groupId>
        <artifactId>teacher-cert-platform</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>platform-boot</artifactId>
    <description>启动模块：聚合、全局配置、Swagger、跨域、全局异常</description>

    <dependencies>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-common</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-system</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-security</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-business</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-exchange</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-statistics</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.edu.gpnu</groupId>
            <artifactId>platform-file</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>teacher-cert-platform</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- Phase 0 写前 fresh attestation 在同一正式 JVM 首次 context refresh 时缓存；
                         后续 Phase00 IT context 逐次重验目标身份并复用首次空栈计数。 -->
                    <forkCount>1</forkCount>
                    <reuseForks>true</reuseForks>
                    <systemPropertyVariables>
                        <video.probe.fat-jar>${project.build.directory}/${project.build.finalName}.jar</video.probe.fat-jar>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
