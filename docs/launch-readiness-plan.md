# 上线就绪计划（Demo → 正式上线过渡）

> **定位声明：本项目已从 demo 阶段进入"以正式上线、正常对外服务为唯一目标"的过渡期。**
> 此后所有工作的验收口径是"上线后真实师生规模（数千学生、截止日高并发）下正常服务"，而非"演示可用"。
> 本文档 = 全库上线就绪审计结论（2026-07-03，Opus 4.8 复核 + Sonnet 5 审计代理逐文件扫描，均有 file:line 依据）+ 分级修复计划。
> 执行模式：**Opus 4.8 亲自做关键/安全/架构项，Sonnet 5 代理做机械铺开项**；每项完成按门禁流程验证合并。

---

## 0. 已在 Phase 36 顺手落地的
- ✅ Spring multipart 上限（原 P0-3）：`application.yml` 增 `max-file-size: 100MB / max-request-size: 120MB`——此前框架默认 1MB/10MB，扫描件/名册 Excel 超 1MB 直接被拒，业务上限（50MB）形同虚设。
- ✅ 审计日志去噪（用户反馈#3）：删 23 处冗余 `@AuditLog`（分片级/薄厚双记/纯读），审计只留有价值记录；`mvn verify` 83/83 绿。

## 1. P0 —— 上线阻断项（不修不能上线）
| # | 问题 | 位置 | 危害 | 修复 | 归属 |
|---|---|---|---|---|---|
| P0-1 | **文件预签名 IDOR**：`presignedGet` 只查文件存在，**无属主/数据范围校验**，expiry 客户端任填无上限 | `FileController.java:50-54`、`FileServiceImpl.java:62-77` | 任何登录学生可凭任意 fileId 下载他人证件照/视频/材料，还能签发超长期链接 | 按文件所属业务记录做权限校验（复用 @DataScope 口径）；服务端强制 expiry 上限（如 ≤15min） | **Opus（安全关键）** |
| P0-2 | ~~**批量下载整包进堆**：`batchDownload` 无行数上限，全部文件读进 `ByteArrayOutputStream` 再返回 byte[]~~ | ~~`ProcessMaterialServiceImpl`~~ | — | **已修（Phase 38a，见 §11）：zip 直写响应输出流 + 2000 条上限** | 完成 |
| P0-3 | ~~multipart 上限缺失~~ | ~~application.yml~~ | — | **已修（见 §0）** | 完成 |
| P0-4 | **无 prod profile，Swagger/Knife4j 对公网全开**：`SecurityConfig` permitAll `/doc.html`/`/v3/api-docs`，且无 `application-prod.yml`（compose 却默认 `SPRING_PROFILES_ACTIVE=prod`） | `application.yml:17-20`、`SecurityConfig.java:61`、`docker-compose.yml:56` | 生产接口文档+试调界面公开 | 新增 `application-prod.yml`：关 knife4j/springdoc；prod 下移除文档路径放行 | Opus |
| P0-5 | ~~**应用用 root 连 MySQL**~~ | ~~`docker-compose.yml:68-69`~~ | — | **配置就绪已合并（Phase 41，见 §11）：`teacher_app` 最小权限账号（`deploy/mysql-init/01-app-user.sh`）+ `application-prod.yml` env 化 + 3306 不 publish** | 已合并 |
| P0-6 | ~~**备份是"演练记录"非真备份**：triggerBackup 仅插一行 COMPLETED；全库 0 处 `@Scheduled`；运维手册命令指向 dev 容器/密码~~ | ~~`SystemManagementServiceImpl.java:132-146`~~ | — | **已实现已合并（Phase 41.2，见 §11）：`DatabaseBackupService` JDBC 逻辑导出→gzip→MinIO(`db-backup/`) 真实产物 + `backup_record` 关联真实 size/checksum/表数/行数 + RUNNING→COMPLETED/FAILED；`BackupScheduleConfig` 定时(prod 门禁)；手册改指 prod 栈（迁移 V25）** | 已合并 |

## 2. P1 —— 上线前应修（规模/一致性风险）
| # | 问题 | 位置 | 修复 | 归属 |
|---|---|---|---|---|
| P1-1 | **全站假分页**：11 文件 14 处 `new PageResult<>(records.size(), records)` 全量返回，前端客户端分页；千级学生后列表接口变慢、内存放大 | student/training/material/exemption/cert/test/video/exchange/security/notification/system 各 ServiceImpl | 切 MyBatis-Plus `selectPage` 真分页（契约已有 total 字段，前端 DataPanel 已用后端 total——改动集中在后端 + 前端各页把筛选改为服务端参数）。**✅ 已全部转真分页（14/14）：2 契约样例（Phase 44e-contract）+ 剩余 12 处 rollout（Phase 44e-rollout，见 §11）。** 分页拦截器顺序已核验正确；`page`(默认1)/`size`(默认20/上限200) 契约 + 共享 `PageQuery`；listUsers/training/cert/material/test/exemption/video.list（变体 A，`@DataScope`）、video.myTasks/notice（变体 B'，当前用户 wrapper 自范围）、backups（变体 B，无范围）、exchange.batches（变体 C，Java 范围下推 wrapper）、auditLogs（变体 D，自定义 mapper 改 `IPage`）逐一转换，均以 IT 证「分页×数据范围」total/records 同受范围过滤；`DataPanel` 向后兼容 `remote` 分页、`NoticeCenterView` 用独立 `n-pagination` 特例。 | **Sonnet 铺开 + Opus 定契约与抽查（已闭环，见 §11）** |
| P1-2 | **视频上传全量过应用服务器** + 512KB 分片致 composeObject 永远失败走"逐片拉回重传"慢路径 | `VideoReviewController`、`VideoReviewServiceImpl:211-254,750`、前端 chunkSize | 见 §4（分两阶段）。**阶段1 已完成（Phase 49，见 §11）：前端分片 512KB→8MiB（命名常量 `VIDEO_UPLOAD_CHUNK_SIZE`）+ `merge()` 按分片实际大小显式路由走服务端 `composeObject` 快路径（≥5MiB 免逐片拉回重传）、保留小分片回退 + 活体 IT 证快路径；阶段2（`S3Presigner` 预签名浏览器直传 MinIO，字节完全不经 app）仍推迟** | Opus |
| P1-3 | ~~统计模块 Java 全表聚合（8 处全表 select 后内存 group）~~ | ~~`StatsServiceImpl.java`~~ | **已完成（Phase 44d，见 §11）：证书/视频/交叉/免考四类聚合下推 SQL `GROUP BY/COUNT/DISTINCT`（新增 `StatsAggregationMapper`），明细改有界 `LIMIT 200`；数据范围经 `scopedStudents()` 以 `student_id IN (scopedIds)` 显式保留（统计链路 `DataScopeContext` 为空、拦截器不生效）。材料/提交/异常/批次四类诚实保留 Java（各有理由）** | 待复核合并 |
| P1-4 | ~~万行 Excel 导入同步逐行处理 vs 前端 30s 全局超时 → 客户端先超时、疑似失败重复提交~~ | ~~`ExchangeServiceImpl.java`、`request.ts`~~ | **已完成（Phase 50，见 §11）：幂等防重早由 Phase 42.2 `confirmImport` 原子认领闭环；本相前端对 prevalidate/confirm/大导出加 5 分钟超时消除误报超时（未改异步轮询——非必要的更大契约改造）** | 已合并 |
| P1-5 | ~~通知扇出逐条 INSERT~~ | ~~`ReviewNotificationHelper.java:100-105`~~ | **已修复（Phase 44a，见 §11）：`NotifyChannel.sendBatch`（default 逐行兜底，`InAppNotifyChannel` 覆盖为 `Db.saveBatch` 真批量）+ `NotificationService/Impl.sendBatch`，`notifyRole`/`notifyVideoAssigned` 改单次批量插入** | 已合并 |
| P1-6 | ~~N+1：`attachmentRows` 循环 selectById~~ | ~~`ExchangeServiceImpl.java:952-985`~~ | **已修复（Phase 44a，见 §11）：`attachmentRows` 内学生查询改 `selectBatchIds`+`Map<Id,Student>`** | 已合并 |
| P1-7 | ~~CORS 生产域名未配置且无文档，默认仅 localhost~~ | ~~`CorsConfig.java:27`~~ | **已完成（Phase 46，见 §11）：`CorsConfig` 早已 env 化读 `CORS_ALLOWED_ORIGINS`；补 `.env.example` + `docker-compose.yml` backend 透传 + `docs/phase-14` 部署变量说明** | 已合并 |
| P1-8 | 学生初始密码=身份证后 6 位（可预测；已有强制首登改密+锁定缓解，但存在抢先注册窗口） | `StudentServiceImpl.java:351-361` | 初始密码加盐随机后缀经名册下发，或首登绑定学号+证件双因子核验 | Opus 定策略 |
| P1-9 | ~~全库无任何定时任务：上传残片/孤儿对象、audit_log/notification 只增不清~~ | ~~全局~~ | **已完成（Phase 47，见 §11）：`CleanupScheduleConfig` 调度层（prod 门禁、同 `BackupScheduleConfig`）——① `RetentionCleanupService` 分批 LIMIT 物理清理 audit_log(默认180d)/notification(默认90d，绕 `@TableLogic` 真删)、保留期经 `ParamService` 可调；② `FileMaintenanceService` 幂等设 MinIO `AbortIncompleteMultipartUpload` 生命周期规则（8.5.12 无 list/remove 未完成分片 API，服务端 abort）；③ `FileObjectScanMapper` 孤儿 file_object 扫描仅报告不删（NOT EXISTS 覆盖 5 处业务引用）** | 待复核合并 |
| P1-10 | ~~兜底异常返回 HTTP 200（监控/告警失明）~~ | ~~`GlobalExceptionHandler.java`~~ | **已完成（Phase 46，见 §11）：兜底 `Exception→500` + 客户端错误 `405/404` 处理器（错方法/错路径不再落 500）+ 前端拦截器提取非 2xx 的 Result.msg；业务/校验仍 200+码** | 已合并 |

## 3. P2 —— 上线后迭代
- compose 无资源限额（`deploy.resources`）；Hikari 未调优（按并发压测定 pool）；
- 前端：localStorage token 的 XSS 面（已有 TTL 缓解）、全局 errorHandler/router.onError 兜底、请求重试；
- naive-ui 全量注册（已 manualChunks 隔离缓存，可再按需引入）；echarts 按需引入（仅 bar/pie）；
- 播放/下载走 CDN；MinIO 纠删码集群或云对象存储。

## 4. 视频/文件上传上线方案（承接"上传慢"结论，两阶段）
**阶段1（过 app 优化，内测够用）**：前端分片 512KB→8MB + 并发 4–6 + `crypto.subtle` 哈希（Worker）；后端 ≥5MB 走 `composeObject` 服务端合并（修掉必然降级的慢路径）。
**阶段2（上线本体，字节不经 app）**：`init` 由后端（AWS S3 SDK `S3Presigner` 指向 MinIO）发 **multipart 预签名 UploadPart URL 列表** → 浏览器并发直传 MinIO → `complete` 交回 `[partNumber, ETag]` 由后端 `CompleteMultipartUpload` 定稿+落库+触发校验；预签名短时效+限型限长；MinIO 生命周期自动 abort 未完成 multipart；配套异步转码（FFmpeg 统一 H.264/AAC+缩略图+服务端真实时长）与削峰限流。
> 秒传/断点续传语义保留：init 前指纹查重（大小+分块采样哈希，Worker 计算）；进度=已完成分片集。

## 5. 执行序（建议）
1. **Phase 37（安全急修，Opus）**：P0-1 IDOR + P0-4 prod profile/文档关闭 + P1-10 异常状态码 → 全量 verify + 活体渗透自测。
2. **Phase 38（容量正确性）**：P1-1 真分页（Sonnet 铺开 11 文件 + 前端参数化，Opus 契约与门禁）+ P0-2 流式 zip + P1-3 SQL 聚合。
3. **Phase 39（上传直传阶段2 + P1-4 异步导入）**：契约改造，Opus 主刀。
4. **Phase 40（运维基建）**：P0-5/P0-6/P1-7/P1-9（账号、真备份、CORS、定时清理），Sonnet 脚本化 + Opus 审。
5. 上线冒烟：`docker compose up -d --build` 全栈 + README 双端核对 + 压测（列表/导入/上传/统计并发基线）。

## 6. 门禁口径（不变 + 加严）
- 每阶段：`mvn -B -ntp verify` 全绿 + `type-check/build` 绿 + 活体多角色走查零 403/500；涉契约改动加"前端旧行为兼容"检查。
- 新增：安全项修复必须附**攻击复现→修复后阻断**的活体证据（如 IDOR：学生 token 取他人 fileId 必须 403）。
- git 仍本地私有（无 remote/不 push），上线部署另行导出。

---

## 7. 二轮深度扫描（2026-07-03，6 维并行审计，均 file:line 依据）
> 覆盖：并发/事务、规模/性能、证书/导入正确性、领域规则合规、前端、部署运维（部署维仍在收尾，见 §7.8）。下列均为 §1–§3 之外的**新发现**，去重后按主题+分级。**好消息在 §7.5/§7.9**：领域业务规则 11/12 后端强校验、无缺失索引、证书序列号并发安全、无 SQL 注入。

### 7.1 并发一致性 —— 系统性无乐观锁（最重要新发现）P0
根因：`platform-common/.../entity/BaseEntity.java` **无 `@Version` 乐观锁列**，所有 mapper 是裸 `BaseMapper`。所有"状态流转"都是 `selectById→Java 查状态→updateById(id)`，**无版本、无 SELECT FOR UPDATE、无 UPDATE...WHERE status=** 前置条件 → **TOCTOU 竞态**（双击/重复请求/双评审都过守卫都提交，后写覆盖先写，PASS 可覆盖 REJECT）。
- 涉及：`StudentServiceImpl:139-220`、`TrainingProfileServiceImpl:116-197`、`ProcessMaterialServiceImpl:133-222`、`ExemptionServiceImpl:210-307`、证书 5 流转 `CertificateServiceImpl:137-268`、视频 `VideoReviewServiceImpl` submitScore:356/settle:824/thirdReview:378/arbitrate:402/confirm:423、`ExchangeServiceImpl.confirmImport:230-271`。
- **缺唯一约束致并发重复**：证书 `(student_id, assessment_year)` 仅非唯一索引（`V15:31-36`）→ 并发生成两张有效证书【✅ Phase 42.1，已合并：V24 生成列 `active_key` + `uk_cert_active`】；`student.id_card_no` 非唯一（`V9:31`）→ 并发建同证件号两人【✅ Phase 42.1，已合并：V24 生成列 `idcard_key` + `uk_student_idcard`】；`training_profile` 导入无唯一码 → confirmImport 双确认产生重复培养行【✅ Phase 42.2，已合并：confirmImport 开头 PREVALIDATED→IMPORTING 原子认领，仅首个确认进导入循环，杜绝双确认全量重导（含 training_profile 重复行）】。
- **video merge() 无幂等守卫**（`VideoReviewServiceImpl:211-254`，不同于 uploadChunk 有 status 守卫）→ 重试产生重复 `file_object` 行 + MinIO 孤儿对象 + 重指 videoFileId。**✅ 已修复（Phase 42.3，已合并，见 §11）：** `VideoUploadStatus` 加 `MERGING` 过渡态，merge 在分片校验后、compose 前**原子认领** `UPLOADING→MERGING`（merge 无环绕事务、update autocommit 立即可见、InnoDB 行锁串行化），仅首个 `claimed=1` 者执行 compose+register；负者按会话态幂等回放（MERGED/VALIDATION_FAILED→既有 detail）或拒绝（MERGING→「视频正在合并或已完成」），失败复位 MERGING→UPLOADING 防卡死；保留 37c 的 MinIO 出事务。真栈并发 IT 证并发/重试合并后 `file_object`(teaching-video) 恰 1 行。
- **video settle 丢失**（`:824-848`）：两评审并发提交最后一分，REPEATABLE_READ 下各自快照只见自己那条 → 都不触发结算 → review 卡在 REVIEWING 且无法重提，需人工。**✅ 已修复（Phase 42.3，已合并，见 §11）：** submitScore 对 review 行 `SELECT ... FOR UPDATE` 悲观锁串行化并发结算 + settleIfReady 计票改锁定读（现读、跨事务可见已提交任务）+ 结算落库改原子条件更新 `eq(status,'REVIEWING')`——三者合力保证恰一次结算、review 不卡 REVIEWING；`thirdReview`（settleThirdExpert 原子更新 `eq(status,'NEED_REVIEW')`）/`returnReview`（updateReviewReturned 加 `eq(status,oldStatus)` 守卫）同步补 37b 原子状态守卫。真栈并发 IT 证 2 并发末分提交恰一次结算 REVIEW_COMPLETED、settle 审计恰 1 条。
- **秒传越权**：`FileServiceImpl.getByMd5` 无 bizType/属主校验（`VideoReviewServiceImpl:125-138`）→ 可命中挂载任意他人文件。
→ 修复主线：给 BaseEntity 加 `@Version`（系统性解大部分）+ 关键流转改条件 UPDATE/行锁 + 补 `(student_id,assessment_year)` 等唯一约束。

### 7.2 事务边界与资源 P0/P1
- **P0 MinIO I/O 在 `@Transactional` 内**（`VideoReviewServiceImpl` uploadChunk:162,178 / merge:211,236）→ MinIO 网络往返期间**占用 DB 连接**；截止日并发大上传耗尽 Hikari 连接池（默认 10）→ **全站 DB 阻塞=总瘫**。这是运维上最危险的一条。修：MinIO 调用移出事务，DB 元数据单独短事务。**✅ 已修复（Phase 37c，见 §11）：** uploadChunk/merge 去方法级 `@Transactional`，`putObject/composeObject` 在事务外执行，元数据落库改 `TransactionTemplate` 短事务。**收尾✅（Phase 37c-2）：** material/exemption 4 个上传方法（material `upload`/`replace`、exemption `uploadMaterial`/`replaceMaterial`）去方法级 `@Transactional`，`fileService.upload` 的 MinIO putObject 不再占用 DB 连接（各方法仅 1 次业务写，autocommit 原子）。
- **P1 审计/通知写入吞异常**（`AuditLogServiceImpl.record:22-41`、`ReviewNotificationHelper.notifyXxx` 只 log.warn）→ 在 `@Transactional(rollbackFor=Exception)` 内失败**不触发回滚** → 状态改了但无审计（正是要防的不一致）。
- **P1 `ExchangeServiceImpl.confirmImport` 无 `@Transactional`**（`:229-271`）：状态末尾才翻转，双确认全量重导。**✅ 已修复（Phase 42.2，已合并，见 §11）：** confirmImport 开头**原子认领** `batchMapper.update(null, eq(id).eq(status,'PREVALIDATED').set(status,'IMPORTING'))`——无环绕事务、update 立即自动提交、对并发调用者立即可见（InnoDB 行锁串行化），两并发只有首个 `claimed=1` 进导入循环、另一个 `claimed=0` 抛「当前批次不可确认导入（可能正在导入或状态已变更）」；`ExchangeBatchStatus` 加 `IMPORTING` 过渡态，`rollback` 纳入 IMPORTING（崩溃残留可回收），收尾写以「仍为 IMPORTING」守卫防并发 rollback 终态被覆盖。真栈并发 IT 覆盖（恰 1 导入、1 被拒、无重复行）。
- **P1 `FileServiceImpl.upload` InputStream 未关闭**（`:32-59`，无 try-with-resources）→ 高并发泄漏流/socket。**✅ 已修复（Phase 41，已合并，见 §11）：** 改用 `try (in) { minioClient.putObject(...) }`（Java 9+ 等价 try-with-resources），MinIO putObject 已按声明 size 同步读完流，返回前即可安全关闭；5 处调用方（`FileController.upload`、material `upload`/`replace`、exemption `uploadMaterial`/`replaceMaterial`）均不在 upload 返回后复用该流。

### 7.3 规模/性能 P1（缺失索引=无，见 §7.9）
- **N+1 `toVO`/导出（5 处）**：`VideoReviewServiceImpl.toVO:1011,1034,1060`（三重嵌套）、`TrainingProfileServiceImpl.toVO:329`、`AbilityTestResultServiceImpl.toVO:227,233`、`ExchangeServiceImpl.rowFromCertificate:836-841`/`fullReviewRows:894-899`。正确批量范式在 `ExemptionServiceImpl.toVO:384`/`ProcessMaterialServiceImpl:379` 已有——照抄 `.in()`+Map。**✅ 已修复（Phase 44a，已合并，见 §11）：** 三个 `toVO`（Video/Training/AbilityTestResult）的 `list()` 路径新增批量 `toVOs`/`toVOList`——先 `selectBatchIds`/`.in(ids)` 一次性取值建 `Map<Id,Entity>`（Video 另按 reviewId 批量取 task、按 task 去重批量取 reviewer），原单行 `toVO(entity)` 改委托新增多参重载、供 `get()/detail()` 复用（行为不变）。`ExchangeServiceImpl` 的 `rowFromCertificate`/`fullReviewRows`/`certSummaryRows`（同调用链同款 per-cert `selectById`，一并纳入）+ `attachmentRows`（即 P1-6）同款批量化，training/video/material 按 `(studentId, assessmentYear)` 复合键、采「按 studentId 批量 `.in()`+`groupingBy` → 内存按 year `filter+findFirst`」范式重现原 `LIMIT 1` 语义。VO/行字段与顺序逐一核对不变，纯查询策略改造，`mvn verify` 93/93 绿。
- **`StatsServiceImpl.anomalyReport:319-345`**：对全体学生逐个跑创建期校验器（`TrainingLinkValidator` 每学生 ~7 查询）→ 一次 HTTP 数万次串行查询。
- **POI 全 DOM 进堆 + 无界 rowset**（`ExchangeServiceImpl:159` `file.getBytes()`+`XSSFWorkbook`；导出 `selectCertificates:812` 无界）→ 大批次 OOM。改 SXSSF 流式 + 分页。**✅ 导出侧已修复（Phase 44b，见 §11）：** `ExchangeExcelHelper` 两个写入方法（`writeStandardWorkbook`/`writeTableWorkbook`）改 `SXSSFWorkbook`（窗口 200 行流式写，`dispose()`+`close()` 清理临时文件），不再整表 DOM 进堆；`selectCertificates` 加单次导出 20000 行上限，超限抛业务异常引导缩小筛选范围，避免筛选过宽时整段结果集常驻堆内存。产出仍为标准 OOXML 字节流，列结构/取值不变（`Phase24AcceptanceIT` 仍绿）。**导入侧 `file.getBytes()`（`:159`，上传文件读入内存）不在本次范围，未改动**——该路径受 Spring multipart 上限（`max-file-size:100MB`，见 §0）约束，且本次任务范围明确限定"证书导出内存"，非本次指派项。
- **prevalidate 每行重查字典**（`:147-211` 单大事务，每行 validateSchool/Id/IdentityType/certCode 无批内缓存）→ 一次导入数万次字典查询。
- **非 sargable `%kw%` 前导通配 LIKE 全表扫**（8 表）：Student:74 / Training:230 / Certificate:289 / Material:316 / Exemption:371 / AbilityTest:213 / Video:1003 / Exchange:804。
- **参考数据/权限未缓存**：`ParamServiceImpl` 每 getX 一次 DB（证书生成读 ~6 参数/请求）；`DataScopeServiceImpl.resolve` 每次权限判定最多 3 查询无 memo；`RegionServiceImpl` 全无缓存且 `path()` while 循环逐级查；`OrganizationServiceImpl` majors 列表 ~3N+1（toMajorVO:453 + 每行 3 次字典 selectList）；`NotificationServiceImpl.list:55` 无界。**✅ 参考数据已缓存（Phase 44c，见 §11）：** Spring Cache + Caffeine 进程内缓存 + 写时逐出——Param（getX，`updateParam` 逐出）、Region（children/path/fullName，无写路径靠 TTL）、字典标签（4 处私有 `dictLabels` 收敛到 `DictService.dictLabels`）、组织专业字典（`globalEnabledDictItems` 消 majors 3N+1）；字典项/类型增改删经 `evictItemsCache` 同步逐出 Caffeine。**`DataScopeServiceImpl.resolve` 授权缓存诚实推迟**（失效面覆盖全部 RBAC 写、角色级变更扇出到多用户，无法可靠逐出；陈旧＝越权安全缺陷）。`NotificationServiceImpl.list` 无界非缓存目标、未碰。`mvn verify` 96/96 绿。
- **`AuditLogAspect:32-46` 同步阻塞写**（非 @Async），每个 @AuditLog 变更端点多付一次阻塞写。**⏳ 已尝试、本次未采用（Phase 44b，诚实记录，见 §11）：** 曾完整实现 `@Async` 改造（操作人/IP 等 ThreadLocal 字段在请求线程同步捕获后再转交异步落库 + dev/test 用 `SyncTaskExecutor` 保 IT 确定性），但经受控消融实验证实：引入 `@EnableAsync`/新异步基础设施后，**与审计功能完全无关**的 `Phase5MaterialIT`/`Phase6ExemptionIT`（材料复审/免考评审→通知批量插入路径）出现间歇性 MySQL `Lock wait timeout exceeded`（干净 main 0/93 失败；同一分支同等条件下 3 次独立 full-suite 运行 3/3 复现失败；仅回退审计异步改造、其余改动原样保留重跑即恢复 93/93 绿）——已定位为该项改造引入、非环境噪音，但未能在预算内查明 Spring AOP/事务边界层面的确切触发机制。按任务书 Item 2 逃生舱条款推迟，详见 §11 Phase 44b。

### 7.4 证书/导入领域正确性 P1
- ✅ **REISSUED 从不落库（死枚举）**（原 `CertificateServiceImpl.reissue` 只 setLocked，never setStatus → 原证保持 VOIDED → **作废证书可被反复重开**无界链；审计记录了没发生的流转）——**已修复（Phase 42.4，已合并）**：reissue 改 DB 原子条件更新 `update(entity, eq(id).eq(status,'VOIDED'))` 把原证 VOIDED→REISSUED 落库、行数=0 抛「证书状态已变更或已重开」；活体 2 并发仅 1 成功、原证 REISSUED、再重开被拒（详见 §11）。
- ✅ **导入静默改 voided/archived 证书**（`certificateByStudentYear:1202` 无状态过滤，`applyCertificate:717-740` 覆盖 cert_no/validUntil/issuer）——与 `correct()` 的守卫矛盾。**已修复（Phase 48，待复核合并，见 §11）：** `importOne` 在学院权限校验后、`applyCertificate` 前**镜像 correct() 终态守卫**——命中既有证书为 VOIDED/REISSUED/ARCHIVED 即抛「证书…不可通过导入修改」，由 `confirmImport` 逐行回滚该行 `REQUIRES_NEW` 事务并上报逐行错误（被命中证书分毫不动）；**同相一并修复 P2「`correct()` 不校验 cert_no 内嵌码与学段/层次一致」**（`ensureCertNoMatchesSegmentAndLevel`：仅当更正触及 `certNo`/`teachingSegment` 时校验 idx9 学历码/idx12 学段码，规则同导入 `validateCertificateNo`、复用 `certCode`）。真栈复现→阻断 IT（`Phase48CertImportGuardIT`）。
- ✅ **往返列复用坏账**：导出把 `status` 写入"备注"列（`:865`），导入把该列当学院id `parseLong`（`:1058`）→ **重导入导出文件必坏**（学院识别失败）。**已修复（Phase 43.2，已合并，见 §11）：** `rowFromCertificate` 备注列改写「学院ID」（`cert.getCollegeId()`），与导入端 `resolveCollegeId` 读备注解析学院ID 对齐 → 导出→导入 学院标识无损往返；证书状态另由汇总表/完整审核表的「证书状态」专列承载，不再复用备注一列表达两种含义（模板 26 列 A-Z 布局未变，仅修正自由「备注」列内部语义）。IT 复现→阻断（导出备注=学院ID 非"ISSUED"、读回重导入仍落原学院）。
- ✅ **导入 cert_no 不占 `cert_sequence`** → 与自动生成永久撞号，`generate` 撞号回滚又不推进序列 → "证书编号已存在请重试"**永远失败**，需手工改库。**已修复（Phase 43.2，已合并，见 §11）：** 导入证书落库后调 `CertificateService.reserveImportedSequence(certNo)`，按与 `nextCertNo` 同一 scopeKey 规则把序列推进到 ≥导入号序号 → 后续 `generate` 跳过已占号。IT 端到端：导入占号 00001 后 generate 得 00002（非撞号死锁）。
- ✅ **导入证书 status=ISSUED 但 issueDate 从不设**（`applyCertificate` 只设 issuer/validUntil），且无补设路径（issue 要 GENERATED、correct 无 issueDate 字段）。**已修复（Phase 43.2，已合并，见 §11）：** `applyCertificate` 对「已签发且签发日期为空」的导入证书补一个与 `validUntil` 规则自洽的签发日期（证书年度 + 有效期上/下半年，对其再套 validUntil 规则可复现导入有效期），既覆盖新导入亦补设既有缺失（补设路径）。
- ✅ **P2** `correct()` 接受任意 18 位 cert_no，不校验内嵌码与学生学段/层次一致。**已修复（Phase 48，待复核合并，见 §11）：** `correct()` 更正触及 `certNo`/`teachingSegment` 时经 `ensureCertNoMatchesSegmentAndLevel` 校验编号内嵌 学历码(idx9)/学段码(idx12) 与字段一致，规则同导入 `validateCertificateNo`、复用 `certCode`。

### 7.5 领域规则合规 —— 11/12 后端强校验（核心业务是对的）
逐条 ENFORCED（有 file:line 证据）：身份证 4 类型+18 位+MOD11 校验（`IdCardValidator`，checksum 默认关=决策#16）、证书号 18 位组成（`certCode/nextCertNo`，码取自 dict ext_json 与附录 A 逐字对齐）、有效期 6·30/12·31+3 年、**学段↔学科级联**（`TrainingLinkValidator.requireSubject` save 时无条件校验）、**培养目标↔实习地点级联**（含 V23 中职→其他修正）、免考多科独立+每科佐证+二级+应考口径、学年过滤一致（各表 assessment_year NOT NULL）、**RBAC 矩阵与 V20-23 授权完全一致**（逐角色核对）、测试仅导入+确认、视频退回可改（`reuploadable` 状态集正确）、字典枚举 12 类逐字完整。
- ✅ **唯一真 bug — Rule 11 过程性材料合格判定（P1，近 P0）**：`ProcessMaterialServiceImpl.categoryStatus:363-372` 用**全历史行按当前状态**计数，`passed = passedCount>0 && failedCount==0`；而 FAILED 是终态（`editable()` 排除 FAILED+locked=1，delete/replace 都被 `ensureEditable:445` 挡，控制器**无管理员强删/override**）→ 一条 FAILED 永久钉住 `failedCount≥1` → 该类别再也无法 passed，即便重传新材料通过 → **证书合格永久卡死且无恢复路径**（证书 `CertificateServiceImpl.doPrecheck:344` 以 `processStatus.isQualified()` 为签发前置）。若评审误用 FAIL（应 退回 REJECT）则学生永久不可纠正地不合格。**已修复（Phase 43.3，已合并，见 §11）：** 合格判定改为**只看该类别「最新/有效」的一份材料**——分组槽位 = 同一 `(student_id, assessment_year, category)`（`selectList` 经 `@TableLogic` 已排除软删行），「最新」取该槽位内 `max(id)`（雪花 `ASSIGN_ID` 随创建时间单调递增、不可变、不受 `replace` 改动，比 `uploadTime` 更稳健地表达「最后提交的那份材料」），该有效材料状态为 `PASSED` 才判该类别合格。效果：更晚提交并 PASSED 的材料**取代**更早的 FAILED（恢复合格）；而「最新一份为 FAILED」或「该类别无任何 PASSED 材料」仍判不合格（真失败不放行）。`totalCount/passedCount/failedCount` 保留全历史口径仅作展示、不参与判定。真栈 IT 复现→阻断 + 活体演示恢复路径（见 §11）。

### 7.6 前端 P1/P2
- **P1 批量下载永远失败**：`api/material.ts:103` 泛型写成 `request.post<Blob>`（只设第一个泛型），拦截器已返回 `response.data`=Blob 本身，消费端 `MaterialManageView:270` 又取 `res.data`=undefined → `createObjectURL(undefined)` 必抛 → 所有有 `material:batchDownload` 的角色点批量下载**必报失败**。修：`request.post<unknown, Blob>` + 直接用 res（对齐 exchange.ts/stats.ts）。
- **P2 Blob 端点错误显示英文 axios 文案**（`request.ts:36-60` 对 blob 响应不解析 `{code,msg}`）→ 模板下载/导出失败显示 "Request failed with status code 500"。
- **P2 未捕获的 `loadOptions()` 白屏**：`CertificateManageView:140`/`TestResultManageView:127` 无 try/catch 且 onMounted 先 await 它再 loadRecords → 字典失败则列表不加载、空页无提示。
- **P2 免考申请非原子**：`ExemptionDrawer:104-116` 先建条目再 Promise.all 传佐证；某佐证失败则条目已存但报"保存失败"，留下缺附件的孤儿免考记录。
- **P2 合格判定取任意学生**：`MaterialManageView:241` 评审无选中学生时静默用列表第一行的学生，弹窗显示不知是谁的结果。
- **P2 死代码/重复**：`v-perm` 指令注册但零使用；`batchCreateStudents` 无调用方；saveBlob/saveStatsBlob/内联 三份重复；死权限 `material:view`/`student:export`/`test:edit`；404 路由无 title。

### 7.7 校验 fail-open P2
- `StudentStatus/TrainingStatus/MaterialStatus.of` 未知值 `orElse(DRAFT)` → 损坏状态静默变最可编辑草稿（fail-open）；`ExemptionStatus.of` 却 `orElseThrow`（不一致）。**✅ 已修复已合并（Phase 43.4，见 §11）：** 三者改 `orElseThrow`（`BizException`），与 `ExemptionStatus.of` 一致；改前对 `student`/`training_profile`/`process_material` 三表（含软删行）做 `SELECT DISTINCT status` 安全预检，确认无脏值方切换。
- `BirthDateValidator` 只校验月 1-12 日 1-31 → 接受 2023-02-30。**✅ 已修复已合并（Phase 43.4，见 §11）：** 改用 `LocalDate.of(year,month,day)` 日历规则校验（含闰年/大小月），2023-02-30 等日历上不存在的日期现被拒绝。
- training/exemption 双提交唯一约束冲突未捕获 → 裸 500（非 BizException）。（不在 Phase 43.4 范围，另行分发）

### 7.9 正面确认（审计明确判定"无问题/已正确"，避免误改）
- **无缺失索引**：通读 V1–V23，material/exemption/video/certificate/training/user/student 及参考表热点列索引齐全（仅 certificate 有一处近重复复合索引可删）。
- **证书序列号并发安全**：`nextSequence` 用 `selectByScopeKeyForUpdate`(SELECT FOR UPDATE)+唯一约束+DuplicateKey 捕获，非 MAX+1 竞态。
- **无 SQL 注入**：全 LambdaQueryWrapper，无字符串拼接/`${}`；DataScope 建 jsqlparser Expression 对象。
- **V-01..V-13 校验全部接线**（无 stub return true）；导入 rollback 覆盖与导入写入一致（无残留）。
- **前端**：25 个 Drawer 提交前均 validate()/手工必填校验，按钮均 :loading（无重复提交）；权限串与 V8/V20/V21 授权无幽灵项；深链接/刷新/路由守卫正确；无 console/TODO/mock 残留。

### 7.10 授权 · 会话 · RBAC 安全（好消息：核心 IDOR 面干净）
**执行结论**：逐个核对 23 个控制器——**没有发现新的、普通学院用户可轻易利用的业务 IDOR**：每个 by-id/detail/写路径都被 `@DataScope`（拦截器）或服务层 `ensureCanRead/ensureCanWrite*` 之一守住（逐模块验证）。写侧学院范围**确系服务端从实体取**（非信任请求）。@pms 无通配绕过、JWT 算法安全、登录锁定正确、mustChangePwd 门有效、无 SQL 注入。真正的新风险在**会话/令牌生命周期**与**管理端 RBAC 写授权**：
- **P0（补，第 2 个 IDOR）导入批次越权**：`ExchangeServiceImpl.requireBatch:1297` 只查存在；`confirmImport:230`/`rollback:275`/`errorReport:214`/`batches() 列表:313` **均无 operator/college 过滤** → COLLEGE_CLERK 可枚举/确认/回滚/下载**任意学院**批次的错误报告（含文件名、操作人、学生 PII）。`StatsServiceImpl.batchVisible:549` 证明此处本应 scope，Exchange 漏了。`import_export_batch` 用 operatorId+scopeJson（无 college_id），需手动强制。
- **P1 登出是空操作、令牌不可撤销**：`AuthController.logout:48` 什么都不做；无 Redis/JWT 黑名单 → 泄露的 access token 存活满 1h、refresh 满 7 天。
- **P1 改密/重置密码不失效既有令牌**：`AuthService.changePassword:83`、`SecurityAdminServiceImpl.resetPassword:127` 只改 hash；令牌无 passwordChangedAt/版本 → 被盗后改密，旧令牌仍有效到自然过期。
- **P1 refresh 令牌可重放不轮换**（`AuthService.refresh:74`，7 天无重用检测）。
- **P1 RBAC 管理写无 scope、无权限天花板（自提权）**：`SystemSecurityController:51-152` 全部管理写只有 @PreAuthorize、无 @DataScope；`assignRolePermissions:216`/`assignUserRoles:260` 接受任意 permissionId（含 SYSTEM/SCHOOL 范围）/roleId → 持 `system:role:manage` 者可给自己/任何人授满 SYS_ADMIN 等价权（无"不可编辑更高权用户"守卫）。种子里仅 SYS_ADMIN@SYSTEM 持这些权，故**当前潜伏**，但是设计级授权缺口；若哪天给 COLLEGE 范围自定义角色发了这权，即可跨学院增删改用户/重置密码。
- **P2 验证码形同虚设**：`CaptchaService.svg:55` 把 4 字符明文当可选中 `<text>` 渲染，脚本直接从 SVG 读出（无需 OCR）——`/auth/login` 无真正防自动化（一次性+TTL 是对的）。
- **P2** 管理员建的 STAFF 账号固定初始密码 `ChangeMe123!`（`SecurityAdminServiceImpl:61`，env 未设时的硬编码兜底）；RBAC 关系表 PK 用 `id*1000`（snowflake ~19 位）**long 溢出**→ 可能 PK 碰撞覆盖他人授权行（`:155,220,268`）；`plainIdCard` 忽略自己的 `plain` 参数无条件返回明文（`StudentController:129`）；多个 detail-by-id 仅靠服务层手写校验（模式脆弱，将来漏写即 IDOR）。
- **P2（数据范围）** `StatsServiceImpl.batchVisible:556` 用 `scopeJson.contains(collegeId)` 子串匹配 → collegeId `1` 匹配 `10/11/100`（跨学院泄露）；SELF 范围统计恒空（学生看不到本人统计，`:440`）；`@DataScope` 表未在 TABLE_RULES 则**静默不过滤**（opt-in 非默认拒绝，未来给带属主新表加 @DataScope 漏配则泄露）。

### 7.11 部署 · 运维 · 依赖 · TLS
- **P0 Spring Boot 3.2.11 已 EOL + 命中方法级鉴权 CVE 影响面**（`pom.xml:10`）：3.2.x 2024-11 停 OSS 支持、2025-12 全面 EOL；传递 Spring Security 6.2.x，2025 年鉴权绕过族（CVE-2025-41249/41248/41232）正打 `@EnableMethodSecurity`+`@PreAuthorize`——**本平台 RBAC 的根基**。配置无法缓解，**上线前须升到受支持的 3.3+/3.4+**。**✅ 已修复（Phase 40，已合并，见 §11）：** 父 POM 升 **Spring Boot 3.2.11 → 3.4.13**（传递 **Spring Security 6.2.x → 6.4.x**，含上述 CVE 族修复）；同步 **MyBatis-Plus 3.5.7 → 3.5.16**（+ 显式 `mybatis-plus-jsqlparser-4.9`）、**knife4j 保持 4.5.0** 但用 `springdoc-openapi-bom 2.8.17` 覆盖其传递的 2.3.0（否则 SB3.4 的 Spring Framework 6.2 下 doc.html `NoSuchMethodError`）。`mvn verify` **93/93 绿**、Flyway 9.22→10.20 平滑（25 迁移校验通过、库仍 V25），前端零改动 type-check/build 绿；**RBAC 活体 3 条通过**（学生 `GET /system/user`→403、学院A clerk 读学院B学生→未找到、本域→200），证方法级安全 + 数据范围未因升级被绕过。
- **P0 全链路无 TLS**：`nginx.conf:1` 只听 `:80`，仓库无 443/证书/HSTS；`docker-compose.yml:107` 前端 :80 直发主机。登录密码、`Authorization: Bearer` JWT、身份证 PII **明文传输**——面向数千学生 PII 的公网服务不可接受。
- **P1 无 `.dockerignore` → 真实 `.env` 被打进镜像层**（后端 `Dockerfile:17` `COPY . .`，README:49 让运维在构建上下文放含生产密码的 `.env`）→ 密钥随镜像/推送泄露；还带入 `.git/target/*.log`。**✅ 已修复（Phase 41，已合并，见 §11）：** 根新增 `.dockerignore`，排除 `.env`/`.git`/`**/target`/`**/*.log`/`node_modules`/`frontend/dist`。
- **P1 生产把 DB/Redis/MinIO 端口发布到主机 + Redis 无密码**（`docker-compose.yml:14/29/47`，`:28` redis 无 requirepass）→ 主机网可达=无认证 Redis（存登录锁/验证码/会话）。后端只需内部 DNS，无需 publish。**部分修复（Phase 41，已合并，见 §11）：** DB(3306)、Redis(6379，加 `--requirepass ${REDIS_PASSWORD}`) 已不再 publish 到主机。**MinIO(9000/9001) 本次未动**——`FileServiceImpl.presignedGet` 返回的预签名 URL 本就直接嵌入 `minio` 这一容器内部 DNS 名，浏览器端无论端口是否 publish 都无法解析，对外可达性是独立的架构问题（需反代或公网 endpoint 设计），非本次任务范围，留后续 phase 一并处理。
- **P1 nginx `/api` 无 proxy 超时**（默认 60s）→ 万行导入/大视频上传 502/504，与 `client_max_body_size 2048m` 自相矛盾（`nginx.conf:8-17`）。
- **P1 nginx 无安全响应头**（无 X-Frame-Options/CSP/X-Content-Type-Options/HSTS）。
- **P1 健康端点是"静态谎言"**：`HealthController:23` 无条件返回 UP，却被用作容器 healthcheck+依赖门（`compose:94,108`）→ MySQL/Redis/MinIO 挂了后端仍"健康"，无重启/就绪信号。无 actuator/可观测性（无 metrics/trace）。
- **P1 容器日志无上限无轮转**（compose 无 logging 块→ json-file 无 max-size）→ 磁盘涨满；无 prod 日志配置。
- **P1 生产库被种入已知口令测试账号**（README:61 + V8/V13 seed `test_*` 全 `ChangeMe123!`）；**容器以 root 运行**（两个 Dockerfile 无 USER）。
- **P2**：镜像 `:latest` 不定；dev/prod MySQL 漂移（8.0 vs 8.4，迁移只在 8.0 验证）；Flyway prod 未显式 `clean-disabled`/`baseline-on-migrate`（迁移本身无破坏性 DDL，已确认）；无优雅停机；JVM 无 `-XX:+ExitOnOutOfMemoryError`；nginx 无 gzip/缓存头；MyBatis-Plus 3.5.7 偏旧；dev compose 无 healthcheck/depends_on（"后端早于 mysql"竞态在 dev 仍可能，prod 已正确处理）。
- **发现的密钥（均 dev/占位，无生产真密钥入库；`.env` 已 gitignore）**：`docker-compose.dev.yml:8` root123、`:37` minioadmin123；`.env.example:22` 弱 JWT 占位；**`.github/workflows/ci.yml:43` 提交了一个真 64-hex JWT_SECRET 明文**；~14 个 IT + AGENTS.md/HANDOFF.md 里的 `0123...` JWT 示例；备份手册硬编码 dev 口令。
- **数据持久化**：命名卷齐全（`mysql/redis/minio-data`）——**无重启即丢数据的 bug（好）**；残留风险：单主机 local 驱动 + 假备份 + 无 @Scheduled → 主机/磁盘故障全丢无异地副本；`down -v` 永久清空业务数据/附件/视频；MinIO 版本化未在 compose 启用 → 覆盖/删除不可恢复。
- **正面**：prod compose `depends_on: service_healthy`（解决了"后端早于 mysql"）、多阶段构建、`JwtService` 对空/短密钥 fail-fast、无破坏性迁移。

### 7.12 二轮新增"上线阻断"P0 汇总（合入 §1 优先级）
1. **P0-7 导入批次越权 IDOR**（§7.10）——与 P0-1 文件 IDOR 同级，学院教务员可跨学院操作批次。
2. **P0-8 Spring Boot EOL + 方法鉴权 CVE**（§7.11）——升级 3.3+/3.4，动摇整个 RBAC。**✅ 已修复（Phase 40，已合并，见 §11）：升 SB 3.4.13 / Security 6.4.x，mvn verify 93/93、RBAC 活体 3 条通过。**
3. **P0-9 无 TLS**（§7.11）——PII/JWT/密码明文。
4. **P0-10 并发无乐观锁**（§7.1）——评审/证书/视频状态可被竞态覆盖（PASS 覆盖 REJECT），加 `@Version`+条件更新+缺失唯一约束。
5. **P0-11 MinIO I/O 在事务内**（§7.2）——大上传耗尽连接池致全站瘫。
> 执行序相应调整：**Phase 37 拆成 37a 安全急修（P0-1/7/9 授权+TLS+IDOR+会话）与 37b 依赖升级（P0-8 Spring Boot）**；并发无锁（P0-10）与 MinIO 出事务（P0-11）并入 Phase 38/39。前端"批量下载永远失败"（§7.6 P1）建议随手修（纯前端泛型笔误）。

---

## 8. 活体渗透/回归测试结果（2026-07-04，栈起，脚本实测，非静态推断）
以 demo 数据实测，测完已还原。**两个 P0 现场证实可利用**，多条正面项现场证实有效，并纠正 1 处审计过报。

### 8.1 🔴 P0-1 文件预签名 IDOR —— 现场证实可利用（最高优先修）
- 手法：`test_student`(学院A) 本人上传一份材料 → 得 `file_id`；**`test_student_b`(学院B 的学生)** 携自己 token 调 `GET /api/file/{file_id}/url` → **HTTP200 拿到预签名 URL，并实际下载到文件字节** `%PDF FAKE private material of student 9001 (college A)`。
- 结论：**任一登录用户可凭 file_id 下载任意他人（跨学院跨学生）材料/证件/视频**。根因 `FileController:50 presignedUrl` 仅 `@PreAuthorize("isAuthenticated()")`、无 @DataScope、无属主校验。
- 修复验收：修后同一手法必须 **403**。
- **纠正审计**：过长有效期 `expiry=10年` 实测**被拒**（后端有上限）——§7.10 里"expiry 无上限"一项**不成立**，删除该子项，只保留"无属主校验"。

### 8.2 🔴 P0-10 并发无乐观锁 —— 现场证实竞态
- 手法：6 线程用 barrier 同时对同一 `FIRST_REVIEW` 学生发 `POST /student/{id}/first-review` PASS → **6 次全部 HTTP200 成功**，`audit_log` 产生 **6 条**同一逻辑审核记录，`first_review_comment` 最终为 `race-2`（last-writer-wins，非确定）。
- 结论：状态流转无 `@Version`/无条件更新，重复/并发提交都过守卫都提交 → 若一 PASS 一 REJECT 并发，最终态不确定；审计与实际状态可背离。修复验收：同样 6 并发应仅 1 次成功、审计 1 条。**✅ 已修复验证（Phase 37b，见 §11）：** 6 并发仅 1× 成功、5× code=1000「操作冲突」、审计恰 +1、终态确定 SECOND_REVIEW。

### 8.3 🟢 正面项现场证实（避免误修）
- **数据范围 SELF/COLLEGE 生效**：学生 `GET /student` 列表只返回本人 1 条(id 9001)；学生读他人学生详情(990..005)→404；学院A教务员读学院B学生(990..009)→404。
- **越权访问被拒**：学生 `GET /system/user` → 403。
- 即 §7.10 的"核心业务 by-id/list 面干净"经活体复核成立；风险确实集中在 **文件预签名**（非 @DataScope 表）与 **导入批次**（§7.10 P0，未活体因缺 B 学院导出账号，代码已确认）两处非常规入口，以及会话/并发。

### 8.4 🔴 P1 会话不可撤销 —— 现场证实
- 手法：登录得 token → `POST /auth/logout`(HTTP200) → 用**同一 token** 调 `GET /auth/me` → **仍 HTTP200 code=0**。证实 `AuthController.logout` 是空操作、无黑名单，登出/改密后旧 access/refresh token 存活到自然过期（access 1h、refresh 7d）。修复验收：登出后旧 token 必须 401。

### 8.5 待补活体项（Phase 37 修复时成对"复现→阻断"）
- 导入批次 IDOR（需构造 xlsx + 跨学院导出账号，代码已确认）；前端批量下载泛型 bug（需浏览器）；refresh 令牌重放。

---

## 9. 三轮深扫（数据模型/迁移 · 系统域模块 · 测试覆盖）

### 9.1 数据模型 / 迁移 / 种子完整性
- **全库无任何 DB 外键约束**（23 迁移 grep `FOREIGN KEY/REFERENCES/ON DELETE` = 0）：父子关系全靠应用码维护，级联删除 100% 手动且各服务不一致。软删统一 `deleted TINYINT`(@TableLogic)，生产码无硬删（正面）。**根因性**：无 DB 安全网，下列孤儿问题无兜底。**✅ 决策已定（Phase 43.1，已合并，用户拍板）：不加 DB 外键。** 理由：全库全程软删、生产码无硬删 → 软删父行仍物理存在（FK 约束仍满足）→ FK 级联删除几乎不触发；对既有存量回填「无违约 + 迁移」再加 FK 风险高收益低。孤儿防治维持在**应用层**（删学院用户守卫 + 删学生停登录，Phase 39 已交付；`deleteMajor→停用`，Phase 43.1 已交付；孤儿巡检定时任务 P1-9 后续）。FK 部分仅文档化决策与理由、**零 schema/代码改动**（无新迁移）。
- **P0 删除父实体产生静默孤儿**：`OrganizationServiceImpl.deleteCollege` 只查 major 数、`deleteMajor` **零守卫**；而 `MajorCodeValidator` 让 `education_master` 学生**跳过 sys_major 存在校验** → 学院可有真实学生却无 major 行 → 通过 deleteCollege 唯一守卫，student/training/material/exemption/video/cert/sys_user 仍指向已删学院，无报错永久悬挂。**✅ 部分修复（Phase 39）：** `deleteCollege` 增 `sys_user.college_id` 在用校验（有账号=学生/教职工则拒删，补住"有学生无 major"缺口）。**✅ 收尾（Phase 43.1，已合并）：** `deleteMajor` 由「软删专业行 + 硬删其 `major_training_goal` 联动」改为「**停用**」（`status=DISABLED(0)`、行不软删、联动保留、幂等）——引用方 student/training 用 code/name 快照（非 major_id）、system 无法跨模块数使用量，遂把 major 移除定义为停用：行持久化、快照可解析、杜绝孤儿、可恢复；DB 外键按上条决策**不加**。活体见 §11。
- **P0 删除学生不停用其登录账号**：`StudentServiceImpl.delete` 只 `deleteById(student)`，不级联、**不停用关联 `sys_user`**（sys_user.student_id）→ 已删/退学学生仍有可用登录（访问撤销失败）。**✅ 已修复（Phase 39）：** `delete` 后置停用关联 `sys_user`(student_id) `status=DISABLED`；JWT filter 每请求校验 `status=ENABLED`（`:62`）→ 旧 token 下次请求即 401。**活体（复现→阻断）：** 删学生前其旧 token `/auth/me`=200，管理员删除后同一 token=**401「用户不存在或已停用」**、账号 DISABLED。
- **P1 sys_user/sys_role 唯一键漏 `deleted`**（`V7:28-30,49`，与 V9-V22 惯例不一致）+ create 未捕获 DuplicateKey → **删了再建同名用户/工号/角色码 直接崩**（裸 DuplicateKeyException，对比 `CertificateServiceImpl:129` 有捕获）。
- **P1 无 Flyway prod 配置**：`baseline-on-migrate` 只在 application-dev（prod 走默认 false）→ 首次部署若 schema 非全空 Flyway 硬失败无恢复文档。
- **P1 种子污染生产参考数据**：`V12:74` 免考依据/科目 seed 了 7+2 占位值（违背 `待确认事项确认单` 决策#12"初始置空"）；`V8` 把 `PHASE2_COLLEGE_A/B`(id 201/202) + V10 假专业 seed 进 `sys_college/sys_major` → 真实下拉里出现假学院/专业。
- **P1 MinIO 分片/源文件从不真正删除**：`archiveUploadSessions:670` 只软删 DB 行，`merge:211` 合并后不删源分片 → **每个分片永久留存 + 合并视频双份**（无界存储增长）。
- **P1 `student.college_id` 写时不校验存在**（`allowedCollegeId` 只解析范围权限）→ 可存到不存在/已删学院。
- **P1 VARCHAR 日期无补零**：`CertificateServiceImpl.normalizeDate/validUntil` 产出 `2026/7/3` → 将来对 issue_date/valid_until 做 `ORDER BY`/范围比较字典序错（`2026/10/1` < `2026/2/1`）。当前无查询触发，是种子格式里的雷。
- **VARCHAR 存日期/枚举清单**：birth_date、全表 assessment_year、cert issue_date/valid_until、ability_test.score 均 VARCHAR；status 类枚举全是无 CHECK 的自由 VARCHAR（脏值只在下次读时抛异常）。
- **P2**：role/user 删除不级联 junction 表（经查**非提权洞**——权限解析都 re-join `r.deleted=0 AND status=1`，但留孤儿行扰乱审计）；归档视频任务把 `reviewer_id` 覆盖成自身 PK（毁"谁评审过"痕迹）；cert_sequence 若将来被软删可致重号（当前不可达）。**正面**：证书序列号 `SELECT FOR UPDATE` 并发安全；JWT 密钥 fail-fast 稳。

### 9.2 系统域/参考数据模块
**跨切 P0（新，数据/授权正确性）**：
- **P0-14 RBAC 授权 upsert 静默损坏**：`SysUserRoleMapper/SysRolePermissionMapper/SysUserDataScopeMapper` 用"先全禁 → 逐项 `INSERT...ON DUPLICATE KEY UPDATE`，id=parentId*1000+序号"，**冲突时 SET 从不更新区分列(role_id/permission_id/college_id)**，且唯一键无 `deleted`(`V7:63,99,116`)。实迹：把角色 [A,B,C] 改存 [A,C] → 用户最终得 **{A,B} 而非 {A,C}**（B 被复活、C 被丢弃），无报错无回滚，且每请求重查即时生效。`SecurityAdminServiceImpl.replaceUserRoles/assignRolePermissions/assignUserDataScope` 全用此模式。**日常改权就触发**。
- **P0-15 角色授权弹窗改一个权限，静默改写所有权限的数据范围**：`RolePermissionDrawer.vue:29-78` 丢弃后端真实 per-permission `scopeType`，保存时用硬编码 3 桶猜测 `defaultScopeFor` 重建全量 payload，后端 `assignRolePermissions` 只校验枚举照单全收 → 种子里 ASSIGNED 等真实范围被改一次全被冲掉。每次保存必触发。
- **P0-16 重新指派视频评审从不撤销旧评审**：`VideoReviewServiceImpl.assign:296/resolveAssignReviewerIds:1123` 只**增**任务不禁旧的；被移除评审仍可读/评分，其残留分数可被 `settleIfReady` 计入结算；无单独取消指派 API；`AssignReviewerModal.vue:45` 还把旧评审一直显示为"已指派"。

**模块 P1/P2**：
- **P1 终审通过从不通知学生**：4 个非视频复审流程的二审分支**只在非 PASS 才发通知**（`Exemption:303`/`Material:218`/`Student:216`/`Training:193` 都是 `if(!PASS) notify...`）→ 学生材料**最终通过时静默无通知**，只在被退回时收到消息；视频结算路径同样无通过通知。
- **P1 多个 `@DataScope` 是误导性空操作/恒拒**：`training_goal_config`/`sys_dict_type`/`sys_dict_item`/`sys_region` 别名**不在 TABLE_RULES** → 不加任何过滤（失败开放，好在这些是全局参考数据）；而 `major_training_goal` 对 COLLEGE/SELF 调用者**恒拒返回空**（`GET /major/{id}/training-goals` 学生/学院职员永远看不到，仅 SCHOOL 走 allSchool 才有数据）。
- **P1 字典编码唯一性软删盲 → 复建报 500**：`DictServiceImpl.existsTypeCode/existsItem` 用 `selectCount`(过滤 deleted=0)，但唯一键不含 deleted → 删了再建同码 → 裸 DuplicateKey → 无 `DataIntegrityViolationException` 处理器 → "系统异常"。Organization 的同类检查用了 `*IncludingDeleted` 正确，Dict/RBAC 没跟上（**修复不一致**）。**✅ Dict 已修复已合并（Phase 43.4，见 §11）：** 对齐 Organization 的 `*IncludingDeleted` 模式（新增 `SysDictTypeMapper.countByTypeCodeIncludingDeleted`/`SysDictItemMapper.countByItemIncludingDeleted`），并在 `GlobalExceptionHandler` 加 `DataIntegrityViolationException` 兜底处理器（友好业务错误替代裸 500）。**RBAC 同类检查未排查、不在本次范围，仍待跟进。**
- **P1 禁用字典"类型"对其"项"零级联**：`queryItems/listItems` 只看 item.status 不查父 type.status → 禁用类型纯装饰，项仍可服务可缓存可新增。
- **P1 组织删除守卫不全**：`deleteMajor` 零使用校验（且 training_profile 用 code/name 快照非 major_id，无法常规校验）**✅ 已解（Phase 43.1，已合并）：改为停用（不软删、无孤儿），旁路跨模块使用量校验难题**；`deleteCollege` 只查 major 数不查人（✅ Phase 39 增用户守卫）；编辑专业培养目标联动时 `getMajorTrainingGoals` 过滤掉"已禁用但仍生效"的联动 → 全量替换保存时**静默软删管理员没看见的联动**。
- **P1 param 解析静默兜底**：`ParamServiceImpl.getInt/Boolean` 解析失败静默返回默认值**无日志** → 管理员把阈值(如视频过线分)打错成非数字，系统静默按默认跑，毫无提示。
- **P1 审计查询硬编码 `LIMIT 500` 无分页无 offset 无截断提示**（`AuditQueryMapper:144`）→ 大量审计静默截尾（合规/取证缺口；SQL 本身参数化不可注入）。
- **P1 work_no/student_id 复用烧号**：`sys_user` 唯一键无 deleted，username 有友好校验但 work_no/student_id 没 → 重发离职工号撞裸约束 → 500。
- **P2**：`UserDrawer.vue:87` 改用户重复写两次角色（updateUser 内已 replaceUserRoles，又单独 assignUserRoles）；deleteUser/deleteRole 无级联清 junction、deleteRole 无在用守卫；permissionTree 无状态过滤；role code 更新可改无不可变守卫。
- **验证正面**：Region 三级级联防环+链一致；Subject 类别节点排除+recent 自愈；`NotificationServiceImpl.markRead` 原子 `WHERE id AND user_id` 防越权；`DataScopeAspect` finally 清 ThreadLocal 无泄漏；reviewer_group DataScope 正确接线+指派时活体校验学院；证书/College/Major 编码用软删包含检查正确；审计删除被拒并留痕。



### 9.3 测试覆盖缺口（84 个 IT，全是 boot 层集成测试）
- **安全网范围**：`platform-boot` 下 14 个 IT 共 84 个 @Test；**business/exchange/statistics 无任何单测；前端零测试（无 *.spec、package.json 无 test 脚本）**。全靠 DB 支撑的集成测试兜底。
- **P0 三个已知 bug 全部 UNTESTED，会静默上线**：①材料 FAILED 永久污染——Phase5 唯一涉 FAILED 的用例用"换学年"绕过恢复场景，**同年同类重传复活从未被测**；②`confirmImport` 双确认——**全套无任何二次 confirm 调用**（连顺序都没有，更别说并发）；③视频双结算——Phase7 11 个用例全顺序、无 ExecutorService 并发。
- **P0 全库仅 1 个并发测试**（Phase9 证书序列号 50 线程，也是唯一乐观/悲观锁验证）；reissue/评审/结算等 check-then-act 全无并发覆盖。
- **⚠️ reissue 死枚举被测试"固化"**：Phase9 `voidAndReissue...` **断言了当前错误行为**（原证保持 VOIDED）→ 修复该 bug 会让此测试假失败，改时需同步改测试（否则误判回归）。
- **P1 RBAC 负向测试失衡**：14 文件里只有 COLLEGE_CLERK(3 端点)、REVIEW_TEACHER(1 端点)有活体 403；SYS_ADMIN/ACADEMIC_ADMIN/COLLEGE_AUDITOR/STUDENT **零活体拒绝测试**（只有静态权限串断言，不证服务端强制）→ 谁的 `@PreAuthorize` 丢了都不会被测出。
- **P1 跨学院夹具单向**：所有 IDOR 测试只测"A 学院职员→B 数据"，无 B 学院职员账号 → 反向/职员对职员对称性无法测。
- **P1 业务 IDOR 其实测得不错**（Phase3-11 各模块跨学院 DB 验证）——与本次活体"业务 by-id 安全"一致；漏的是**文件预签名**（非业务端点）与导入批次。
- **P1 覆盖漏项**：证书下半年有效期(7-12 月)分支未测（用例名自己标了）；导入导出真往返(导出再导入)未测、`UPDATE_EMPTY` 策略零覆盖、V-01..13 只松散聚合断言非逐行；exchange 跨学院是 soft-fail(code=0/failCount=1) 而非 403（与全站惯例不一致）；未指派但同学院评审教师碰他人任务的 IDOR 轴未测。
- **P2**：**全库零 404 测试、零分页/大数据测试**（所有数据集 1-5 条）；系统参数只测了 1 个的即时生效；审计只在部分流程断言、时间字段从不断言。
- **测试质量红旗**：Phase24:314 `auditLogMapper.selectCount(空条件)>0`（表里有任意行就过）；Phase3:119 拿 nanoTime 随机 id 比硬编码常量（恒不等，形同虚设）；多处仅断 HTTP200 不查字段/DB；14 文件共用硬编码种子 id + `@Order`/PER_CLASS 实例字段跨用例传递 → **不可并行/分片，一个早失败级联假失败**；Phase5/7 的 file_object 清理按 biz_type 全删（越界）。

### 9.4 三轮 P0 汇总（并入优先级）
- **P0-12 无 DB 外键 + 删父静默孤儿 + 删学生不停登录**（§9.1）——数据完整性靠应用码且不一致，上线前需补关键级联/停用逻辑或 DB 约束。**✅ 关键两项已修（Phase 39）：** 删学生停用登录（活体 200→401）、deleteCollege 增用户在用校验；**✅ 收尾（Phase 43.1，已合并）：** deleteMajor 改停用（不软删/无孤儿，活体验证）、DB 外键决策＝不加（软删设计下 FK 近无用且回填风险高，孤儿防治留在应用层 + P1-9 巡检）；**仍待：** MinIO 孤儿清理（P1-9）。
- **P0-13 关键 bug 零测试**（§9.3）——修 P0 时必须**同步补并发/复活/双确认测试**，否则改完无从证明、且 reissue 测试会假失败。
> 迁移建议随手做的低风险项：sys_user/sys_role 唯一键补 `deleted`（V24 新迁移）、create 捕获 DuplicateKey、免考种子占位值清空、Flyway prod 配置。

---

## 10. 执行摘要与结论（给决策）

### 10.1 整体判断
**核心业务逻辑是可靠的**（好消息，别推倒重来）：领域规则 12 条 11 条后端强校验、业务 by-id/数据范围 IDOR 面经活体+单测双证干净、证书序列号并发安全、无 SQL 注入、字段/证书/级联规则正确。**问题集中在"从 demo 到生产"的产品化层**——安全加固、并发正确性、RBAC 管理端一致性、运维基建、测试网——这正是过渡期该补的，现已全部摸清并带 file:line + 修复验收口径。

### 10.2 上线阻断 P0 全清单（16 项，按修复批次）
**批次 A · 安全急修（Phase 37a，多为独立改动，建议 Claude 亲自 + 复现/阻断活体证据）**
1. 文件预签名 IDOR（§8 已现场利用）— 加属主/范围校验。
2. 导入批次 IDOR（§7.10）— batches/confirm/rollback/errorReport 加 operator+学院过滤。
3. RBAC 授权 upsert 静默损坏（§9.2 P0-14）— 改权把用户改坏，日常触发。
4. 角色授权弹窗改一权限清掉全部数据范围（§9.2 P0-15）。
5. 会话不可撤销（§8 已现场证）— 登出/改密令牌黑名单或 token 版本。
6. 无 TLS（§7.11）— 前置 443 终端 + HSTS。
7. Swagger 生产公开 + 无 prod profile（§1 P0-4）。

**批次 B · 依赖与并发（Phase 37b/38）**
8. Spring Boot 3.2.11 EOL + 方法鉴权 CVE（§7.11）— 升 3.3+/3.4。**✅ 已修复已合并（Phase 40，见 §11）：升 SB 3.4.13（Security 6.4.x，含 CVE 修复）+ MP 3.5.16 + knife4j 4.5.0/springdoc 2.8.17；mvn verify 93/93 绿、RBAC 活体 3 条通过。**
9. 并发无乐观锁（§8 已现场证 6/6）— BaseEntity 加 `@Version` + 条件更新 + 补唯一约束(证书 student+year、id_card_no)。
10. 重新指派评审不撤销旧评审（§9.2 P0-16）。
11. MinIO I/O 在事务内（§7.2）— 移出事务防连接池耗尽。

**批次 C · 数据/运维/容量（Phase 38/40）**
12. 无 DB 外键 + 删父孤儿 + 删学生不停登录（§9.1）。**关键项已闭环（Phase 39 删学生停登录/删学院守卫；Phase 43.1 deleteMajor→停用 + 不加外键决策，均已合并，见 §11）；剩 MinIO 孤儿清理（P1-9）。**
13. 批量下载整包进堆 OOM（§1 P0-2）— 流式。
14. app 用 mysql root（§1 P0-5）— 专用最小权限账号。**配置就绪已合并（Phase 41，见 §11）。**
15. ~~假备份 + 全库无 @Scheduled（§1 P0-6）~~ — **已实现已合并（Phase 41.2，见 §11）：应用内 JDBC 逻辑备份→gzip→MinIO 真实产物 + 定时任务(prod 门禁)。**
16. 关键 bug 零测试（§9.3）— 修 P0 时**同步补并发/复活/双确认测试**（否则改完无从证明，且 reissue 测试会假失败）。

### 10.3 强烈建议
- **修 P0 必须配测试**：当前 3 个已知 P0 bug（材料 FAILED 污染 / confirmImport 双确认 / 视频双结算）零覆盖，改完无回归网 → 每修一项补对应测试。
- **顺手低风险快赢**（可先做，风险极低、收益明确）：前端批量下载泛型 bug（一行）、终审通过通知学生、param 解析失败记日志、审计查询去 500 硬顶、唯一键补 deleted、种子占位清空、`.dockerignore`、Redis 加密码/不暴露端口、nginx 安全头+代理超时。
- **本清单是活文档**：随修随勾；每个 P0 修复以"复现→阻断"活体证据验收（§8 已示范）。

> 三轮扫描 + 活体测试累计：~10 个专项审计代理（授权/业务/并发/性能/前端/部署/领域/模块/测试/数据模型）+ 4 项现场证实（文件 IDOR、并发竞态、登出空操作 = 可利用；数据范围/越权 = 已防护）。git 全程本地私有。

---

## 11. 修复进展（随修随勾，每项带活体验收证据）

> **合并状态（截至 2026-07-04）**：Phase 37a–39、40、41、41.2、42.1–42.4、43.1–43.4、44a–44f 的**全部特性分支均已 ff-merge 入 main**（`git remote -v` 空、本地私有仓库、从未 push）。main 为线性历史，`git log` 即证。下列个别条目正文/结尾仍保留的「未合并入 main / 未 checkout main / 未 merge / 待人工复核」等字样，是各相**撰写当时**的分支瞬时状态历史留痕，现均已合并——以本 banner 为准。

### Phase 45（本地/dev 启动崩溃修复 = 用户报告「点开是 500」的根因，Phase 45 —— 分支 `feature/phase45-dev-startup-jwt`，单 commit，已 ff-merge 入 main，mvn verify 114/114 绿 + 活体不带 JWT_SECRET 起栈成功）
- ✅ **根因**：base `application.yml` 为 `secret: ${JWT_SECRET:}`（空默认、全 profile；prod 正确取舍——密钥不硬编码进包）但 dev 无覆盖 → 未注入 `JWT_SECRET` 时 `JwtService.init` 抛 `BizException("JWT密钥未配置")` → 后端启动失败、未起在 :8080 → 前端 Vite dev 把 `/api/*` 代理到空端口 → 浏览器 500。**与本会话已合并的分页/44f 无关**（活体逐一验证 7 端点均 200 code=0，含 `/api/audit/log` 4.2 万行真分页、`/api/system/user` total=17）。
- ✅ **修法**：① `application-dev.yml` 加 `platform.security.jwt.secret: ${JWT_SECRET:<dev 默认>}`（dev 专用、明确标注勿用于生产），dev「直接起栈」开箱即用。② `JwtService` 的 base64 回退 `catch` 补 `io.jsonwebtoken.io.DecodingException`（jjwt 对非 base64 串抛此异常、非 `IllegalArgumentException` 子类，原未捕获 → 任何可读口令型 `JWT_SECRET` 都崩溃启动）——现 `JWT_SECRET` 接受任意 ≥32 字节串。
- ✅ **prod 安全不削弱**：prod active=prod 不加载 dev 文件 + base 空默认 → 仍强制注入 `JWT_SECRET`。⚠️ **运维提示**：默认 active=dev（既有设计），生产部署务必设 `SPRING_PROFILES_ACTIVE=prod`，否则会用 dev 非密默认（DB root/JWT/MinIO）。
- ✅ **验证**：`mvn verify` 114/114 绿（ITs 经 `@SpringBootTest properties` 覆盖密钥、不受影响）；活体重建 jar **不带** `JWT_SECRET` 起栈 → health=UP + captcha/login/鉴权调用 200。已 ff-merge 入 main。

### Phase 46（P1-10 异常状态码契约 + P1-7 CORS 部署配置，Phase 46 —— 分支 `feature/phase46-errstatus-cors`，单 commit，已 ff-merge 入 main，mvn verify 114/114 绿 + 前端 build 绿）
- ✅ **P1-10 异常状态码契约（契约敏感、前后端同步）**：`GlobalExceptionHandler` 兜底 `handle(Exception)` 加 `@ResponseStatus(500)`（此前默认 200 → 未知故障对监控/告警隐形）；**同步补** `HttpRequestMethodNotSupportedException→405`、`NoHandlerFoundException→404`——否则错方法/错路径也落进 500 兜底、把客户端错误误报为服务端故障（正确语义：客户端错误 4xx、服务端故障 5xx、业务可恢复 200+码）。前端 `request.ts` 错误分支补「非 2xx 但响应体是统一 `Result`（code≠0）→ 提取 `data.msg`」，与成功分支一致、不夺 401-refresh 分支，保证状态码改变后友好提示不丢。`Phase8TestResultIT.manualCreateAndUpdateEndpointsAreOffline`（对仅 GET 的 `/api/test` 发 POST/PUT）断言从 `OK` 更正为 `METHOD_NOT_ALLOWED`（更强证明端点不可用，非弱化）。
- ✅ **P1-7 CORS 部署配置**：`CorsConfig` 早已 env 化读 `CORS_ALLOWED_ORIGINS`（无需改码）；补 `.env.example`（`CORS_ALLOWED_ORIGINS` + 用法注释）、`docker-compose.yml` backend 环境透传 `${CORS_ALLOWED_ORIGINS:-}`、`docs/phase-14-非功能部署验收.md`「关键环境变量」小节（`SPRING_PROFILES_ACTIVE=prod`/`JWT_SECRET`/`DB_PASSWORD`/`REDIS_PASSWORD`/CORS 必设项）。CORS 留空 fail-closed（仅本地）；同源部署 CORS 不参与。
- ✅ **验证**：`mvn -B -ntp clean verify` **114/114 绿**（Phase8 offline 断言更正为 405）；前端 `npm run type-check` 干净 + `npm run build` 成功。已 ff-merge 入 main。无迁移（库 max 仍 V25）。

### Phase 47（P1-9 定时清理，Phase 47 —— 分支 `feature/phase47-scheduled-cleanup`，单 commit，待主 Opus 复核合并，mvn verify 115/115 绿）
- ✅ **问题**：全库除备份外 0 处 `@Scheduled`——audit_log（追加写）与 notification 只增不清、MinIO 可积压未完成分片、file_object 可能孤儿。P1-9 要求加**保守、prod 门禁**的清理调度层。
- ✅ **调度门禁（复用 P0-6 约定）**：`CleanupScheduleConfig`（platform-boot）`@EnableScheduling + @ConditionalOnProperty(platform.cleanup.schedule.enabled=true)`，与 `BackupScheduleConfig` 完全同款——dev/测试/未配置不注册、`@Scheduled` 不触发（不扰动 115 IT），仅 `application-prod.yml` 置 true 生效；三作业 cron 可配、错峰备份之后（03:30/03:45/04:00）。放 boot 因三作业跨 system+file+boot 三模块。
- ✅ **① 保留期分批物理清理**：`RetentionCleanupService`（platform-system，无接口具体服务，仿 `DatabaseBackupService`）`pruneAuditLog()`/`pruneNotification()`——`ParamService` 读保留窗口（`cleanup.auditLog.retentionDays` 默认 180 / `cleanup.notification.retentionDays` 默认 90，可覆盖），`@Delete ... LIMIT` 原生 SQL 分批循环删（`cleanup.prune.batchSize` 默认 1000 / `cleanup.prune.maxBatches` 默认 500 上限）避免锁表；notification 有 `@TableLogic`，**刻意原生 SQL 绕软删做真物理删除**回收空间；非正保留期回退默认防误清空整表。
- ✅ **② MinIO 未完成分片 abort（诚实核验）**：minio 8.5.12 高层 `MinioClient` **无** `listIncompleteUploads`/`removeIncompleteUpload`（7.x 后移除，javap 核验 public 方法确无）——`FileMaintenanceService`（platform-file）改按任务书授权的第二方案：幂等设桶 `AbortIncompleteMultipartUpload` 生命周期规则（真实 API `LifecycleConfiguration`/`setBucketLifecycle`，`cleanup.minio.abortIncompleteDays` 默认 7，保留桶上其它既有规则），**实际 abort 由 MinIO 服务端按规则执行、非应用侧遍历、未伪造任何「已 abort N 个」结果**；MinIO 不可达吞异常仅告警、返回 false。
- ✅ **③ 孤儿 file_object 扫描（仅报告不删）**：`FileObjectScanMapper`（platform-boot，聚合全部迁移/表结构、天然知跨表引用）`countOrphans`+`sampleOrphanIds`，`NOT EXISTS` 覆盖全部 5 处业务引用列（`process_material.file_id`、`exemption_material.file_id`、`video_upload_session.file_id`、`video_review.video_file_id`、`import_export_batch.error_report_file_id`，对 V11–V17 迁移逐一核验）；保守：跳过逻辑删+grace 窗内新上传（`cleanup.orphan.graceHours` 默认 24，避免误报在途上传）、业务侧不过滤 deleted（宁少报不误伤）；**只 log 统计+样例 id、绝不自动删对象/行**，交运维核查。
- ✅ **验证**：`mvn -B -ntp clean verify` **115/115 绿**（原 114 + `Phase47CleanupIT`）。IT 直接调 `RetentionCleanupService`（不靠调度器）：插过期(now-500d)+近期(now-1d)标记行入 audit_log/notification，断言过期物理删除/近期保留/`file_object`+`sys_user` 行数不变，finally 按高位 id 自清理；日志实证 audit_log 保留 180 天删 1 行、notification 保留 90 天删 1 行（仅命中标记、无真实存量误删）。无迁移（库 max 仍 V25；audit_log 已有 `idx_audit_time(operate_time)`）。⚠️ 仅 `SPRING_PROFILES_ACTIVE=prod`+门禁开时启用。

### Phase 48（§7.4 剩余证书完整性两项：导入静默篡改终态证书［P1］+ correct() 校验 cert_no 内嵌码［P2］，Phase 48 —— 分支 `feature/phase48-cert-import-guard`，单 commit，待主 Opus 复核合并，mvn verify 117/117 绿；真栈复现→阻断 IT）
- ✅ **Item 1（P1）问题**：`ExchangeServiceImpl.importOne` 解析既有证书（`certificateByNo`/`certificateByStudentYear:1298` 无状态过滤）后仅校验学院权限、**无状态守卫**即 `applyCertificate`+`updateById` 覆盖 → 静默改写**已作废/已重开/已归档**终态证书（编号/签发人/有效期/快照/隐式回写状态），与 `CertificateServiceImpl.correct()` 的终态守卫（VOIDED/REISSUED/ARCHIVED→「当前状态不可更正」）**直接矛盾**。
- ✅ **Item 1 修法**：在 `ensureCanUpdateExisting` 之后、`applyCertificate` 之前**镜像 correct() 守卫**——命中既有证书且状态 `VOIDED/REISSUED/ARCHIVED` 抛 `BizException("证书"+label+"，不可通过导入修改")`；由 `confirmImport` 逐行循环捕获→回滚该行 `REQUIRES_NEW` 事务（整行原子回滚，被命中证书分毫不动）→计入逐行错误（与既有逐行上报语义一致）。**守卫集严格对齐 correct()**：放行 `EXPORTED`（correct 亦放行、内容更正非流转），刻意不扩大避免两路径再分叉。**全库唯一**未加状态守卫的证书更新路径（其余 issue/markExported/archive/void/reissue/correct 均已守卫）。
- ✅ **Item 2（P2 · 判为「已包含」并实现）**：`correct()` 允许改 `teachingSegment`/`certNo` 却不校验与 18 位标准号内嵌段码自洽——可把「编号」与「学段/层次」改成互相矛盾。新增 `ensureCertNoMatchesSegmentAndLevel(entity)`：**仅当本次更正触及 `certNo` 或 `teachingSegment`** 时校验 学历码（idx9）==`certCode(education_level…)`、学段码（idx12）==`certCode(teaching_segment…)`，不一致即拒；非 18 位历史/外部号跳过（同 `reserveImportedSequence` 取舍）。**规则复用真源**——与 `nextCertNo` 编排、导入 `validateCertificateNo:637` 段码校验同规则、复用同一 `certCode(...)` 助手，**不重排 cert_no**故属「已包含」。「仅触及时校验」保证只改 validUntil/学科名 等无关更正零误伤（`Phase9CertificateIT` 既有 correct 用例触发条件为假、恒绿）。
- ✅ **验证**：`mvn -B -ntp clean verify` **117/117 绿**（原 115 + `Phase48CertImportGuardIT` 2）。IT 端到端（真 Tomcat+MySQL）：①`importDoesNotSilentlyOverwriteVoidedCertificate`——导入建 ISSUED→OVERWRITE 再导入成功改签发人（非终态无误伤）→作废→换号命中 `certificateByStudentYear` 的 OVERWRITE 再导入 `successCount=0/failCount=1`、错误含「不可通过导入修改」、作废证书 状态/编号/签发人/有效期 逐字段未变、无凭空新号证书；②`correctRejectsSegmentInconsistentWithEmbeddedCertNoCode`——只改学段不改号→拒「学段码与任教学段不一致」+证书不变，学段与号同改到自洽（高中+idx12=4）→通过。无迁移（库 max 仍 V25）、无前端。

### Phase 49（P1-2 阶段1：视频上传「必然降级慢路径」修复，Phase 49 —— 分支 `feature/phase49-video-upload-compose`，单 commit，待主 Opus 复核合并，mvn verify 118/118 绿 + 前端 type-check/build 绿；活体 MinIO 证快路径）
- ✅ **根因**：前端分片 **512KB** < MinIO 服务端合并部件下限 **5MiB**（`io.minio.ObjectWriteArgs.MIN_MULTIPART_SIZE`）；旧 `merge()`（`VideoReviewServiceImpl:211-254`）为「先试 `composeObject`、`catch(Exception)` 一律回退 `streamComposeForSmallChunks`」——512KB 非末片分片令 `composeObject` 客户端下限校验**必抛** → **每次多分片上传都走慢路径**：对每个分片 `getObject` 拉回应用、再 `putObject` 整体重传，**字节两次穿过应用**、吞吐腰斩。
- ✅ **后端修法（异常驱动回退 → 按分片实际大小显式路由）**：新增 `canServerSideCompose(sortedChunks)`（多分片且**除末片外每片 ≥`MIN_COMPOSE_PART_SIZE`**＝`ObjectWriteArgs.MIN_MULTIPART_SIZE`＝5MiB，与 SDK 下限锁步）→ `merge()` `if 走 composeServerSide else 走 streamComposeForSmallChunks`。`composeServerSide`＝MinIO 服务端 `composeObject`（服务端 `UploadPartCopy` 拼接、字节不经应用），**显式 `.headers("Content-Type","video/mp4")`**（分片以 octet-stream 存储、快路径须回填视频类型供鉴权播放，与旧慢路径 `putObject` 显式 video/mp4 对齐、不回归）。**保留回退**：单分片 / 末片以外存在 <5MiB 分片时仍 `streamComposeForSmallChunks`（末片可 <5MiB、单分片上传均安全）。真实 compose 失败改**如实抛** `BizException`（由 `merge()` 外层 `catch` 复位 `MERGING→UPLOADING` 允许重试，语义不变、不再静默吞错）。
- ✅ **前端修法**：`UploadPanel.vue`/`UploadVideoDrawer.vue` 的 `chunkSize` 512KB→**8 MiB**，抽为 `api/video.ts` 命名常量 `VIDEO_UPLOAD_CHUNK_SIZE = 8 * 1024 * 1024`（两组件单一真源）。**part-size 契约端到端锁步**：前端 8MiB ≥ 后端 `MIN_COMPOSE_PART_SIZE`(5MiB) ＝ `io.minio` `MIN_MULTIPART_SIZE`；`VideoUploadInitRequest.chunkSize` 刻意仅 `@Min(1)`（单分片/末片可 <5MiB）、路由按**实际存储分片大小**（`VideoUploadChunk.chunkSize`＝`MultipartFile.getSize()`）判定不信任声明值；multipart 上限（100MB/120MB）已容 8MiB，无需改。
- ✅ **诚实证明「走了快路径」**：新增 `Phase7VideoReviewIT.largeMultipartUploadTakesServerSideComposeFastPath`——真 HTTP + 真 MinIO 传 2 片（首片恰 5MiB、末片 4096B）→ merge → 断言 ① 恰一个 `teaching-video` `file_object`；② 合并对象 `size`==两片和 且下载字节**逐字节==原始拼接**；③ `contentType()`==video/mp4；④ **`etag()` 含 '-'**——S3/MinIO 多部件合并 ETag＝`<hex>-<partCount>`，而流式回退单次 `putObject` 得纯 MD5（无 '-'），**④ 即活体判别「确实走服务端 compose 而非回退」的确定性信号**。既有 4 字节分片用例（<5MiB）继续行使**回退**分支，两分支均被 IT 覆盖。
- ✅ **验证**：`mvn -B -ntp clean verify`（先按 §0 精杀 :8080）**BUILD SUCCESS 118/118 绿**（原 117 + 新增 1），`Phase7VideoReviewIT` 16/16；前端 `npm run type-check` 干净 + `npm run build` 成功（仅既有 echarts/naive chunk 体积告警）。**无迁移**（纯代码 + 前端常量，库 max 仍 V25）。**阶段2**（`S3Presigner` multipart 预签名 → 浏览器直传 MinIO、字节完全不经 app + 异步转码）按 §4 **仍推迟**、非本相范围；阶段1 增强（并发 4–6 / Worker `crypto.subtle` 哈希）非平凡邻接，本相未纳入。

### Phase 37a-part1 ✅ 已完成并合并（`72beeaf`，mvn verify 83/83 绿 + 前端 build 绿）
- ✅ **P0-1 文件预签名 IDOR** — 删除无属主校验的 `GET /file/{id}/url`（无任何调用方，合法访问走带范围校验的业务端点）。**活体：** 学院B学生带 token 请求 → `data=None` 拿不到下载 URL（原可下载出学院A学生材料字节）。
- ✅ **P1 会话不可撤销** — 新增 `TokenRevocationService`（Redis 记撤销时点），登出/改密/管理员重置密码后使旧 token 立即失效；filter + refresh 均校验。**活体：** 登出后旧 token `/auth/me`→401（原 200），改密后旧 token→401，重新登录正常。
- ✅ **P0-4 无 prod profile/Swagger 公开** — 新增 `application-prod.yml` 关闭 Swagger/Knife4j + Flyway 安全项（clean-disabled、baseline-on-migrate）。
- ✅ **P1 前端批量下载永远失败** — `material.ts` 泛型笔误 `post<Blob>`→`post<unknown,Blob>` + 消费端 `res.data`→`res`。
- 说明：删除端点后命中会经全局兜底返回 500「系统异常」而非 404（P1-15 的 200/500-on-error 另行修）；安全目标（不泄露）已达成。

### Phase 37a-part2 ✅ 已完成并合并（本提交，mvn verify 83/83 绿 + 前端 type-check/build 绿；栈起活体实测）
- ✅ **P0-7 导入批次 IDOR** — `ExchangeServiceImpl` 新增 `ensureBatchAccessible(batch, perm)`：全校/系统范围放行，否则仅限本人创建的批次（批次以 `operatorId` 归属、无 college_id，用操作人归属校验规避 scopeJson 子串匹配）；植入 `errorReport/confirmImport/rollback`，`batches()` 列表按 allSchool-else-本人 operator 过滤。
- ✅ **P0-14 RBAC 授权"静默损坏/500"** — **改用"物理先删后插"**替代 `INSERT...ON DUPLICATE`：三张关联表加物理 `DELETE`（`deleteByUserId/deleteByRoleId`），Service 三方法（`replaceUserRoles/assignRolePermissions/assignUserDataScope`）改为 `deleteByX` + `mapper.insert(entity)`（id 由 `ASSIGN_ID` 生雪花、审计字段由 `AuditMetaObjectHandler` 自动填充），同时消除 id 复用与 `parentId*1000` 溢出。**过程更正：** 初拟"给 ON DUPLICATE 补 `role_id=VALUES`"经活体证伪——唯一键不含 `deleted`（`V7:63,99,116`），改 role_id 会撞软删旧行→`DuplicateKeyException`→HTTP200 code=500、DB 不变；先删后插彻底规避。**活体（复现→阻断）：** test_sys_admin 对 test_review_teacher_c 分配 [CLERK,AUDITOR,RT]→code=0/DB 三角色；改分配 [CLERK,RT]→**旧码 code=500/DB 仍 {A,B,C}；新码 code=0/DB 精确 {CLERK,RT}**，junction 用新雪花 id（`2073…858/859`），复原时 hard-delete 顺带清除历史脏行。
- ✅ **P0-15 角色授权弹窗静默改写数据范围** — `RolePermissionDrawer.vue` 打开时构建 `scopeByPermission` 记录后端真实 per-permission `scopeType`，保存用 `scopeFor()`（保留原范围→权限自身 canonical scopeType→兜底 SCHOOL）替代原 3 桶猜测 `defaultScopeFor`，不再一改全冲。
- ✅ **P0-9 TLS/安全头/超时** — `nginx.conf` 加 X-Frame-Options/X-Content-Type-Options/Referrer-Policy、`/api` 代理超时 600s（大导入/上传免 502/504）、gzip、`/assets` 长缓存，并附 443 TLS/HSTS/301 跳转模板（运维供证后启用）。
- 说明：验收栈与 IT 共用 `teacher_cert` 库，`mvn verify` 跑 83 IT 会改测试账号状态（本次把 `test_sys_admin.must_change_pwd` 置 1，已手工复位为 0）；上线前测试库须与运行库物理隔离（见 §7.11/P0-5）。

### Phase 37b ✅ 已完成并合并（本提交，mvn verify 83/83 绿；栈起 6 并发活体实测）
- ✅ **P0-10 状态流转 TOCTOU 竞态** — 18 处审核状态流转由"读状态→Java 判断→`updateById`"（无守卫、后写覆盖先写）改为 **DB 原子条件更新** `update(entity, wrapper.eq(id).eq(status, oldStatus))` + 校验受影响行数，0 行即抛"操作冲突"（`code=1000`）。覆盖：学生(submit/初审/复审)、专业培养(submit/初审/复审)、过程材料(submit/初审/复审)、免考(submit/初审/复审，状态字段 `finalStatus`)、证书(issue/markExported/archive/void)、视频(arbitrate/confirm)。单线程 happy-path 语义不变（status 恒等 → 命中 1 行），故 83 IT 全绿。**活体（复现→阻断）：** test_college_clerk 对同一 FIRST_REVIEW 学生（`990000000000000002`）6 线程 barrier 同时初审 PASS —— **旧码 6× code=0 / 审计 6 条 / 终态非确定（last-writer-wins）；新码仅 1× code=0、5× code=1000「操作冲突」、审计恰 +1、终态确定 `SECOND_REVIEW`**（达成 §8.2 验收标准）。
- ⏳ **P0-10 收尾（下一批，需异于状态守卫的处理）**：证书 `reissue`（并 §7.4「REISSUED 从不落库」+ 唯一约束）、`correct`（内容编辑非流转）、导入 `confirmImport` 双确认（需新增 IMPORTING 过渡态或导入幂等/唯一码，§7.1）、视频 `submitScore/settle/thirdReview/merge/returnReview`（分数计票/幂等）；以及 §7.1 唯一约束 `(student_id, assessment_year)` 等与 `BaseEntity` `@Version` 系统性兜底。

### Phase 37c ✅ 已完成并合并（本提交，mvn verify 83/83 绿，含 `Phase7VideoReviewIT` 11/11 真实 MinIO HTTP 集成）
- ✅ **P0-11 MinIO I/O 在事务内（连接池耗尽=全站总瘫，§7.2 运维最危项）** — `VideoReviewServiceImpl.uploadChunk/merge` 去方法级 `@Transactional`：MinIO `putObject`（分片）/`composeObject`+小分片流式回退（合并）在**事务外**执行，网络往返期间不再占用 DB 连接；元数据落库改 `TransactionTemplate` 短事务（分片：chunk 增改 + 进度刷新；合并：文件对象 + 评审 upsert + 会话状态；`detail()` 移到提交后）。`TransactionTemplate` 由 Spring Boot 自动装配注入。**验证：** 83/83 绿，其中 `Phase7VideoReviewIT` 11/11 经 RANDOM_PORT TestRestTemplate 走**真实 HTTP + 真实 MinIO/MySQL** 跑通 init→chunk→merge→评审全链路（栈级活体）；"连接不再跨 MinIO 往返被持有"属架构级保证（MinIO 调用已移出事务边界，新起后端 PID 亦确认 `TransactionTemplate` 运行期装配成功）。孤儿风险与改前一致（MinIO 非事务性，分片 key 确定可覆盖、合并随机 key 失败重试留孤儿，均同旧行为，未劣化）。
- ✅ **P0-11 收尾（Phase 37c-2，本提交）**：material/exemption 4 个上传方法（material `upload`/`replace`、exemption `uploadMaterial`/`replaceMaterial`）去方法级 `@Transactional`——各方法仅 1 次业务写（material/exemption insert/updateById），`fileService.upload`（MinIO putObject + file_object insert）在无环绕事务下执行、不占用 DB 连接。仅控制器调用（无自调用），单线程 happy-path 语义不变，83 IT 全绿（Phase5/6 覆盖上传/替换）。孤儿风险与视频一致（插入失败仅遗留无引用 file_object 行 + MinIO 对象，罕见）。

### Phase 38a ✅ 已完成并合并（本提交，mvn verify 83/83 绿，含 `Phase5MaterialIT` 4/4 真实 MinIO；栈起活体下载）
- ✅ **P0-2 批量下载整包进堆 OOM** — `ProcessMaterialServiceImpl.batchDownload` 不再用 `ByteArrayOutputStream` 攒完整 zip 再返回 `byte[]`；改为返回 `BatchDownloadFile(fileName, ContentWriter)`，`ContentWriter` 把 zip **直写 HTTP 响应输出流**（逐文件从 MinIO 读→写，全程不整包进堆），控制器改 `file.content().writeTo(response.getOutputStream())`。新增单次 **2000 条上限**（超限抛业务异常提示缩小筛选）。查询/上限校验在写响应头前完成（当前请求线程，数据范围/权限生效，错误干净）。**验证：** 83/83 绿，其中 `Phase5MaterialIT`（`:173` 上传材料入 MinIO → 批量下载 → `ZipInputStream` 解析断言 manifest）真实 MinIO 覆盖文件流式全链路；栈起活体：test_college_auditor `POST /material/batch-download` → HTTP200 `application/zip`、合法 zip（PK 头）+ `manifest.csv` + CRC 通过。
- ✅ **收尾（§7.3 P1，非本 P0，已完成 —— Phase 44b，见 §11）**：证书导出 `selectCertificates` 无界 + `XSSFWorkbook` 全 DOM 进堆 → 改 SXSSF 流式（窗口 200 行）+ 单次导出 20000 行上限。

### Phase 39 ✅ 已完成并合并（本提交，mvn verify 83/83 绿；栈起活体删学生→401）
- ✅ **P0-12 删学生不停登录（安全/访问撤销）** — `StudentServiceImpl.delete` 后置停用关联 `sys_user`（`.eq(studentId).set(status,'DISABLED')`）；JWT filter 每请求校验 `status=ENABLED`（`filter:62`）→ 旧 token 下次请求即 401。**活体（复现→阻断）：** test_student 旧 token `/auth/me` 删除前 200；管理员 `DELETE /student/9001` 后同一 token=**401「用户不存在或已停用」**、`sys_user.status=DISABLED`、`student.deleted=1`；测毕复原 ENABLED。
- ✅ **P0-12 删学院静默孤儿** — `OrganizationServiceImpl.deleteCollege` 在原「有专业则拒删」外，增「有用户（`sys_user.college_id`，含学生/教职工）则拒删」，补住"有学生却无 major 行"缺口（注入 `SysUserMapper`，同模块）。**活体：** 删有专业/用户的学院被拒、学院仍在（本例经既有专业守卫拦下；用户守卫为 majors=0 场景兜底，demo 无此实例，由编译+IT+守卫简单性保证）。
- ✅ **P0-12 收尾（Phase 43.1，已合并，见下条目）**：`deleteMajor` 改「停用」（引用方在 business 模块 + training 用 code/name 快照非 major_id、跨模块无关联键，遂旁路为停用而非补使用量守卫）、DB 外键决策＝**不加**（软删设计下近无用 + 回填风险高，孤儿防治留应用层）。⏳ **仍待**：MinIO 分片/孤儿清理与定时任务（P1-9）。

### Phase 41（41.1 + 41.3，Phase 41，已合并 —— 分支 `feature/phase41-ops`，单 commit，已 ff-merge 入 main，mvn verify 83/83 绿）
- ✅ **P0-5 生产 DB 非 root 最小权限账号** — 新增 `deploy/mysql-init/01-app-user.sh`（`docker-entrypoint-initdb.d` 初始化脚本；用 `.sh` 而非计划原文提到的 `.sql`——官方 MySQL 镜像对 `.sql` 按字面执行、不做变量替换，若要"密码来自环境变量、不硬编码"必须用 shell 读容器 env 再拼 SQL，`.sh` 是唯一能同时满足"env 驱动"与"非硬编码"两个约束的标准做法），创建 `teacher_app`@`%` 账号，仅授 `SELECT,INSERT,UPDATE,DELETE,CREATE,ALTER,INDEX,REFERENCES,DROP ON teacher_cert.*`（覆盖 DML + Flyway 46 个迁移文件实际所需 DDL，已逐一 grep 确认无 `CREATE TRIGGER/VIEW/PROCEDURE/EVENT`/`LOCK TABLES`/`CREATE TEMPORARY` 等更高权限需求），不授 `GRANT OPTION`/`SUPER`/`FILE`/`PROCESS`/`*.*`；若 `DB_PASSWORD` 未设置则初始化直接 `exit 1` 拒绝以空密码建账号。`application-prod.yml` 新增 `spring.datasource.username/password: ${DB_USERNAME}/${DB_PASSWORD}`（url 不变，仍用既有 `SPRING_DATASOURCE_URL`）。生产 `docker-compose.yml`：mysql 服务不再 `ports:` publish 3306 到主机、挂载 `./deploy/mysql-init:/docker-entrypoint-initdb.d:ro`；backend 服务把 `SPRING_DATASOURCE_USERNAME: root`/`SPRING_DATASOURCE_PASSWORD: ${MYSQL_ROOT_PASSWORD}` 替换为 `DB_USERNAME`/`DB_PASSWORD`。**dev 栈（`docker-compose.dev.yml`/`application-dev.yml`）未动，仍用 root**，符合任务范围。**验证：** 在 dev 库 `tcp-mysql` 容器内活体建同名测试账号 → `SHOW GRANTS` 核对与设计**完全一致**（`GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, REFERENCES, INDEX, ALTER ON \`teacher_cert\`.* TO ...`，无 `GRANT OPTION`/`SUPER`/`FILE`/`*.*`）→ 以该账号实测 `SELECT`/`CREATE TABLE`/`DROP TABLE` 均成功（满足 Flyway DDL 需求）→ `DROP USER` 清理，dev root 数据源全程未动、未重启。
- ✅ **41.3 `.dockerignore`** — 根新增，排除 `.env`/`.git`/`**/target`/`**/*.log`/`node_modules`/`frontend/dist`，防后端 `Dockerfile:17` 的 `COPY . .` 把真实密钥/`.git` 历史/编译产物打进镜像层。
- ✅ **41.3 Redis 生产鉴权** — `docker-compose.yml` redis 服务 `command` 加 `--requirepass ${REDIS_PASSWORD}`、不再 publish 6379 到主机，healthcheck 同步改 `redis-cli -a ${REDIS_PASSWORD} --no-auth-warning ping`；`application-prod.yml` 新增 `spring.data.redis.password: ${REDIS_PASSWORD}`。**MinIO(9000/9001) 端口本次未动**（见 §7.11 说明，presigned URL 内部 DNS 是独立更大的架构问题，非本任务范围）。
- ✅ **41.3 `FileServiceImpl.upload` InputStream 泄漏（§7.2 P1）** — 改 `try (in) { minioClient.putObject(...) }`（Java 9+ 等价 try-with-resources）；已逐一追踪全部 5 处调用方确认 upload 返回后均不复用该流，可安全内部关闭。
- ✅ `.env.example`/`README.md` 同步补 `DB_USERNAME`/`DB_PASSWORD`/`REDIS_PASSWORD` 说明，移除不再使用的 `MYSQL_PORT`/`REDIS_PORT`。
- **验证：** `mvn -B -ntp verify` **BUILD SUCCESS，83/83 绿（0 fail/error/skip）**，9 个 reactor 模块全 SUCCESS（含 `FileServiceImpl.upload` 改动路径覆盖的 `Phase5MaterialIT`/`Phase6ExemptionIT` 等真实 MinIO 上传集成测试）。配置类改动（yml/compose/`.dockerignore`）为语法与静态核对，未起 prod 栈活体（生产 compose 需要真实生产网络/密钥，本地不可行，符合 §3 前言"验收以配置正确+文档齐+可解释为准"）。
- **与规格的偏差**：§7.11 原文把 MySQL/Redis/MinIO 端口发布合并为一条，本 phase 任务书显式只收窄到 MySQL(41.1)+Redis(41.3)，MinIO 端口发布/公网可达性留待后续独立 phase（需先设计反代或公网 endpoint，否则即便发布端口，浏览器仍无法解析 `minio` 这一内部服务名）。
- ⏳ **未合并**：本 phase 严格按任务要求只做 41.1+41.3，**41.2（真实定时备份，P0-6）未动**；分支 `feature/phase41-ops` 停在单个 commit，待人工复核后再决定是否 merge 进 main。

### Phase 41.2（P0-6 真备份，Phase 41.2，已合并 —— 分支 `feature/phase41-backup`，单 commit，已 ff-merge 入 main，mvn verify 91/91 绿；栈起 fresh jar 活体复现→阻断，迁移 V25）
- ✅ **P0-6 假/空备份 → 真实应用内逻辑备份** — `SystemManagementServiceImpl.triggerBackup` 从「仅插一行 `COMPLETED`、`storageUri=manual://docs/...`、无产物」改为委托新增 `DatabaseBackupService`（platform-system）：先落 `backup_record`(`RUNNING`，`TransactionTemplate` 独立提交、运行期可见) → 取**单一 `REPEATABLE_READ` 只读连接**对 **36 张恢复关键表**（curated 显式清单，排除 `flyway_schema_history`）逐表 `SELECT *` 序列化为**可回放 `INSERT`**（含逻辑删除行、`deleted` 原样保留；按列 `Types` 生成 NULL/数值/BIT/二进制`0x`/转义字符串字面量）→ 流式写临时文件 `GZIP` 同步算 **SHA-256** → `putObject` 上传 MinIO（默认复用业务桶 `teacher-cert` 的 `db-backup/` 前缀，对象 `teacher_cert_<ts>_<id>.sql.gz`）→ 回写 `COMPLETED` + `storage_uri`/`byte_size`/`checksum(sha256:)`/`table_count`/`row_count`（失败置 `FAILED`+`error_message`，**不再伪造 COMPLETED**）。迁移 **V25** 给 `backup_record` 加 4 列，实体/VO 同步。
- ✅ **全库 0 处 @Scheduled → 定时全量备份（生产门禁）** — 新增 `BackupScheduleConfig`（`@EnableScheduling`+`@Scheduled` 每日 03:00 复用 `DatabaseBackupService`），**由 `platform.backup.schedule.enabled` 门禁、默认 false**（`@ConditionalOnProperty havingValue=true`）：dev/测试/未配置**整个配置连同 @EnableScheduling 都不注册、定时不触发**（保 91 IT 不被扰动），仅 `application-prod.yml` 显式置 true（cron 可配）。
- ✅ **手册改指 prod 栈 + 真机制** — 重写 `docs/备份与恢复手册.md`：应用内逻辑备份机制/触发/定时/从产物恢复（`mc cp`+`sha256sum`校验+`gunzip|mysql` 回放到 `teacher_cert_restore`）+ 表清单 + `backup_record` 字段表；dev 口令（root123/minioadmin123）明标**仅 dev**、生产用非 root `teacher_app`+env 注入凭据；声明「应用逻辑备份（A）与运维物理全备/binlog PITR/MinIO mirror（B）互补、A 不替代 B」。
- **决策（诚实标注）：** ① **JDBC 逻辑导出而非 shell out `mysqldump`**（计划 §3.2 择 B，Opus 定）——可移植、不给 Web 进程运维 shell/更高 DB 权限。② **curated 全量 36 表非狭义子集**——多含表只增强恢复保证不削弱，**非 fork**（计划 fork 警示针对「削弱恢复保证的取舍」），显式枚举保证确定性且不误导出 `flyway_schema_history`。③ **默认复用业务桶+`db-backup/`前缀非新建独立桶**——规避「新建桶是否合理」的 fork，桶启动即由 `MinioConfig` 保证存在、`ensureBucket()` 沿用既有 `bucketExists→makeBucket` 模式支持 prod 独立桶。④ 状态流用 `TransactionTemplate` 各自独立提交（RUNNING 运行期可见、FAILED 留痕），`triggerBackup` 去 `@Transactional`、导出读用独立连接不占业务事务。
- **验证：** `mvn -B -ntp verify`（先按 PID 精杀 :8080 验收后端 PID=5736；docker `tcp-mysql`/`tcp-redis`/`tcp-minio` healthy）**BUILD SUCCESS，91/91 绿（90 基线 + 新增 `Phase41BackupIT` 1，0 fail/error/skip）**，8 reactor 模块全 SUCCESS，Flyway 确认 **V25 24→25 迁移成功**（IT 启动即应用，全绿即证）。`Phase41BackupIT`（真实内嵌 Tomcat+MySQL+MinIO）触发→断言 `COMPLETED`/`storageUri` `minio://…/db-backup/`/`byteSize>0`/`checksum sha256:`/`tableCount≥30`/`rowCount>0`，`statObject` 核对大小、`getObject`+`GZIPInputStream` 解压断言含 `INSERT INTO \`sys_permission\`/\`sys_role\``、DB 查 `byte_size` 一致，末尾删对象+硬删记录自清理。**活体（复现→阻断，fresh jar PID 36620）：** test_sys_admin（`system:backup`，先复位 `must_change_pwd=0`）`POST /api/system/backup/trigger` → `code=0 status=COMPLETED`、`storageUri=minio://teacher-cert/db-backup/teacher_cert_20260704_135017_<id>.sql.gz`、`byteSize=378354`、`checksum=sha256:39f8d8c3…`、`tableCount=36`、`rowCount=19270`，`backup_record` DB 行同值；**独立 S3 SigV4 `GET` 取回→HTTP200 len=378354(==byteSize)、重算 SHA-256 与记录一致、`gunzip`=6494521B/19270 条 INSERT（含 `sys_permission`/`student` 真实行）**；`GET /system/backup` 列表可见。**旧（复现）＝记录是谎言、`storageUri=manual://`、无产物/大小/校验和/行数。** 测毕清理：MinIO 对象 `DELETE`→204、再 `GET`→404、`backup_record` 测试行硬删归 0、`test_sys_admin.must_change_pwd` 复位 1。前端未涉及。
- ⏳ **未合并**：分支 `feature/phase41-backup` 单 commit，`git remote -v` 空、未 push、未 checkout main，待人工复核合并。

### Phase 42.2（Phase 42.2，已合并 —— 分支 `feature/phase42-confirmimport`，单 commit，已 ff-merge 入 main，mvn verify 84/84 绿；真栈并发 IT 复现→阻断）
- ✅ **P0-10 收尾 / §7.2 confirmImport 双确认重复导入** — `ExchangeServiceImpl.confirmImport`（无 `@Transactional`、状态末尾才翻转）两并发都过 `PREVALIDATED` 检查 → 全量导入两次。修：`ExchangeBatchStatus` 加 `IMPORTING` 过渡态；confirmImport 开头**原子认领** `batchMapper.update(null, new LambdaUpdateWrapper<>().eq(id).eq(status,'PREVALIDATED').set(status,'IMPORTING'))`——因本方法无环绕事务、该 update 立即自动提交、对并发调用者立即可见（InnoDB 行锁串行化），仅首个 `claimed=1` 者进入导入循环、其余 `claimed=0` 抛 `BizException(当前批次不可确认导入（可能正在导入或状态已变更）)`；末尾照常翻 IMPORTED/FAILED，但改用「仍为 IMPORTING」守卫写（`update(entity, eq(id).eq(status,'IMPORTING'))`），防崩溃恢复边界下并发 rollback 已置的终态被本次覆盖。**IMPORTING 一致性处理（grep 全部用法）：** ① `ExchangeBatchStatus.of("IMPORTING")` 天然可用（`of` 遍历 `values()`）；② `rollback` 可回滚态集**纳入 IMPORTING**——进程在导入中途崩溃残留 IMPORTING 时，其经 REQUIRES_NEW 已提交的部分导入行可被 rollback 按 `import_record_ref` 追溯回收；③ `batches()`/`toBatchVO` 原样透传 status 字符串、无 NPE/mislabel；前端 `statusLabels.ts`/`StatusTag.vue`/`ExchangeImportView.vue` 补 `IMPORTING='导入中'` 标签+配色（原 `map[value]||value` 已优雅降级、不崩，此为可读性收敛）。`errorReport` 无状态门槛，IMPORTING 下照常可下载。**验证：** `mvn -B -ntp verify` **BUILD SUCCESS，84/84 绿（83 基线 + 1 新增并发 IT，0 fail/error/skip）**。**复现→阻断（真栈并发 IT，非 mock）：** 扩展 `Phase10ExchangeIT.concurrentConfirmImportClaimsBatchAtomicallyAndImportsExactlyOnce`——真实内嵌 Tomcat + 真实 MySQL，先 prevalidate 造一个 PREVALIDATED 批次，再 `ExecutorService` 2 线程经 HTTP 真并发打同一批次 confirmImport：**恰 1 个 `code=0`（data.status=IMPORTED、successCount=1）、恰 1 个 `code=1000`「…不可确认导入…」**，学生/证书各恰 1 行（无重复导入），批次终态 IMPORTED（未卡 IMPORTING）；旧码下两个都会过 PREVALIDATED 检查、都进循环（本 IT 即为其回归网）。
- **决策与残留（诚实标注）：** 采「真栈并发 IT + 代码边界」为证，未另起独立 :8080 :脚本活体——构造 PREVALIDATED 批次需完整 xlsx 上传+prevalidate 流，该 IT 已在真实内嵌 Tomcat+真实 MySQL 上以真并发 HTTP 完成等效复现（认领由 InnoDB 行锁串行化、确定不 flaky），比脚本活体更确定且可回归。**rollback 纳入 IMPORTING 的取舍：** 令崩溃残留可恢复（计划 §4.2 明示"崩溃残留恢复"），代价是打开一个极窄的「导入进行中被并发 rollback」窗口——收尾守卫写已保证状态一致（rollback 终态不会被错误覆盖成 IMPORTED），但 rollback 读 `import_record_ref` 之后导入循环仍可能提交个别后续行成孤儿；彻底关闭需在导入循环内逐行复核批次态，超出 42.2 范围，已诚实记录（同操作人因浏览器阻塞不可能触发、仅跨操作人全校范围对在飞导入 rollback 的极端情形；非产品分叉，主 P0 双确认重导已完全阻断）。
- ⏳ **未合并**：分支 `feature/phase42-confirmimport` 单 commit，待人工复核；42.3（视频计票/幂等）另行分发。

### Phase 42.3（Phase 42.3，已合并 —— 分支 `feature/phase42-video`，单 commit，已 ff-merge 入 main，mvn verify 86/86 绿；真栈并发 IT 复现→阻断）
- ✅ **P0-10 收尾 / §7.1 视频 settle 丢失更新** — `VideoReviewServiceImpl.submitScore/settleIfReady`：两评审并发提交末分，各自 REPEATABLE_READ 快照只见自己那条任务 → 都判票数不足不结算 → review 永卡 REVIEWING（或双结算）。修：**行锁 + 现读 + 原子条件更新三合一**——① submitScore 把 `requireReview` 换成新增 `lockReview`（`reviewMapper.selectOne(eq(id).last("FOR UPDATE"))`）对 review 行加悲观锁，串行化并发结算事务（后到者在锁释放=先到者提交后才计票）；② `settleIfReady` 计票 `taskMapper.selectList` 加 `.last("FOR UPDATE")`（锁定读=现读，绕过本事务快照读最新已提交行——后到者快照建于先到者提交前，普通读仍看不到对方任务，必须现读）；③ 结算落库由 `updateById` 改原子条件更新 `update(review, eq(id).eq(status,'REVIEWING'))`、0 行抛「操作冲突」（37b 同款收口）。**三者缺一不可**（仅串行化：后者普通读快照旧仍漏；仅现读不串行化：单事务内对方任务未提交、并发计票都=1 仍都不结算；仅条件更新：只防双结算防不住都不结算）。
- ✅ **P0-10 收尾 / §7.1 视频 merge 无幂等守卫** — `merge`（37c 已去方法级 `@Transactional`）重试/并发产生重复 `file_object` 行 + MinIO 孤儿 + 重指 videoFileId。修：`VideoUploadStatus` 加 `MERGING` 过渡态；merge 在 `ensureReviewReuploadable`+分片齐全校验**之后**、`composeObject`**之前**原子认领 `sessionMapper.update(null, eq(uploadId).eq(status,'UPLOADING').set(status,'MERGING'))`——merge 无环绕事务、update 立即 autocommit、InnoDB 行锁串行化，仅首个 `claimed=1` 者继续 compose+register；`claimed==0` 走 `handleNonClaimableMerge`（重读会话态：`MERGED`/`VALIDATION_FAILED`→回放既有评审 `detail` 幂等成功、不重复 compose+register；`MERGING`/其它→抛「视频正在合并或已完成，请勿重复提交」）；认领后 compose+落库整体 `try/catch`，异常把 `MERGING→UPLOADING` 复位允许重试防卡死。**保留 37c 的 MinIO 出事务**（compose 仍事务外、元数据仍 `TransactionTemplate` 短事务），仅前插认领。认领位置在 reuploadable 校验之后，故 Phase7 `reuploadIsRejected` 的「评审进行中不可重新上传」仍先抛、行为不变。
- ✅ **P0-10 收尾 / §7.1 thirdReview / returnReview 状态守卫（37b 手法）** — `settleThirdExpert` 结算落库改原子条件更新 `eq(status,'NEED_REVIEW')`、0 行抛冲突（两并发各插一条 THIRD_EXPERT 任务后争这条更新，负者 0 行抛异常 → `@Transactional` 回滚其任务插入 → 恰一次复评、恰一条第三专家任务）；`returnReview` 的 `updateReviewReturned(review, oldStatus)` 增 `.eq(status, oldStatus)` 守卫并返回行数、0 行抛冲突（防并发 return/confirm/arbitrate 竞态覆盖）。`arbitrate`/`confirm` 已在 37b 加同款守卫，本次未重做。
- **决策与偏差（诚实标注）：** ① settle 采悲观锁 `FOR UPDATE` 而非 `@Version`——本表无 `@Version`（§7.1 根因未系统性铺开）、且「都不结算」死局需跨事务现读可见性，乐观锁重试无法解决，`FOR UPDATE` 是最小且确定的解。② **MP `.last()` 位置坑（首轮 verify 复现并修正）**：本版 MyBatis-Plus 把 `.last("FOR UPDATE")` 段紧跟 WHERE、排在 `ORDER BY` **之前**（生成 `... FOR UPDATE ORDER BY submit_time ASC`，MySQL 语法错、每次 score 返 500），故 `settleIfReady` 去掉 wrapper `.orderByAsc`、改 Java 侧 `Comparator.comparing(getSubmitTime)` 排序（`submitted=1` 任务 `submit_time` 恒非空、等价原 `ORDER BY`）。③ **采「真栈并发 IT + 代码边界」为证，未另起 :8080 脚本活体**——视频全链路造数极重（init→分片→merge→assign→双评审→并发 score），新增 2 条 IT 已在真实内嵌 Tomcat + 真实 MySQL/MinIO 上 2 线程 `CyclicBarrier` 真并发 HTTP 等效复现（与 42.2 同口径）；settle「复现」侧（旧码卡 REVIEWING）时序相关、以代码边界为主并诚实标注，merge「阻断」侧 file_object 计数为确定性不变式。范围严格限 42.3，未碰 42.1/42.2/42.4。
- **验证：** `mvn -B -ntp verify`（先按 PID 精杀 :8080——本次无监听者）**BUILD SUCCESS，86/86 绿（84 基线 + 2 新增并发 IT，0 fail/error/skip）**，9 reactor 模块全 SUCCESS。**真栈并发 IT（复现→阻断，非 mock）：** `Phase7VideoReviewIT` 13/13（原 11 + 新 2）——① `concurrentFinalScoreSubmissionsSettleExactlyOnceWithoutStuckReviewing`：assign 双评审后 2 线程 barrier 同发末分 score，**两请求均 code=0**、review 落 `REVIEW_COMPLETED`（未卡 REVIEWING）、finalScore=83、`settle` 审计**恰 1 条**、两 REVIEWER 任务 submitted=1；② `concurrentMergeProducesExactlyOneFileObject`：全分片后 2 线程同发 merge，codes∈{0,1000}（含 0）、**`file_object`(teaching-video) 恰 1 行**、review=WAIT_REVIEW、会话=MERGED（未卡 MERGING）。既有 11 用例 + `Phase14E2EIT` 14/14（视频 thirdReview/confirm 子流程）全绿未回归。首轮曾因 MP `.last` 坑致 18 fail+1 err（含 Phase14 连带 NPE），改 Java 排序后二轮全绿——证 NPE 系视频 500 连带、非证书导出真回归。至此 Phase 37b 遗留的视频计票/幂等子批全部闭环。
- ⏳ **未合并**：分支 `feature/phase42-video` 单 commit，待人工复核；42.1（uk 唯一约束/V24）、42.2（confirmImport 双确认）、42.4（reissue REISSUED）另行分发。

### Phase 42.4（Phase 42.4，已合并 —— 分支 `feature/phase42-reissue`，单 commit，已 ff-merge 入 main，mvn verify 83/83 绿；栈起 2 并发活体阻断）
- ✅ **P0-10 收尾 / §7.4 证书 reissue REISSUED 死枚举 + 并发双重开** — `CertificateServiceImpl.reissue` 原「`setLocked(1)`+`updateById`、从不 `setStatus(REISSUED)`」→ 原证永远停在 VOIDED、可被反复重开（无界链），且审计记录了 DB 从未发生的 VOIDED→REISSUED。改为沿用 Phase 37b 同款 **DB 原子条件更新**：`original.setStatus(REISSUED)` 后 `certificateMapper.update(original, wrapper.eq(id).eq(status,'VOIDED'))`，受影响行数=0 抛 `BizException(证书状态已变更或已重开，请刷新后重试)`——既把 REISSUED 真正落库（审计与 DB 一致），又保证两并发只有一个命中 `status='VOIDED'`、另一个被拒。`activeCertificate` 活跃态集本已 `notIn(VOIDED,REISSUED)`，REISSUED 天然非活跃、不计入活跃证书（无需改）。`Phase9CertificateIT.voidAndReissue...` 同步：断言由「原证仍 VOIDED」改「原证 REISSUED」+ 新增「再 reissue 被拒 code=1000『仅已作废证书可重开』」。未依赖 42.1 的 `uk_cert_active` 唯一约束，本守卫独立成立。**验证：** `mvn -B -ntp verify` **83/83 绿**（Phase9CertificateIT 7/7）。**活体（阻断）：** 后端 PID 13592（fresh jar），DEMO 学生 005 补齐前置 → generate(C0，certNo …00001)→ void(VOIDED)→ **2 并发 reissue：恰 1 成功**（新证 certNo …00002 GENERATED）、**1 失败 code=1000「证书状态已变更或已重开」**（命中原子守卫）、**原证 C0 落库 REISSUED**、审计 `reissue|VOIDED|REISSUED`、活跃证书恰 1 张；再对已 REISSUED 的 C0 reissue → code=1000「仅已作废证书可重开」。测毕全量复原（certificate/cert_sequence/materials/test/video/cert-audit 均归 0）。「复现」侧由改前 Phase9 断言（曾固化「原证保持 VOIDED」错误行为）佐证，未对旧码另起活体。
- ⏳ **未合并**：分支 `feature/phase42-reissue` 单 commit，待人工复核；42.1（uk 唯一约束/迁移 V24）、42.2（confirmImport 双确认）、42.3（视频计票/幂等）另行分发。

### Phase 42.1（Phase 42.1，已合并 —— 分支 `feature/phase42-uniques`，单 commit，已 ff-merge 入 main，mvn verify 83/83 绿；栈起 2 并发活体复现→阻断）
- ✅ **P0-10 收尾 / §7.1 缺唯一约束致并发重复创建（证书 + 学生证件号）** — 迁移 **V24**（`V24__unique_active_cert_and_student_idcard.sql`，磁盘 max V23+1）用 **MySQL STORED 生成列 +「活跃态才非空」** 方案补两处唯一约束（避开"重开合法多张 + 软删撞键"两难，NULL 不入唯一约束）：① 证书 `active_key = CASE WHEN deleted=0 AND status NOT IN('VOIDED','REISSUED') THEN CONCAT(student_id,'-',assessment_year) END` + `uk_cert_active`——活跃态口径严格对齐源码唯一真源 `CertificateServiceImpl.activeCertificate`（`notIn(VOIDED,REISSUED)` + `@TableLogic` deleted=0，**含 WAIT_GENERATE**），刻意采此而非计划示例的正列举 `IN(GENERATED,ISSUED,EXPORTED,ARCHIVED)`，以杜绝 DB 约束与应用层守卫口径缝隙（WAIT_GENERATE 实不落库、两写法对真实数据等价）；② 学生 `idcard_key = CASE WHEN deleted=0 THEN id_card_no END` + `uk_student_idcard`（id_card_no 明文存储，可直接生成列唯一）。服务侧最小加固：`CertificateServiceImpl.generate` 既有 `catch(DuplicateKeyException)` 按索引名区分——撞 `uk_cert_active`→`BizException(本年度已有有效证书)`、撞 `uk_certificate_cert_no`→原「证书编号已存在，请重试」；`StudentServiceImpl.create` 新增 `catch(DuplicateKeyException)`，撞 `uk_student_idcard`→`BizException(证件号码已存在)`（与既有 `existsIdCardNo` 预检同措辞，预检命中/并发撞键 UX 一致），非该键原样上抛（不劣化既有行为）。**上线前存量违约前置检查（迁移文件头注明）：** 两条 `GROUP BY…HAVING COUNT>1` 在共享 dev 库 `teacher_cert` 均为空（无违约）+ 以真实数据抛弃型克隆表试跑全部 DDL 通过（MySQL 8.0.46 生成列语法有效、无违约）。**验证：** `mvn -B -ntp verify` **BUILD SUCCESS，83/83 绿（0 fail/error/skip）**，V24 于 IT 启动经 Flyway 干净应用（`flyway_schema_history` V24 `success=1`、两唯一索引在库）。**活体（复现→阻断）：** fresh jar（后端 PID 16336），DEMO 学生 005 补齐前置 → **2 并发 `POST /cert/generate` 同 student-year：恰 1 成功（GENERATED，certNo …00001）、1 被拒 `code=1000`「本年度已有有效证书」（命中 `uk_cert_active` DB 约束、非裸 500）**，活跃证书恰 1 张；确定性 DB 级佐证：活跃证在库时直插第二张活跃证 → `ERROR 1062 (23000) Duplicate entry '990000000000000005-2026' for key 'certificate.uk_cert_active'`。**2 并发 `POST /student` 同证件号/异学号：恰 1 成功、1 被拒 `code=1000`「证件号码已存在」（非 500）**、DB 仅 1 名未删同证件号学生。测毕全量复原（certificate/cert_sequence/cert-audit/pm/atr/vr 归 0，新建学生 + 账号硬删，未删学生数回基线 12；索引保留、共享库无残留）。
- ⏳ **未合并**：分支 `feature/phase42-uniques` 单 commit，待人工复核；42.2（confirmImport 双确认）、42.3（视频计票/幂等）另行分发。

### Phase 43.4（Phase 43.4，已合并 —— 分支 `feature/phase43-failopen-dict`，单 commit，已 ff-merge 入 main，mvn verify 88/88 绿）
- ✅ **§7.7 校验 fail-open → fail-closed（StudentStatus/TrainingStatus/MaterialStatus）** — 三者 `of()` 原 `orElse(DRAFT)`（未知值静默变最可编辑草稿）改 `orElseThrow`（`BizException`"未知XX状态: {value}"），与 `ExemptionStatus.of` 已有写法一致。**改前安全预检（强制门禁）：** 对 `student`/`training_profile`/`process_material` 三表执行 `SELECT DISTINCT status`（`deleted=0` 与含软删行两口径均查）核对是否存在超出枚举定义的脏值——`student`（1276 行）DISTINCT 值为 `{DRAFT,FIRST_REJECTED,FIRST_REVIEW,SECOND_REVIEW,PASSED}`、`training_profile`（5 行）为 `{DRAFT,FIRST_REVIEW,SECOND_REVIEW,PASSED}`，均 ⊆ 各自枚举全集（`DRAFT/FIRST_REVIEW/FIRST_REJECTED/SECOND_REVIEW/SECOND_REJECTED/PASSED/FAILED`）；`process_material` 当前 0 行（空表，vacuously clean，非"零风险"但当前无违约数据）。三者均无脏值，故按计划全部切换，未触发"发现脏值即 STOP"的门禁分支。已确认 `StatsServiceImpl` 对 `StudentStatus.of`/`MaterialStatus.of` 的调用已自行 try/catch 兜底（不受影响），全仓 `.of(` 调用点均作用于持久化实体字段（非用户直接可控的原始输入）。
- ✅ **§7.7 `BirthDateValidator` 校验过松** — 原实现仅检查月份 1-12、日期 1-31（如 2023-02-30 会被放行），改用 `LocalDate.of(year, month, day)` 的日历规则强校验（自动涵盖闰年 2 月 29 日、大小月边界），日历上不存在的日期统一抛 `BizException("出生日期格式异常")`。新增回归断言于 `Phase3StudentIT`（`resident_id_card` + 生日 `2023/02/30` → 建档被拒，`msg` 含"出生日期格式异常"）。
- ✅ **§9.2 字典编码唯一性软删盲 → 复建报 500** — `DictServiceImpl.existsTypeCode/existsItem` 原用 `selectCount`（MyBatis-Plus `@TableLogic` 自动过滤 `deleted=0`），但 `uk_sys_dict_type_code`/`uk_sys_dict_item_type_code_year` 两唯一键均不含 `deleted` → 软删后同码/同项重建会绕过该检查、直接撞库抛裸 `DuplicateKeyException`，此前无专属处理器，经兜底 `Exception.class` 返回 HTTP 200 但 `code=500`"系统异常"。修复对齐 `OrganizationServiceImpl` 已有的 `*IncludingDeleted` 模式：新增 `SysDictTypeMapper.countByTypeCodeIncludingDeleted`/`SysDictItemMapper.countByItemIncludingDeleted`（原生 `@Select`，不带 `deleted` 过滤），`existsTypeCode/existsItem` 改调用两者，使应用层在写库前即能发现软删冲突、返回友好 `BizException`（"字典类型编码已存在"/"字典项编码在当前年度版本已存在"，`code=1000`）。**并加全局兜底：** `GlobalExceptionHandler` 新增 `@ExceptionHandler(DataIntegrityViolationException.class)`，把任何未被服务层预检拦下的唯一键冲突统一转为业务提示"数据已存在或不满足唯一性约束，请刷新后重试"（`code=1000`）而非裸 500——作为其余唯一键（RBAC 关联表等，本次未逐一排查）的最后一道防线。RBAC 侧同类检查是否存在同款盲区**不在本次范围，未排查，仍待跟进**。
- **测试补强：** Dict 此前无任何 IT 覆盖（`platform-boot/src/test` 14 个 `Phase*IT` 均未涉及），新增 `Phase1DictIT`（2 个用例）：`deleteThenRecreateSameTypeCode_returnsFriendlyErrorNotServerError`、`deleteThenRecreateSameItemCode_returnsFriendlyErrorNotServerError`，均为真栈 HTTP（`TestRestTemplate`+真实 MySQL）"创建→软删→同码重建"全链路，断言 `code=1000` 且 `msg` 精确匹配友好提示（非 500）。
- **验证：** `mvn -B -ntp verify`（无 :8080 占用者，docker `tcp-mysql`/`tcp-redis`/`tcp-minio` 均 healthy）**BUILD SUCCESS，88/88 绿（86 基线 + 2 新增 Dict IT，0 fail/error/skip）**，9 个 reactor 模块全 SUCCESS。fail-closed 改动为行为敏感变更，86 基线用例全部通过、无一依赖原 DRAFT 兜底行为，未发现需要为此减弱断言的情形。前端未涉及（无 `.ts`/`.vue` 改动），未跑 `npm run type-check`。未另起独立 :8080 活体栈——两条新 Dict IT 已在真实内嵌 Tomcat + 真实 MySQL 上端到端覆盖"软删→同码重建→友好错误"，IT 证据已充分（任务书注明"IT + 代码边界即可"）。
- **与规格的偏差/疑问：** 无阻塞、无 STOP 级分叉。范围严格限 43.4，未碰 43.1/43.2/43.3，亦未处理 §7.7 第三条"training/exemption 双提交唯一约束冲突裸 500"（不在本次任务范围，另行分发）与 RBAC 同类软删盲区排查（同上）。
- ⏳ **未合并**：分支 `feature/phase43-failopen-dict` 单 commit，待人工复核；43.1/43.2/43.3 及 §7.7 遗留项、RBAC 软删盲区排查另行分发。

### Phase 43.2（Phase 43.2，已合并 —— 分支 `feature/phase43-cert-roundtrip`，单 commit，已 ff-merge 入 main，mvn verify 90/90 绿；导出→导入往返/序列/签发日期 IT 复现→阻断）
- ✅ **§7.4 往返列复用坏账（导出 `status`→备注 / 导入备注→学院id `parseLong`）** — `ExchangeServiceImpl.rowFromCertificate` 原把 `cert.getStatus()` 写入标准表「备注」列（Z 列），而导入端 `resolveCollegeId` 却把备注列当学院ID `parseLong` → 重导出文件的备注="ISSUED"（非数字）被 `parseLong` 得 null、学院回退识别，一列被赋予两种冲突语义。修：`rowFromCertificate` 备注列改写 `cert.getCollegeId()`（学院ID），与导入端读备注解析学院ID 的既有契约对齐 → 导出→导入 学院标识无损往返；证书状态本已有汇总表/完整审核表的「证书状态」专列承载，不再复用备注表达两义。**教育部 26 列 A-Z 模板布局（列头/列序/列数）完全未改，仅修正自由「备注」列（`required=false`）承载的内部值**（见下 FORK CHECK）。
- ✅ **§7.4 导入 cert_no 不占 `cert_sequence`（与自动生成永久撞号）** — 导入证书号不推进序列 → `generate` 迟早命中被导入占用的号、撞 `uk_certificate_cert_no` 回滚，而 `nextSequence` 的 `current_seq+1` 与 `generate` 同事务一并回滚 → 每次重试重算同一号、永远「证书编号已存在，请重试」。修：新增 `CertificateService.reserveImportedSequence(certNo)`（`CertificateServiceImpl`，`@Transactional`），由 18 位编号自身还原 年度/学校码/学段码、按与 `nextCertNo` 同一 `scopeKey` 规则把对应序列推进到 ≥导入号序号（`FOR UPDATE` 锁行，与既有 `nextSequence` 共用 `lockScopeRow`/`setSequence`）；`ExchangeServiceImpl.importOne` 证书落库后调用之，与本行导入同一 REQUIRES_NEW 事务（证书与序列同提交/同回滚）。非本系统 18 位标准号（历史/外部号）无法映射作用域 → 跳过占用、不阻断导入。
- ✅ **§7.4 导入 status=ISSUED 但 issueDate 从不设 + 无补设路径** — `ExchangeServiceImpl.applyCertificate` 对「状态为 ISSUED 且 issueDate 为空」的导入证书补一个**与系统 `validUntil` 规则自洽**的签发日期：签发年度取证书年度（`assessmentYear`=证书号前 4 位）、上/下半年取自 `validUntil` 结尾（`.../6/30`→`/6/30`、`.../12/31`→`/12/31`），使对该签发日期再套 `CertificateServiceImpl.validUntil()` 能复现导入的有效期。既覆盖新导入、亦对既有 ISSUED 缺失签发日期的行按 `!hasText(issueDate)` 补设（补设路径，不覆盖已有值）。
- **测试补强（+2，90/90）：** 新增 `Phase43CertRoundTripIT`（真栈 HTTP：导入走 prevalidate→confirm、生成走 `/api/cert/generate`）2 用例：① `standardExportCarriesCollegeIdInRemarkAndRoundTripsCollegeAndIssueDate`——导入一张 COLLEGE_B 证书→标准导出的备注列(索引25)=学院ID 且 ≠"ISSUED"（旧码会写"ISSUED"，此断言在旧码上失败），再把导出文件 `readStandardRows` 读回、改学号/证件号/证书号后重导入→新学生/新证书仍落 COLLEGE_B（学院无损往返）、且新证书 `issueDate`="2035/6/30"（缺陷3）；② `importedCertNoOccupiesSequenceSoLaterGenerateDoesNotDeadCollide`——导入号占用 junior/2035 作用域序号 00001→`cert_sequence.current_seq`≥1（代码边界证据），再对同作用域自动生成→得 `...00002`（跳过已占号，旧码会在 generate 处撞号失败）。**同时更新既有 `Phase24AcceptanceIT.assertStandardExport`**：原 `data.getCell(25)`（备注列）断言 `"ARCHIVED"`——它固化了「导出把证书状态写入备注」的错误行为，现改断言学院ID `String.valueOf(COLLEGE_A)`，与修复后的正确往返语义一致。
- **验证：** `mvn -B -ntp verify`（先杀 :8080 占用者——本次全程 :8080 空闲；docker `tcp-mysql`/`tcp-redis`/`tcp-minio` 均 healthy）**BUILD SUCCESS，90/90 绿（88 基线 + 2 新增 IT，0 fail/error/skip）**，9 个 reactor 模块全 SUCCESS。**证据口径诚实：以 IT + 代码边界为证，未另起 :8080 活体栈**——三条缺陷均在真实内嵌 Tomcat + 真实 MySQL 上端到端复现→阻断（往返列/序列/签发日期），符合任务书「IT + 代码边界即可、不得虚构活体」。前端未涉及（无 `.ts`/`.vue` 改动），未跑 `npm run type-check`。
- **FORK CHECK（已核，非分叉）：** 教育部 26 列 A-Z 标准模板是固定外部规格，但本次**未改任何列的列头/列序/列数**；「备注」列在标准里即自由 remark 字段（`ExchangeColumn.REMARK` `required=false`），并无外部规格规定它必须承载证书状态或学院ID——是导出/导入两端对该自由列的**内部语义各行其是**（导出写状态、导入读学院ID）造成的往返坏账。故这是内部契约 bug、非模板格式的产品决策，可直接修正、无需 STOP 上报。
- **与规格的偏差/范围：** 严格限 §7.4 的三条往返/序列/签发日期项（对应 §7.4 列表第 3/4/5 条），未碰 43.1/43.3/43.4 及其它 phase。**§7.4 另两条〔撰写 43.2 时未在其范围、留待另行分发〕现已由 Phase 48 修复（见 §11）：**「导入静默改 voided/archived 证书」（`certificateByStudentYear` 无状态过滤，与 `correct()` 守卫矛盾）与 P2「`correct()` 不校验 cert_no 内嵌码与学段/层次一致」——Phase 48 在 `importOne` 镜像 correct() 终态守卫拦截导入改写终态证书，并在 `correct()` 触及 certNo/segment 时校验内嵌段码自洽。
- ⏳ **未合并**：分支 `feature/phase43-cert-roundtrip` 单 commit（off main `0323e7e`），`git remote -v` 为空、未 push、未 checkout main、未 merge，待人工复核。

### Phase 43.1（Phase 43.1，已合并 —— 分支 `feature/phase43-nofk-disablemajor`，单 commit，已 ff-merge 入 main，mvn verify 91/91 绿；栈起 fresh jar 活体阻断）
- ✅ **P0-12 收尾 / DB 外键决策＝不加（用户拍板，仅文档化）** — 全库全程软删（`@TableLogic`）、生产码无硬删 → 软删父行仍物理存在（FK 约束仍满足）→ FK 级联删除几乎不触发；对既有存量回填「无违约 + 迁移」再加 FK 风险高、收益低。孤儿防治维持在**应用层**：删学院用户守卫 + 删学生停登录（Phase 39 已交付）+ `deleteMajor→停用`（本 phase）+ 孤儿巡检定时任务（P1-9 后续）。**零 schema/代码改动、无新迁移**（库 max 仍 V25）。
- ✅ **P0-12 收尾 / `deleteMajor` 软删 → 停用** — `OrganizationServiceImpl.deleteMajor` 原「`majorTrainingGoalMapper.delete(...)` 硬删该专业全部 `major_training_goal` 联动 + `majorMapper.deleteById` 软删专业行、零使用守卫」→ 改为「`requireMajor` 取实体；已 `DISABLED(0)` 则幂等直接返回；否则 `setStatus(DISABLED)` + `updateById`」，**不再删除联动**（停用非删除，联动随专业保留、恢复启用后目标不丢；停用专业已从新增/下拉可选项排除，联动仅在启用专业生效）。新增 `DISABLED=0` 常量。**为何停用而非补跨模块使用量守卫**：引用方 `student`/`training_profile` 在 business 模块且以专业 **code/name 快照**引用（非 `major_id`），system 无法常规 count 跨模块使用量；停用使行持久化、快照可解析、杜绝孤儿且可恢复，旁路了跨模块数使用量难题。控制器 `MajorController.delete`（`DELETE /api/major/{id}`，`@pms.has('major:manage')`）端点与 `@AuditLog(operation="delete")` **不动**（HTTP DELETE 仍表「移出可用名册」、审计沿用历史），仅服务语义与前端标签改。
- ✅ **前端 `MajorsPanel.vue` 标签由「删除」改「停用」** — 行操作按钮 `删除`→`停用`（`type` `error`→`warning`）、`NPopconfirm` 文案 `确认删除该专业？`→`确认停用该专业？停用后不再可选用（可在编辑中恢复启用），已有引用不受影响。`、处理函数 `removeMajor`→`disableMajor`、提示 `专业已删除/删除失败`→`专业已停用/停用失败`。「状态」列本已渲染 `status===1?启用:停用`、`MajorDrawer` 状态开关可切回启用 → 停用专业读作「停用」且可编辑恢复，无需新增 UI。
- **IT 影响：无需修正** — 全仓核对 `platform-boot/src/test` **无任何 IT 断言 major 删除行为**（`grep deleteMajor / DELETE /major/{id}` 均无命中；含"major"的测试文件仅把专业作夹具）→ 未新增/删除/减弱任何测试，91 基线不受扰。
- **验证：** `mvn -B -ntp verify`（先按 PID 精杀 :8080 验收后端 PID=36620；docker `tcp-mysql`(healthy)/`tcp-redis`/`tcp-minio` 均在）**BUILD SUCCESS，91/91 绿（0 fail/error/skip）**，8 reactor 模块全 SUCCESS。前端 `npm run type-check` 干净、`npm run build` 成功（仅既有 echarts/naive chunk-size 警告）。**活体（栈起 fresh jar，新 PID 20968，非旧 36620；复位 `must_change_pwd`）：** test_sys_admin（`SYS_ADMIN`，持 `major:manage`）对 major `810000000000000101`（P4_NORMAL_A，学院A，2 条 `major_training_goal` 联动）`DELETE /api/major/810000000000000101` → `http=200 code=0`；**改后 `sys_major.status=0`（停用）、`deleted=0`（行仍在库、`GET /major/{id}` 仍返 status=0 = 未软删）、`major_training_goal` 活跃联动仍 2 条（旧行为硬删归 0）、`training_profile` 活跃 5 行不变（业务快照引用不孤儿）**；再次 `DELETE`→`code=0`、`status` 仍 0（**幂等**）。测毕**精确复原** `sys_major(810…101)` 至 `status=1/deleted=0/updated_by=NULL/updated_at=2026-06-18 20:37:00`（原值）、2 条联动原样、`test_sys_admin.must_change_pwd` 复位 1。**旧（复现）＝ deleteMajor 软删专业行 + 硬删联动 → 引用 code/name 快照的 student/training 悬挂已删专业成孤儿。**
- ⏳ **未合并**：分支 `feature/phase43-nofk-disablemajor` 单 commit（off main），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核。P0-12 剩 MinIO 分片/孤儿清理 + 孤儿巡检定时任务（P1-9）；43.3（Rule 11 材料卡死）、40（SB 升级）另行分发。

### Phase 43.3（Phase 43.3，已合并 —— 分支 `feature/phase43-rule11`，单 commit，已 ff-merge 入 main，mvn verify 93/93 绿；真栈 IT 复现→阻断 + fresh jar 活体演示恢复路径）
- ✅ **§7.5 唯一真 bug / Rule 11 过程性材料合格永久卡死（near-P0）** — `ProcessMaterialServiceImpl.categoryStatus` 原按**全历史行**计数 `passed = passedCount>0 && failedCount==0`：FAILED 是终态（`editable()` 排除 FAILED、`locked=1`，delete/replace 被 `ensureEditable` 挡、控制器无管理员 override），一条 FAILED 永久钉住 `failedCount≥1` → 该类别再也无法 passed，即便重传新材料通过 → `processStatus.qualified` 永假 → 证书 `doPrecheck` 永远缺「过程性考核」、**签发永久卡死无恢复**。修：合格判定改为**只看该类别「最新/有效」的一份材料**。
- ✅ **实现的确切合格语义** — 分组槽位 = 同一 `(student_id, assessment_year, category)`（`processStatus` 按 `studentId+assessmentYear` 查 `process_material` 再 `groupingBy(category)`，`selectList` 经 `@TableLogic` 已排除软删行）；「最新/有效材料」= 该槽位内 `records.stream().max(Comparator.comparing(getId))` 一条；`item.setPassed(effective != null && MaterialStatus.of(effective.getStatus()) == PASSED)`。**「最新」取主键 `id` 而非 `uploadTime`**：`id` 为雪花 `ASSIGN_ID`、随创建时间单调递增、**不可变**、不受 `replace`（可编辑态才可 replace，会改 `uploadTime`）影响，最稳健地表达「最后一次提交的那份材料」；在所有关键场景（FAILED→PASSED 恢复、PASSED→FAILED、仅 FAILED、无材料、happy-path）`id` 与 `uploadTime` 排序一致。`totalCount/passedCount/failedCount` **保留全历史口径、仅作前端展示**（「复审通过」「不通过」列），不参与合格判定。
- ✅ **正确性边界（为何真失败仍失败）** — 该语义只让**被取代的 FAILED**（其后同槽位有更晚材料）停止阻断，绝不放行真失败：① 某类别唯一一份材料 FAILED、无更晚材料 → `effective` = 该 FAILED → 不合格；② 类别无任何 PASSED 材料 → `effective` 非 PASSED → 不合格；③ **先 PASSED 再上传更晚材料被判 FAILED → `effective` = 最新的 FAILED → 不合格**（证明修复不是「只要历史上有过 PASSED 就放行」，而是严格按最新有效材料判定）；④ 恢复仅当学生为同槽位重传的**更晚**材料真正走完初审+复审并 PASSED（`effective` = 该 PASSED）才成立。
- **测试补强（+2，93/93）：** `Phase5MaterialIT` 由 4→6。① `failedCategoryRecoversWhenNewerMaterialPasses`（恢复/正向、即修复的回归网）——某类别 upload→submit→初审 PASS→复审 **FAIL**（FAILED、locked）后 `process-status` 该类别 `passed=false`；学生同类别重传一份新材料并复审 PASS → 该类别 `passed=true`（`totalCount=2/passedCount=1/failedCount=1`，历史计数不变但合格恢复）。**旧码在同一数据上 `passed = (1>0 && 1==0) = false`，本用例即对旧码的失败断言。** ② `genuineFailureStaysUnqualifiedIncludingLatestFailOverridingEarlierPass`（负向边界）——类别 A 唯一材料 FAILED → `passed=false`；类别 B 先复审通过(PASSED) 再上传更晚材料被判 FAILED → `passed=false`（最新 FAILED 取代先前 PASSED）、整体 `qualified=false`。真栈 HTTP（`TestRestTemplate` + 真实内嵌 Tomcat + 真实 MySQL + 真实 MinIO，走完整 upload→两级审核→重传→再审 全链路）。
- **IT 影响：无需修正/减弱任何断言** — 全仓引用 `processStatus`/`qualified` 的既有 IT 仅 `Phase5MaterialIT.processQualifiedRequiresAllFourCategoriesPassed`（含「SKILL 唯一材料 FAILED → qualified=false」——真失败、单份材料，本修复保持 false）、`Phase24AcceptanceIT`/`Phase14E2EIT`（均 happy-path、全类别 PASSED → qualified=true，本修复保持 true）。**无任何既有 IT 固化「一条 FAILED 永久钉死」的旧行为，故无需修正**（区别于 43.2/43.4 曾更正固化错误行为的断言）。
- **验证：** `mvn -B -ntp verify`（先按 PID 精杀 :8080 验收后端 PID=20968；docker `tcp-mysql`(healthy)/`tcp-redis`/`tcp-minio` 均在）**BUILD SUCCESS，93/93 绿（91 基线 + 2 新增 IT，0 fail/error/skip）**，8 reactor 模块全 SUCCESS，`Phase5MaterialIT` 6/6。纯后端逻辑改动、无迁移（库 max 仍 V25）、无前端改动（前端本已直用后端 `passed` 字段、`passedCount/failedCount` 仅展示，不再自行推导合格，无 UI bug 复燃；未跑 `npm run type-check`）。**活体（复现→阻断，fresh jar PID 40912，非旧 20968）：** `jhw-rule11-livetest.py` 以 test_student(9001，SELF；先复位 `must_change_pwd=0`) 直接 seed `process_material` 两场景并调真实 `GET /material/process-status/9001?year=LIVE-43.3` → **MORALITY（先 FAILED id 更小、后 PASSED id 更大）`passed=true`（恢复）**、**COURSE（先 PASSED、后 FAILED id 更大）`passed=false`（最新 FAILED 边界）**、`qualified=false`；脚本同时复算**旧算法在同一数据上 MORALITY.passed = `(passedCount>0 && failedCount==0)` = false（永久卡死）**——复现→阻断成立。测毕精确复原（`DELETE` 两场景 seed 行、`must_change_pwd` 复位 1；`process_material(9001)`=0、`must_change_pwd=1/ENABLED`）。
- **决策与偏差（诚实标注）：** ① 采**用户已拍板的方案 A「合格只看最新/有效材料」**（非「加管理员 override 路径」）——领域模型下一个 category = 一项要求、一份通过材料即满足（旧码 `passedCount>0` 即证「≥1 通过即可」，`failedCount==0` 才是那条 bug 阻断），多份材料仅因重传产生，故「按 `(student,year,category)` 取最新一条」语义无歧义、无「一个类别合法需要多份不同材料」的分叉，未触发 STOP 上报。② 未改 `passedCount/failedCount` 展示口径（保留全历史信息，评审仍可见该类别历史通过/不通过条数），仅修 `passed` 判定，最小爆炸半径。③ 活体虽属「重材料流」（任务书允许 IT+代码边界为证），仍以 seed+真实 `/process-status` 打在运行中 fresh jar 上完成了恢复路径的**部署产物级**演示（未走多附件上传的重流程、但直证部署 jar 的合格判定逻辑），诚实标注：完整 upload→审核→重传→再审 全链路由两条真栈 IT 覆盖。范围严格限 43.3，未碰 40/其它 phase。
- ⏳ **未合并**：分支 `feature/phase43-rule11` 单 commit（off main `f15bd61`），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核合并。Rule 11 材料合格卡死已闭环；§7.5 其余 11 条领域规则本就 ENFORCED（无待办）；40（SB 升级）另行分发。

### Phase 40（P0-8 Spring Boot EOL + 方法鉴权 CVE 升级，Phase 40，已合并 —— 分支 `feature/phase40-springboot`，单 commit，已 ff-merge 入 main，mvn verify 93/93 绿；栈起 fresh jar RBAC 活体回归）
- ✅ **P0-8 Spring Boot 3.2.11 EOL + Spring Security 6.2.x 鉴权绕过 CVE 族（CVE-2025-41249/41248/41232，正打 `@EnableMethodSecurity`+`@PreAuthorize`——本平台 RBAC 根基，§7.11）** — 升级到含修复的受支持分支。**采目标版本 SB 3.4.x（非回退 3.3.x）：** 父 POM `spring-boot-starter-parent` **3.2.11 → 3.4.13**（Maven Central 上 3.4 线最新补丁），传递 **Spring Security 6.2.x → 6.4.x**（含 CVE 族修复）。3.4 路径一次跑通、未触发回退预案。
- ✅ **依赖兼容矩阵（仅动版本号 + 两处机械补齐，不改应用逻辑）：** ① **MyBatis-Plus 3.5.7 → 3.5.16**（SB3.4 需 ≥3.5.9）。MP **3.5.9 起把 JSqlParser 外置为独立模块**（starter 不再传递）→ 若不补，`platform-boot` 的 `DataScopeSqlHandler`（直接 `import net.sf.jsqlparser.*`）编译失败、分页/数据权限拦截器运行期 `NoClassDefFoundError`；故在 `platform-common` 显式引入 **`mybatis-plus-jsqlparser-4.9`**（root `dependencyManagement` 用 `${mybatis-plus.version}` 统一版本）——**刻意选 4.9 线而非默认的 `mybatis-plus-jsqlparser`（带 JSqlParser 5.2）**，保 `DataScopeSqlHandler` 现有 `ParenthesedExpressionList<>`/`ExpressionList<>` 4.x API 零改动。② **knife4j 保持 4.5.0**（Maven Central 上 `knife4j-openapi3-jakarta-spring-boot-starter` 最新即 4.5.0，无更新版），但它传递 **springdoc 2.3.0**（面向 Spring Framework 6.1/SB3.2），与 SB3.4 的 **Spring Framework 6.2 不兼容**（doc.html/api-docs 触发 `NoSuchMethodError: ControllerAdviceBean.<init>` / `SpringDocConfigProperties.getGroupConfigs`）→ root `dependencyManagement` 导入 **`springdoc-openapi-bom 2.8.17`**（≥2.8.9 与 knife4j 4.5.0+SB3.4 兼容）覆盖之。hutool/minio/jjwt/fastexcel 与 Spring 无关，未动。
- ✅ **破坏面复盘（本次实际遇到/规避的）：** Spring Security config DSL（`authorizeHttpRequests`/`requestMatchers`/lambda）在 6.x 稳定、`SecurityConfig` **零改动**；全仓**无 `@MockBean`/`@SpyBean`**（3.4 弃用项）→ 测试无需迁移 `@MockitoBean`；`application*.yml` SB 配置键无重命名（启动日志无 warn/fail）；**Flyway 9.22.3 → 10.20.1** 大版本跳跃平滑——`flyway-mysql` 早已在 `platform-boot` 依赖内，启动日志 `Successfully validated 25 migrations`（校验和跨版本稳定、无 mismatch）、`Current version: 25 ... up to date`，**本 phase 无迁移、库 max 仍 V25**。唯一残留是 springdoc 2.8.x 新增的 `SpringDocAppInitializer` 两条 INFO/WARN（"endpoint enabled by default"，提示 prod 可关）——非错误。
- **验证：** `mvn -B -ntp clean verify`（先按 PID 精杀 :8080 验收后端 PID=40912；docker `tcp-mysql`(healthy)/`tcp-redis`/`tcp-minio` 均在）**BUILD SUCCESS，93/93 绿（0 fail/error/skip）**，8 reactor 模块全 SUCCESS（`Phase7VideoReviewIT` 13/13 真实 MinIO HTTP 亦通过）。前端**零改动**、`npm run type-check` 干净、`npm run build` 成功（仅既有 echarts/naive chunk-size 警告）。`git grep 3.2.11`/`3.5.7` 在**活跃构建配置（pom/yml）零残留**（仅历史 DEVLOG/HANDOFF/plan 描述性文字提及旧版本）。
- **RBAC 活体回归（CVE 面，强制）：** 栈起 **fresh jar（新 PID 39788，`Started PlatformApplication in 7.96s`，非旧 40912）**，复位 test 账号 `must_change_pwd=0` 后 `jhw-phase40-rbac-livetest.py`（只读 GET，不改业务数据）三条 + 阳性对照全 **PASS**：① `test_student` `GET /api/system/user` → **HTTP 403「无权限」**（`@PreAuthorize("@pms.has('system:user:manage')")` 方法级安全强制拒绝；阳性对照 `test_sys_admin` 同端点 → 200，证 403 是真鉴权非端点坏/全量拒绝）；② `test_college_clerk`(学院A=…201) `GET /api/student/9002`(学院B=…202) → **`Result.code=404「学生不存在」`**（`@DataScope` 跨学院过滤，本应用「未找到」走 HTTP200+业务码信封、安全拒绝才走真 HTTP403）；③ 同 clerk `GET /api/student/9001`(本院A) → **200**（本域授权放行）。**同一 clerk+同端点：本院 200 / 跨院未找到 → 数据范围强制生效未被绕过。** 证方法级安全 + 数据范围经 SB3.4/Security6.4 升级后语义未变、CVE 面未被静默打开。
- **决策与偏差（诚实标注）：** ① **走目标 3.4.13、未用 3.3.x 回退**——3.4 依赖僵局未出现（MP+knife4j/springdoc 均以「仅补版本/覆盖传递依赖」解决，无不可解冲突）。② 依赖改动**严格机械化**（版本号 + jsqlparser 补齐 + springdoc 覆盖），**未改任何应用逻辑/DSL**。③ 活体为**只读 GET**（未改业务数据，无需复原业务数据）；`must_change_pwd` 由 `mvn verify` 副作用置 1，已按 §0 复位为 0（可登录态，为验收栈的期望态）。④ 范围严格限 Phase 40（SB 升级），未碰其它 phase；无迁移。
- ⏳ **未合并**：分支 `feature/phase40-springboot` 单 commit（off main），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核合并。P0-8 至此闭环。

### Phase 44a（§7.3 N+1 查询 + P1-5/P1-6 批量通知/attachmentRows，Phase 44a，已合并 —— 分支 `feature/phase44-nplusone`，单 commit，已 ff-merge 入 main，mvn verify 93/93 绿）
- ✅ **§7.3 N+1 `toVO`/导出（5 处）** — `VideoReviewServiceImpl.toVO`（三重嵌套 student/task/reviewer）、`TrainingProfileServiceImpl.toVO`、`AbilityTestResultServiceImpl.toVO` 的 `list()` 路径新增批量 `toVOs`/`toVOList`：先 `selectBatchIds`（student）+ `.in(ids)`（task、去重 reviewerId）一次性取值、`Collectors.toMap`/`groupingBy` 建 `Map<Id,Entity>`（Video 另按 reviewId 分组任务、按任务去重批量取 reviewer），原单行 `toVO(entity)` 改为委托新增的多参重载（`toVO(entity, student, ...)`），继续供 `get()`/`detail()` 单行调用、行为不变。`AbilityTestResultServiceImpl` 额外把 `dictLabels("exam_org_mode")`（原每行重复查询、参数恒定的纯函数式读取）提到循环外只查一次。
- ✅ **`ExchangeServiceImpl` 导出行构建器 + `attachmentRows`（P1-6）** — `rowFromCertificate`/`fullReviewRows`/`certSummaryRows`（同 `export()` 调用链、同款 per-cert `studentMapper.selectById` 反模式，`certSummaryRows` 虽未在任务原文点名但同文件同链路一并纳入）与 `attachmentRows`（循环 `selectById` 两处）均改批量：`export()` 入口一次性 `selectBatchIds` 建 `Map<Long,Student>`，`training/video/material` 按 `(studentId, assessmentYear)` 复合键——因非单一 id，采「按 studentId 批量 `.in()` 取回全部年份 → `groupingBy(studentId)` → 逐行内存 `filter(year).findFirst`」范式重现原 `.eq(studentId).eq(year).last("LIMIT 1")` 语义；删除因此变为死代码的旧单行 `videoByStudentYear`/`materialSummary` 私有方法，`trainingByStudentYear` 因仍被 `applyTraining`/`matchTrainingAndStudent`（导入/证书候选筛选，非本次范围）调用而保留。
- ✅ **P1-5 通知批量插入** — `NotifyChannel` 接口新增 `sendBatch` default 方法（逐个调用既有 `send`，未覆盖的通道行为不变，如 `NoopNotifyChannel`）；仅真正落库的 `InAppNotifyChannel` 覆盖为一次 `com.baomidou.mybatisplus.extension.toolkit.Db.saveBatch`（`mybatis-plus-extension` 自带静态工具类，经 `unzip -l` 确认存在于本仓 3.5.16 版 jar 内，本仓首次使用该 API）批量插入；`NotificationService`/`NotificationServiceImpl` 新增对应 `sendBatch`（完整复用 `send` 的空标题短路/非法 userId 过滤/`safe`/`trim` 归一化逻辑）；`ReviewNotificationHelper.notifyRole`（审核待办抄送多个学院管理员）与 `notifyVideoAssigned`（视频评审多个评审人）改调用 `sendBatch`，产出通知行的字段与顺序不变。
- **验证：** `mvn -B -ntp verify`（先按 §0 精杀 :8080 验收后端旧 PID 39788 释放 jar 锁；docker `tcp-mysql`/`tcp-redis`/`tcp-minio` 均在）**BUILD SUCCESS，93/93 绿（0 fail/error/skip）**，9 个 reactor 模块全 SUCCESS（`teacher-cert-platform` 父模块 + `platform-common/system/security/file/business/exchange/statistics/boot`）。改动路径全部由既有 IT 覆盖（`list()`/`toVO()`/`export()`/审核提交与视频分配通知扇出），93/93 无回归即证 VO/行/通知字段与顺序byte-for-byte 不变；纯查询策略重构不引入新分支/边界，未新增 IT。无迁移（库 max 仍 V25）。前端零改动，未跑 `npm run type-check`。
- **决策与偏差（诚实标注）：** ① 复合键（非单一 id）批量化为本次唯一新技巧——training/video/material 按 `(studentId, year)` 唯一是既有业务不变式，按 studentId 批量取回后内存过滤取首个与原 `LIMIT 1` 严格等价，非弱化。② 批量通知选「接口 default 方法 + 仅一个真实现覆盖」而非改 `NotificationService` 签名破坏式重构——全仓 grep 确认零 mock/测试替身实现该接口，新增抽象方法不破坏编译；`NoopNotifyChannel` 用默认逐行回退，零风险。③ `certSummaryRows` 纳入范围系工程判断（同文件同调用链同反模式），非任务原文字面点名，已诚实标注。④ **严格未碰**：`myTasks`/`taskDetail`/`tasks(reviewId)`（未点名、非 `toVO` 调用路径）、`matchTrainingAndStudent`/`selectCertificates`（filter 谓词而非行构建器，牵涉共享的证书候选筛选逻辑，风险/收益比不划算）——均非 N+1 目标缺失，是刻意排除。范围严格限 N+1 查询 + 批量通知插入两项，未碰 Phase 44 其余分页/缓存/SXSSF/统计聚合下推项，另行分发。
- ⏳ **未合并**：分支 `feature/phase44-nplusone` 单 commit（off main），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核合并。§7.3 N+1 toVO/导出 5 处 + P1-5/P1-6 至此闭环；Phase 44 剩余分页/缓存/SXSSF/统计聚合下推另行分发。

### Phase 44b（§7.3 证书导出内存［已完成］+ §7.2 审计写异步［已尝试、诚实推迟］，Phase 44b —— 分支 `feature/phase44-export-async`，单 commit，已 ff-merge 入 main，mvn verify 93/93 绿）
- ✅ **§7.3 证书导出内存（POI 全 DOM 进堆 + `selectCertificates` 无界）** — `ExchangeExcelHelper.writeStandardWorkbook`/`writeTableWorkbook` 两个导出写入口由 `XSSFWorkbook`（整表 DOM 常驻堆）改 `SXSSFWorkbook`（`ROW_ACCESS_WINDOW=200` 行滑动窗口流式写、`setCompressTempFiles(true)`），新增 `newStreamingWorkbook`/`disposeQuietly` 两个私有帮助方法，`disposeQuietly` 对 `dispose()`/`close()` 均防御性吞异常（避免清理失败掩盖已生成的字节结果）。行访问窗口只需大于下拉字典（性别/证件类型等，几到几十项）最大行数，与实际导出行数无关；`applyDropdowns`/`addValidationData` 在该窗口下行为与原 `XSSFWorkbook` 一致（隐藏 sheet 复用、按固定行区间写校验元数据均不依赖行是否已刷盘）。产出仍是标准 OOXML `.xlsx` 字节流，列结构/样式/取值不变，`Phase24AcceptanceIT`（读导出单元格断言值）保持绿。`ExchangeServiceImpl.selectCertificates` 新增 `MAX_EXPORT_ROWS=20000` 单次导出上限——早于逐条 `matchTrainingAndStudent` 过滤即拦截，超限抛 `BizException` 引导按学年/学院缩小筛选范围；未超限时行为、返回值不变，与 Phase 44a 已合并的行构建器批处理（`rowFromCertificate` 等）不冲突、不重复劳动。导入侧 `file.getBytes()`（`ExchangeServiceImpl:159`，上传文件整体读入内存）**不在本次范围**，受既有 multipart 上限（`max-file-size:100MB`，见 §0）约束，未改动。
- ⏳ **§7.2 审计写异步 —— 已实现、经受控实验证实有未查明副作用、本次诚实推迟** — 按任务要求实现了完整方案：`AuditLogAspect` 改调用新增 `AuditLogService#recordAsync`；该方法在**请求线程**同步跑 `applyDefaults`（补齐 `operateTime`/`operatorId`（`UserContext.getUserIdOrSystem()`）/`ip`（`AuditIp.clientIp()`）等依赖 ThreadLocal 的字段）后，把已完全体的 entry 交给新增 `AuditLogPersister`（独立 bean，规避 `@Async` 同类自调用陷阱——同本仓 Phase 37c 处理 `@Transactional` 自调用陷阱手法一致）上的 `@Async(AuditAsyncConfig.AUDIT_EXECUTOR)` 方法，该方法**只做 `sysAuditLogMapper.insert`、不读任何 ThreadLocal**。新增 `AuditAsyncConfig`（`@EnableAsync`）：`prod` profile 下是真正有界线程池（core=2/max=4/queue=1000，`CallerRunsPolicy` 拒绝退化同步而非丢审计）；**`!prod`（含全部 IT——默认 `dev` 且未见任何 `@ActiveProfiles` 覆盖）下用 `SyncTaskExecutor`**，`@Async` 方法在调用线程内联同步执行、等价于"没有异步"。`Phase14E2EIT` 同步修正一处失效断言（原按从未被切面填充的 `bizId` 过滤审计行，改按 `bizType+operation` 倒序取最新一条），新增断言 `operatorId` 精确等于发起请求的 `test_academic_admin`（非 null/系统默认号），验证 ThreadLocal 操作人确实在请求线程被捕获、未随异步丢失——单独跑通过。
  **但经受控消融实验（isolate 变量）证实一个未预期的副作用：** 把"仅本项审计异步改造回退、其余改动（含 §7.3 导出修复）原样保留"与"两项改造都在"两种状态各自跑 `mvn verify`（93 项）对比，**干净 `main`（不含任一改造）0/93 失败；仅回退审计异步、保留导出修复后重跑 93/93 绿；两项改造都在的完整分支状态下 3 次独立 full-suite 运行 3/3 都失败**——命中的是与审计功能**完全无关**的 `Phase5MaterialIT`（材料二审→`ReviewNotificationHelper.notifySecondReviewReturned`→`InAppNotifyChannel.sendBatch`→`Db.saveBatch` 批量插 `notification` 表）与 `Phase6ExemptionIT`（免考多科评审同类通知路径），报 MySQL `Lock wait timeout exceeded`（每次卡满约 50s，与 `innodb_lock_wait_timeout` 默认值吻合），三次命中的具体用例不完全相同。已排除的干扰因素：JUnit5 并行执行（repo 内无 `junit-platform.properties`/parallel 配置）、其它后台线程源（全仓仅 `AuditLogPersister` 一处 `@Async`，`BackupScheduleConfig` 定时任务 `@ConditionalOnProperty` 默认关闭不触发）、`notification` 表唯一约束/gap lock（表上无非 PK 唯一键）。核实 `AuditAsyncConfig`/`AuditLogPersister` 的 `@Async` 限定符（`AUDIT_EXECUTOR="auditLogExecutor"`）与 bean 名精确匹配、`!prod` 在测试环境下确实生效（`application.yml` 默认 `spring.profiles.active: dev`、无 `@ActiveProfiles` 覆盖），排除"实际退化为默认 `SimpleAsyncTaskExecutor` 真起新线程"这一最可疑的假设成立。进一步核实 `secondReview` 所在 `ProcessMaterialController` 的二审/免考评审端点**本身并未标注 `@AuditLog`**（该注解只出现在 upload/delete/replace/submit/batchDownload 等端点上），即 `AuditLogAspect` 并未直接包裹触发失败的调用链——说明这是 `@EnableAsync` 引入的全局 AOP/代理基础设施对**同一应用上下文内其它 `@Transactional` 调用链**的某种间接影响（例如自动代理创建器/advisor 组合方式变化），而非审计切面本身的直接因果；受时间预算限制，未能在本次进一步下钻到 Spring 内部的确切触发点。
  **决策（诚实标注，援引任务书 Item 2 逃生舱条款）**：该副作用已超出条款原本设想的"仅审计自身断言的 IT 确定性"范畴，会以未查明机制扰动完全无关的业务事务（材料/免考通知写入），风险判断上不适合与 §7.3 导出修复捆绑随本次一起交付。故**本次完整回退全部审计异步改造**（`AuditLogAspect`/`AuditLogService`/`AuditLogServiceImpl`/`Phase14E2EIT` 四个文件回到修改前内容，新增的 `AuditAsyncConfig`/`AuditLogPersister` 两个文件不纳入提交），切面保持原有同步 `record()` 调用不变、行为零变化；只交付 §7.3 证书导出内存修复。"已交付的 SXSSF 修复 + 诚实推迟的异步改造"优于"绑定一个会间歇性触发不相关业务测试锁等待超时、根因未明的异步改造"。审计写异步改造的完整实现思路（ThreadLocal 请求线程捕获 + 独立 bean 规避自调用 + dev/test 同步执行器）已验证可行、且不改就不会引入此问题，留待后续 phase 单独排查 AOP/事务交互根因后重新提出。
- **验证：** `mvn -B -ntp verify`（先按 §0 精杀 :8080 占用者——本次全程空闲）**BUILD SUCCESS，93/93 绿（0 fail/error/skip）**，9 个 reactor 模块全 SUCCESS，`Phase24AcceptanceIT`（导出单元格）与全部既有 IT 均绿、无回归；本次未新增 IT（导出修复为既有 IT 已覆盖的纯内部实现替换，`selectCertificates` 新增的行数上限分支超出现有夹具数据规模、未新增专门覆盖）。前端未涉及，未跑 `npm run type-check`。
- **与规格的偏差/疑问：** Item 2（审计写异步）未交付，援引任务书显式授予的逃生舱条款诚实推迟，理由与消融实验证据见上；范围内另一项（Item 1 证书导出）已完整交付。严格未碰缓存/统计聚合下推/分页（Phase 44 其余项，另行分发）。
- ⏳ **未合并**：分支 `feature/phase44-export-async` 单 commit，`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核。§7.3 证书导出内存至此闭环；§7.2 审计写异步留待后续 phase 重新排查根因后再实施；Phase 44 其余分页/缓存/统计聚合下推另行分发。

### Phase 44c（§7.3 参考数据缓存，Phase 44c —— 分支 `feature/phase44-caching`，单 commit，已 ff-merge 入 main，mvn verify 96/96 绿）
- ✅ **§7.3 参考数据缓存（Param/Region/字典标签/组织专业字典）** — Spring Cache（`@EnableCaching`）+ Caffeine 进程内缓存，读穿 `@Cacheable`、写时精确逐出。新增 `platform-system` 依赖 `spring-boot-starter-cache`+`caffeine`（版本由 SB3.4 BOM 管理）与 `CacheConfig`（6 命名缓存各自 TTL+maxSize）。①**Param**：`ParamServiceImpl.getInt/getBoolean/getString` 上 `@Cacheable(sysParam, key=方法名+键+默认值)`；生产唯一写路径 `SystemManagementServiceImpl.updateParam` 上 `@CacheEvict(sysParam, allEntries)`。②**Region**：`children/path/fullName` 各 `@Cacheable`；行政区划无应用层写路径（`SysRegionMapper` 无 insert/update/delete）→ 仅 TTL、无需逐出。③**字典标签**：原 `ExemptionServiceImpl`/`AbilityTestResultServiceImpl`/`ProcessMaterialServiceImpl`/`StatsServiceImpl` 各自私有 `dictLabels`/`categoryLabels`（语义一致）收敛到 `DictService.dictLabels(typeCode)`（`@Cacheable(dictLabels)`，返回 `unmodifiableMap`），4 调用方注入 `DictService` 委托、各自复制成可变副本保原语义。④**组织专业字典**：新增 `DictService.globalEnabledDictItems(typeCode)`（`@Cacheable(orgDictItems)`）消 `OrganizationServiceImpl` majors 列表 ~3N+1。③④逐出：字典项/类型增改删的既有 choke point `DictServiceImpl.evictItemsCache(typeCode)` 内新增手工逐出 Caffeine `dictLabels`/`orgDictItems`（self-invocation 下注解 AOP 不生效故手工）。既有 Redis `listItems` 缓存不变、与新增 Caffeine 缓存正交共用同一逐出点。
- ⏸️ **`DataScopeServiceImpl.resolve` 授权缓存——诚实推迟（安全优先）** — 依赖请求态 `UserContext` + 角色权限/用户数据范围表，key 需 (userId,permissionCode)；失效面覆盖全部 RBAC 写（`SecurityAdminServiceImpl` 的 assignUserRoles/assignUserDataScope/assignRolePermissions/create·update·deleteRole/create·update·deleteUser，角色级变更还扇出到持该角色的所有用户），无法在不引入过度/遗漏逐出下可靠覆盖；授权读缓存陈旧＝越权（安全缺陷）。resolve 仅 2-3 条带索引查询，风险远大于收益，按任务书授权推迟。
- **决策与偏差（诚实标注）：** ① 缓存对象返回不可变视图、调用方复制，杜绝共享缓存对象被 `putIfAbsent`（`StatsServiceImpl` 补齐缺失类别）污染。② Param key 含默认值（同键不同默认/不同 getter 不串味）、逐出用 allEntries（参数写罕见）。③ 字典标签合并重复 itemCode 统一取首个——原 4 处 2 取首 2 取末，实际这些类型均 GLOBAL 单版本 itemCode 唯一、行为零变化。④ 逐出沿用既有 `evictItemsCache` 事务内位置（与既有 Redis 逐出同点），存在「逐出后提交前被并发读重填旧值」的既有微竞态、本次未改（字典写罕见可接受）。⑤ 严格未碰统计聚合下推/真分页/N+1/SXSSF（Phase 44 其余项）、`NotificationServiceImpl.list` 无界；无迁移（库 max 仍 V25）。
- **验证：** `mvn -B -ntp verify`（先按 §0 精杀 :8080；docker tcp-mysql(healthy)/tcp-redis/tcp-minio 均在）**BUILD SUCCESS，96/96 绿（93 基线 + 3 新增 `Phase44CachingIT`，0 fail/error/skip）**，9 reactor 模块全 SUCCESS。`Phase44CachingIT` 证：经 service `updateParam` 改值后 `ParamService.getInt` 立即反映（sysParam 逐出）、字典项改值/停用后 `dictLabels`/`globalEnabledDictItems` 立即反映（evictItemsCache→Caffeine）、region 读一致。**首轮曾 2 项失败**（`Phase5MaterialIT`/`Phase7VideoReviewIT`）——根因是其 `resetParam` helper 越过 service 直接 `paramMapper.updateById` 改库、绕过 `@CacheEvict`；全仓 9 个此类测试文件逐一在 mapper 写后补 `cacheManager.getCache("sysParam").clear()` 后二轮 96/96 绿。**`@EnableCaching` 未见任何锁等待/无关回归**（有别于 Phase 44b 的 `@EnableAsync`）——两处失败均属 param 缓存直接因果、非全局 AOP 副作用，补齐逐出即绿。前端未涉及。
- ⏳ **未合并**：分支 `feature/phase44-caching` 单 commit（off main），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核合并。§7.3 参考数据缓存（Param/Region/字典标签/组织专业字典）至此闭环；DataScope 授权缓存留待后续（先厘清 RBAC 写完整逐出面或改带 TTL 保守短缓存）；Phase 44 其余统计聚合下推/真分页另行分发。

### Phase 44d（P1-3 统计聚合下推 SQL，Phase 44d —— 分支 `feature/phase44-stats`，单 commit，已 ff-merge 入 main，mvn verify 100/100 绿）
- ✅ **下推 SQL 的四类聚合（证书/视频/交叉/免考）** — 新增 `platform-statistics` 的 `StatsAggregationMapper`（`@Select`＋`<script>`＋`<foreach>`，风格对齐既有 `AuditQueryMapper`），把原「全量 `selectList` 回内存 `groupingBy`」改为 DB 侧 `GROUP BY/COUNT/DISTINCT` 只回小体量聚合行。①**证书** `certificateReport`：`countCertificatesByStatus`（按状态分组计数）＋`countGeneratedCertificateStudents`（`COUNT(DISTINCT student_id) WHERE status<>'VOIDED'`＝已生成学生数）；证书总数＝各状态计数求和；WAIT_GENERATE 行、按枚举补 0 行的逻辑不变。②**视频** `videoReport`：`countVideosByStatus`＋`countUploadedVideos`（`video_file_id IS NOT NULL`）；评审任务明细改经父表 `video_review` 的 `student_id` 子查询＋`LIMIT 200`。③**交叉** `crossReport`：`aggregateTrainingCross`（`JOIN student` 取 `identity_type`，`GROUP BY COALESCE(段,''),COALESCE(学科名,''),COALESCE(身份,''),COALESCE(学历,'')`——`COALESCE(...,'')` 与原 Java `safe(null->"")` 对齐、保证 NULL/'' 合并同组的 byte-identical）；本报表无明细。④**免考** `exemptionReport`：`aggregateExemptionSubject`（`GROUP BY CASE WHEN subject_label 非空白 THEN subject_label ELSE subject END, final_status`，等价原 `labelOrCode` 语义），总数/通过数由分组结果求和；明细改有界 `LIMIT 200`。明细统一 `ORDER BY id LIMIT 200`（原为「全量后 `.limit(200)`」的任意序，现确定序、对 ≤200 行结果集完全一致，>200 时原本即非确定、非回归）。
- ✅ **数据范围随下推保留（本任务 #1 正确性风险）** — 关键事实：`StatsController` **未加** `@DataScope`，`DataScopeServiceImpl.resolve` 只返回新 `Scope` 而**不** `DataScopeContext.set(...)` → 统计链路 `DataScopeContext.get()` 恒为 `null` → `DataScopeSqlHandler` 拦截器**在统计查询上从不生效**。范围一直由 `StatsServiceImpl.scopedStudents()` **显式**解析（`dataScopeService.resolve("stats:view")` → 学院用 `.in(college_id, scopeCollegeIds)`、SELF 用 `.eq(id, studentId)`）后，子聚合以 `student_id IN (scopedStudentIds)` 承接。下推后**沿用同一入参**（把 `scopedStudents()` 返回的已过滤 `studentMap.keySet()` 作为 `StatsAggregationMapper` 唯一 IN 参数；视频任务经父表 `video_review` 的 `student_id IN` 子查询；交叉 `JOIN student` 的 `tp.student_id IN` 同样受限），故与原 Java 聚合**同范围**，无跨学院泄露。`studentIds` 为空时服务侧不调用 mapper（避免非法 `IN ()`），返回空聚合，与原 `if(isEmpty) return List.of()` 守卫等价。逻辑删除 `deleted=0` 在自定义 `@Select` 里**显式带上**（不再自动追加 `@TableLogic` 条件）。
- ⏸️ **诚实保留 Java 的四类（各有理由，非遗漏）** — **材料** `materialReport`：聚合与「每生合格判定（PASSED 集合 ⊇ 必需类别集）」「每生缺失类别明细枚举」「字典驱动的必需类别推导」深度纠缠、需按生的行级数据，非干净 `GROUP BY`，仅下推 `(category,status)` 计数并不能消除那份按生扫描，收益为负。**提交** `submissionReport`：范围与业务过滤（含基于 `training_profile` 的二次筛选）都在 `scopedStudents()`，在裸 `GROUP BY` SQL 里重编码有范围偏移（正是本任务点名的数据泄露风险），且只触 `student` 表（范围基表）、student 列表本就为明细/范围边界所需。**异常** `anomalyReport`：逐生跑 `idCard/birthDate/major/link` 校验器的行级逻辑，无法用 SQL 表达。**批次** `batchReport`：可见性 `batchVisible`（`operator_id` 或 `scope_json` 子串匹配 college id）是异于学生范围的非标准范围模型、小表有界，下推得不偿失。
- **决策与偏差（诚实标注）：** ① 聚合返回 `List<Map<String,Object>>`（对齐 `AuditQueryMapper`），别名全用无下划线 camelCase（`cnt/status/dimensionLabel/finalStatus/...`），故不受 `mapUnderscoreToCamelCase` 对 map 键影响；`COUNT` 经 `((Number)v).longValue()` 空安全取值。② 交叉/免考分组键用 `COALESCE`/`CASE` 精确复刻 Java 的 `safe`/`labelOrCode` 空值与 label-or-code 语义，保 byte-identical（`teaching_subject_name` 等均 `NOT NULL`，仅 `identity_type` 可空，`COALESCE(...,'')` 使 NULL 与 '' 同组）。③ 顺带把证书/视频/免考明细循环里逐行调用的 `collegeNames()` 提到方法首部一次算（原每明细行一次 DB 查、语义等价、纯减重复查询）。④ 删除下推后不再使用的私有 `videoReviewsFor`/`exemptionFor` 及 `videoReviewMapper`/`videoReviewTaskMapper` 注入与相应 import（`certificatesFor`/`trainingsFor` 因 `anomalyReport` 仍用而保留）。⑤ 无迁移（库 max 仍 V25）；未碰真分页（Phase 44 剩余项）、前端、其它模块。
- **验证：** `mvn -B -ntp verify`（先按 §0 精杀 :8080——本轮无 :8080 占用；docker tcp-mysql/redis/minio 在）**BUILD SUCCESS，100/100 绿（96 基线 + 4 新增，0 fail/error/skip）**，9 reactor 模块全 SUCCESS。`Phase11StatsIT` 由 5 增至 9：既有 `certificateStatsReconcileWithCertificateListGrouping`（SCHOOL 口径证书分组对账，证 SQL 结果＝原 list 分组）保持绿即证 byte-identical；新增 `collegeCertificateStatsExcludeOtherCollegeData`（学院文员仅见本学院证书聚合＝下推后范围保留铁证）、`exemptionStatsReconcileWithSqlGroupingAndCollegeScope`、`videoStatsReconcileWithSqlGroupingAndCollegeScope`、`crossStatsReconcileWithSqlGroupingAndCollegeScope`（各以唯一 `className` 隔离，SCHOOL 口径对账分组计数、COLLEGE 口径证仅本学院——交叉用例专证 `JOIN student` 不泄漏范围）。**首轮全量曾 2 项失败**（`Phase5MaterialIT` 401 / `Phase6ExemptionIT` 断言 false）——经隔离复跑两类各自 10/10 绿、且此二类按字母序在 `Phase11` **之前**运行、本改动仅动统计生产码（跑在其后、不可能影响），确诊为 §0 共享库账号污染残留（源自本会话早前的定向 IT 跑动留下 `must_change_pwd`）；按 §0 复位 `test_%` 账号后从洁净态整跑 **100/100 绿**。前端未涉及、无需 type-check。
- ⏳ **未合并**：分支 `feature/phase44-stats` 单 commit（off main），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核合并。P1-3 统计聚合下推（证书/视频/交叉/免考）至此闭环；材料/提交/异常/批次诚实保留 Java（理由见上）；Phase 44 剩余 P1-1 真分页另行分发。

### Phase 44e-contract（P1-1 真分页 · 定契约 + 2 样例 + rollout 规格，Phase 44e —— 分支 `feature/phase44e-pagination-contract`，单 commit，已 ff-merge 入 main，mvn verify 102/102 绿）
- ✅ **分页拦截器就绪 + 顺序验证正确（未改 bean）** — `MyBatisPlusConfig.mybatisPlusInterceptor` 已注册 `PaginationInnerInterceptor`，顺序为**数据权限拦截器在前、分页在后**（+乐观锁）。核验此顺序**正确且不可反**：`MybatisPlusInterceptor` 一次查询按注册序对每个内拦截器依次 `willDoQuery`→`beforeQuery`；数据权限先把范围 `WHERE` 改写进 `boundSql`，分页的 `willDoQuery` 再基于**已改写的 SQL** 生成并执行 `COUNT` → **count 与数据两条 SQL 都带范围** → `total` 与该页同为「已按学院/本人过滤」结果；若分页在前，count 会基于未过滤原 SQL → total 把别的学院也算进去（错）。已由 IT 正面证实（见下）。
- ✅ **契约（全站统一，落 `docs/pagination-rollout-spec.md`）** — 列表接口新增 `page`（页码从1、默认1）/`size`（默认20、**硬上限200**）查询参数；新增共享工具 `cn.edu.gpnu.platform.common.api.PageQuery.of(page,size)`（platform-common，钳制 page<1→1、size 空/<1→20、>200→200，构造 MyBatis-Plus `Page`）。响应沿用 `PageResult{total,records}` **契约不变**，`total` 改真实总数、`records` 改当前页。**向后兼容默认 size=20（已实测不截断既有 IT）**：既有 IT 数据集均个位数条、页1即全部；且仅本相真正转换的端点的 IT 会见到分页效果（其余 12 处仍全表、零影响），排查确认仅 `/api/student` 列表被 IT 断言（已随样例更新）。
- ✅ **样例 1 · Student（业务 + 数据范围，变体 A）** — `StudentController.list`/`StudentServiceImpl.list` 加 `page/size`、`selectPage`（保留既有 `@DataScope(alias=student)`，范围随分页生效）；把原前端客户端专属的「年级/班级」筛选**下推为后端 `grade` 参数**（`buildListWrapper` 里 `like(grade).or().like(className)`，复刻旧前端 `records.filter` 口径——真分页后客户端 filter 会失真）；全量方法 `listAll` 原样保留。前端 `StudentManageView` 改服务端分页（page/size state + 发后端 + `DataPanel remote` + 删客户端 `filteredRecords`），`student.ts` `cleanParams` 扩为支持 number。
- ✅ **样例 2 · 系统参数（系统 + 无数据范围，变体 B）** — `SystemAuditController.params`/`SystemManagementServiceImpl.params` 加 `page/size`、`selectPage`；前端 `SystemAuditView` 参数页改服务端分页（`paramTotal` 早已与 `params.length` 分离，最干净），`systemAudit.ts` 传 page/size。
- ✅ **`DataPanel` 向后兼容扩展** — 新增 `remote`（默认 false）/`page`（默认1）prop + `update:page`/`update:pageSize` 事件；`remote=false` 时与原客户端分页**逐字等价**（其余 ~15 个视图零影响），`remote=true` 时受控 page + 事件上抛父组件重新请求。
- **数据范围 × 分页正确性（本任务硬要求）以 IT 正面证明** — `Phase3StudentIT.paginatedStudentListIsScopedAndPagedForCollegeUser`：学院文员（学院A）对「学院A 3 名 + 学院B 1 名同关键词前缀」的学生列表分页 → **`total=3`（学院B 那条既不计入 total 也不在 records）= 分页 COUNT SQL 亦经数据权限过滤的铁证**、页大小生效、页间不重叠；`dataScopeUsesRealStudentTable` 补断言 SELF 范围 `total=1`。非范围侧 `Phase13SystemAuditIT.paramListSupportsRealServerSidePagination`：`records.size()==size<total`、total 跨页稳定、页间不重叠、超大 size 被钳到 ≤200。
- ✅ **rollout 规格** — `docs/pagination-rollout-spec.md`：契约 + 四类 before→after 配方（A wrapper+@DataScope / B 无范围 / C 服务内 Java 手工范围须先下推进 wrapper[`ExchangeServiceImpl.batches` operatorId] / D 自定义 `@Select` mapper 加 `IPage` 参数去 `LIMIT 500`[`auditLogs`]）+ 前端配方（含 `NoticeCenterView` 非 DataPanel 特例）+ IT 配方 + **剩余 12 处清单**（listUsers/training/cert/material/test/exemption/video×2/backups/notice/exchange.batches/auditLogs）+ 门禁。
- **决策与偏差（诚实标注）：** ① 契约相只转 2 样例（覆盖数据范围列表 + 非范围列表两大变体），不铺开全部——按任务书 CONTRACT phase 定义，其余 12 处的机械 rollout 交 Sonnet 照规格抄。② 默认 size 的向后兼容风险为**可实测排清的工程问题**（非不可见外部 API 消费者的产品分叉），已实测 102/102 不截断，未触发 STOP 上报。③ 未改分页拦截器 bean（顺序本就正确）。④ 无迁移（库 max 仍 V25）。
- **验证：** `mvn -B -ntp verify`（先按 §0 精杀 :8080——无占用者；docker tcp-mysql(healthy)/tcp-redis/tcp-minio 均在；复位 `test_%` 账号）**BUILD SUCCESS，102/102 绿（100 基线 + 2 新增，0 fail/error/skip）**，9 reactor 模块全 SUCCESS。前端 `npm run type-check` 干净、`npm run build` 成功（仅既有 echarts/naive chunk 警告）。**首轮曾 2 失败**：我新增 Student 分页 IT 的造数用 2 字母前缀致 `hm_travel_permit`（`^[A-Za-z]\d{8}$`）校验失败（改单字母前缀即过）；`Phase5MaterialIT` 401 系 §0 共享库账号污染残留（Phase 44d 亦命中），复位 `test_%` + 修好造数后洁净整跑 102/102。
- ⏳ **未合并**：分支 `feature/phase44e-pagination-contract` 单 commit（off main），`git remote -v` 空、未 push、未 checkout main、未 merge，待人工复核合并。P1-1 **契约已定 + 2 样例已转，其余 12 处待 rollout**（按 `docs/pagination-rollout-spec.md`，Sonnet 铺开）。

### Phase 44e-rollout（P1-1 真分页 · 剩余 12 处铺开收尾，Phase 44e —— 分支 `feature/phase44e-pagination-rollout`，单 commit，已合并入 main（随 Phase 44f）；rollout 期 mvn verify 114 跑/113 绿/1 败——唯一失败=已查明的 `sendBatch` 锁等待，Phase 44f 已根治→合并前连续 2 次 114/114 绿）
- ✅ **12 处全部转真分页**（按 `docs/pagination-rollout-spec.md` 清单）。8 个机械/文件无交叉端点由 8 个并行子代理完成，3 个高风险/文件有交叉端点（backups+auditLogs 与已转 `params` 共享文件；exchange.batches 范围正确性关键）由 Opus 本人实现。**变体终版**（逐一核验 `@DataScope` 注解 + wrapper 条件后落定）：listUsers/`TrainingProfileServiceImpl.list`/`CertificateServiceImpl.list`/`ProcessMaterialServiceImpl.list`/`AbilityTestResultServiceImpl.list`/`ExemptionServiceImpl.list`/`VideoReviewServiceImpl.list` = **A**（控制器 `@DataScope`，selectPage 直接受益）；`VideoReviewServiceImpl.myTasks`/`NotificationServiceImpl.list` = **B'**（无 `@DataScope`，wrapper 内 `eq(reviewerId/userId, 当前用户)` 天然自范围，见下方偏差说明）；`SystemManagementServiceImpl.backups` = **B**（无范围，运维数据）；`ExchangeServiceImpl.batches` = **C**（Java 侧解析范围后下推 wrapper）；`SystemManagementServiceImpl.auditLogs` = **D**（自定义 `@Select` mapper 改 `IPage` 参数）。
- ✅ **变体 C 数据范围正确性**（`ExchangeServiceImpl.batches`）——原「`selectList` 全量 → Java `stream().filter(operatorId)`」若不改造，分页拦截器生成的 COUNT 会在 Java 过滤**之前**执行，`total` 把他人批次也计入、构成跨用户泄露。改为 `dataScopeService.resolve("exchange:import")` 判 `allSchool()`，非全校时把 `operatorId` eq 条件写入 `LambdaQueryWrapper`（`UserContext.getUserId()` 为 null 时用哨兵 `-1L` 复现原「必不匹配」语义）后再 `selectPage`，使 COUNT 与数据两条 SQL 同受范围约束——`Phase10ExchangeIT.batchListIsScopedToOperatorAndSupportsRealPaginationForNonAllSchoolUser` 正面证明。
- ✅ **变体 D 数据范围正确性**（`auditLogs`）——`AuditQueryMapper.selectLogs` 原用硬编码 `LIMIT 500` 兜底防止无上限全表拉取；改签名首参为 `@Param("page") IPage<Map<String,Object>>`、删 `LIMIT 500`，交 `PaginationInnerInterceptor` 自动改写 SQL 追加 COUNT/真实 LIMIT-OFFSET；`auditCollegeScope()` 仍在 Java 侧解析范围、`collegeIds` 仍作为查询参数直接传入 mapper 的 SQL WHERE（非 `selectList` 后再 Java 过滤），故 COUNT 与分页数据同受学院范围约束；`ORDER BY l.operate_time DESC, l.id DESC` 保留为 SQL 末子句（拦截器改写要求，不可移除/挪位）——`Phase13SystemAuditIT.auditLogListSupportsRealServerSidePaginationWithinCollegeScope` 正面证明。
- ✅ **前端**：11 个 `DataPanel remote` 视图（对应 8 个机械端点 + backups/exchange.batches/auditLogs，含 `ExchangeExportView`/`ExchangeImportView` 两个 batches 消费者）+ `NoticeCenterView.vue` 特例（`n-list` 无内置分页，加独立 `page/size/total` state +`search/onPageChange/onPageSizeChange` 三件套 + 独立 `n-pagination` 组件；顺带修正 `unreadCount`/`readCount`——真分页后 `notices` 仅当页，`unreadCount` 改读全局 `noticeStore.unreadCount`，`readCount` 按当前筛选精确导出而非本地 filter 当页计数）。
- **决策与偏差（诚实标注）：** ① **`video.myTasks` 变体从契约相原稿速记的 "A" 更正为实际的 "B'"**——核实 `wrapper.eq(VideoReviewTask::getReviewerId, userId)`（diff 证明分页化前后此行未变、非新增）是「我的任务」天然自带的当前用户过滤，无 `@DataScope` 注解、也不需要；纯变体标签的事后更正，范围正确性不受影响。② **`CertificateIssueView.vue` 协同回归**（本相发现并修复，非契约相/前序 rollout 遗留 bug 而是本相铺开后才会触发的影响面）——`/api/cert` 的第二消费者（签发队列页，区别于已转好的 `CertificateManageView.vue`），原未带 `page/size`、`DataPanel` 非 remote，本相后端默认 `size=20` 生效后会静默截断到 20 条且无翻页；照搬 `CertificateManageView.vue` 已验证模式修复（`certTotal/page/size` + 三件套 + `DataPanel remote` + 「页内状态分布」局限性注释，与姐妹视图一致）。③ 其余 11 处变体标签与契约相原稿一致。无迁移（库 max 仍 V25）。
- **问题与解决（1）：`assessment_year` 列 `VARCHAR(16)` 截断**——3 个子代理写的分页 IT（Phase4Training/Phase5Material/Phase6ExemptionIT）各自用 `"P{4,5,6}PAGE" + System.nanoTime()` 造年度标记，`nanoTime()` 最长 19 位、拼上 6 位前缀最长可达 25 位超限：Training 侧命中 Bean Validation 400，Material/Exemption 侧直接 `MysqlDataTruncation`。改为 `Math.abs(System.nanoTime() % 1_000_000_000L)`（钳到 ≤9 位），`LIKE 'P4%'/'P5%'/'P6%'` 清理前缀不受影响。
- **问题与解决（2）：间歇性失败的深入排查（先后 6 次全量 verify + 2 次隔离重跑）——已查明与本次分页改造无关**：`Phase5Material`/`Phase6ExemptionIT` 里 `processStatus` 相关的既有旧用例（非本相新增）偶发失败，报文分两类且**确认为同一事故的两个阶段**——`NotificationMapper.insert` 批量插入命中真实 InnoDB `Lock wait timeout exceeded`（约 50s，与默认 `innodb_lock_wait_timeout` 吻合）阻塞请求线程，测试方法紧接的下一次 HTTP 调用因该 ~50s 意外延迟导致会话/令牌被判过期而收到 401。**排除本次改动嫌疑**：`git diff main` 核实 `NotificationServiceImpl`/`NotificationService` 的改动仅涉及本相分页目标 `list()` 方法，从未触碰 `sendBatch`；`innodb_trx`/`data_lock_waits` 查证每次失败后均无残留事务（瞬时争用而非死锁）；**决定性证据**——隔离单独重跑 `Phase5MaterialIT`+`Phase6ExemptionIT`（`-Dit.test=... -Dfailsafe.failIfNoSpecifiedTests=false`，无前面约 20 个测试类的累积负载）**12/12 全绿**，证明该争用是全量长串行跑（114 测试类累计 10+ 分钟）的时序产物，非这两类自身逻辑问题。**额外定位可清理诱因**：`notification` 表无任何测试清理动作覆盖，本会话当天重复跑 verify 已在此表累积 1616 行同日数据（对比前一日仅 41 行）；比照 §0 既有先例（`test_%` 的 `must_change_pwd` 残留即「已知共享库残留、非我的 bug」）处理：重置全部 `test_%` 账号 `must_change_pwd=0`（核实 12 个账号确带残留）+ 清理当日累积通知残留——纯环境卫生操作，未改任何源码/测试代码。处理后失败数从首次全量的 4 项降到稳定的 1 项（第 5、6 次整跑均恰 114 跑/1 败/同一断言点），但**未 100% 消除该原生争用**（根治需改 `sendBatch` 批量写路径的事务边界/连接粒度，超出「仅分页」范围，诚实未做）。**12 个新增分页 IT 在全部 5 次全量跑 + 2 次隔离跑中无一次失败**（逐日志核对确认，见 DEVLOG 同日条目）。
- **验证：** `mvn -B -ntp verify`（每次先按 §0 精杀 :8080——均无占用者）。**最终稳定态：114 跑 / 113 绿 / 1 败**（100 基线 + 2 契约样例 + 12 新增，0 error/skip；唯一失败为上述已彻底查明、证实与本次分页改造无关的既有旧用例间歇性问题）；隔离重跑确认两类逻辑本身 12/12 全绿。前端 `npm run type-check` 干净、`npm run build` 成功（仅既有 echarts/naive chunk 警告）。
- ✅ **已合并入 main**（ff-merge，随 Phase 44f 一并）：分支 `feature/phase44e-pagination-rollout` 含 rollout 单 commit（234c07e）+ Phase 44f 通知锁等待修复独立 commit；`git remote -v` 空、未 push。**P1-1「全站假分页」14 处（2 契约样例 + 12 rollout）至此全部转真分页闭环。** 上文「问题与解决（2）」诚实标注、当时归为「另开工单」的 `sendBatch` 路径 `Lock wait timeout` 争用**已由 Phase 44f 根治**（见下）；合并前连续 2 次全量 verify 稳定 114/114 绿。

### Phase 44f（P1 收尾 · 修复 44a 引入的通知批量插入锁等待，Phase 44f —— 与 rollout 同分支 `feature/phase44e-pagination-rollout`，独立 commit，已 ff-merge 入 main，mvn verify 连续 2 次 114/114 绿）
- ✅ **根因**：MyBatis-Plus `Db.saveBatch`（Phase 44a 为 P1-5 引入）以 BATCH 执行器**另开一个 SqlSession/连接**、不参与外层 Spring `@Transactional`；二审 `secondReview`（持 `process_material` 行锁的外层事务）经 `ReviewNotificationHelper.notifyRole → notificationService.sendBatch → InAppNotifyChannel.sendBatch` 调用时，另开连接的批量 insert 与外层事务持锁互等 → 间歇 `Lock wait timeout exceeded`（~50s，吻合默认 `innodb_lock_wait_timeout`），后续 HTTP 调用再因该延迟被判令牌过期收 401。此即 44e-rollout「问题与解决（2）」诚实标注、当时归为「另开工单」的遗留争用。
- ✅ **修法**：`InAppNotifyChannel.sendBatch` 由 `Db.saveBatch(notifications)` 改为当前事务/连接内 `for` 循环 `notificationMapper.insert(n)`（即 44a 之前的原始同事务逐行插入，彼时恒绿从无锁等待；逐行走标准 insert 的 `ASSIGN_ID` + 审计自动填充，字段/行数/语义与批量版逐行等价）。收件人扇出很小（单学院某角色成员），逐行往返成本可忽略；舍弃 44a 微小批量收益、保同事务安全。删 `Db`/`ArrayList` 无用 import。无迁移（库 max 仍 V25）、无前端。
- ✅ **验证（活体绿条 + 复现消失）**：`mvn -B -ntp clean verify`（先按 §0 精杀 :8080——无占用者；复位 `test_%` + 查证 0 残留事务/0 lock_waits）**连续 2 次 BUILD SUCCESS，均 114/114 绿**，日志 grep `Lock wait|BatchUpdateException|<<< FAILURE` 均 0 命中。此前 rollout 稳定复现失败的 `Phase5MaterialIT`（`secondReview→notify` 路径）本相 7/7 绿；`Phase12NotificationIT` 5/5 绿佐证通知语义未变。
- ✅ **已合并**：与 rollout 同分支独立 commit，已随分支 ff-merge 入 main（`git remote -v` 空、未 push）。**Phase 44 全部 P1（§7.3 N+1/证书导出 SXSSF/参考数据缓存 + P1-3 统计聚合下推 + P1-1 真分页 + 本 P1 通知锁等待）至此全部收尾闭环。**

