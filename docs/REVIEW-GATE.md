# REVIEW-GATE.md — 阶段复核闸门（Claude 复核协议）

> 规则：**每个 Phase 由 codex 实现，完工后必须经 Claude 复核通过才算完成。** codex 不得自行把阶段标记为"完成"。
> 本文件既是流程规范，也是 **Claude 执行复核的 playbook**。配套报告模板 `docs/reviews/REVIEW-TEMPLATE.md`。

---

## 1. 角色与流程
- **codex（实现）**：完成本 Phase 全部任务并自测后——
  1. 各任务在 `PROGRESS.md` 置 `[x]`，阶段状态置 **待复核**；
  2. 提交代码（feature/develop 分支），在 `DEVLOG.md` 写本阶段小结（做了什么/验证/遗留）；
  3. 发起复核：通知人类"Phase N 待 Claude 复核"。**此时不得置 ✅。**
- **Claude（复核）**：按本协议**独立**复核 → 产出 `docs/reviews/phase-NN-review.md` → 判定：
  - **PASS** → `PROGRESS.md` 阶段置 **✅ 已复核**，更新「AT 跟踪」，放行下一阶段；
  - **退回** → 列 Blocker/Major 问题，阶段置 **复核退回**；codex 修复后重交，Claude **只复核增量 + 回归**，直至 PASS。
- **人类**：在 codex 完工与 Claude 复核间转交，可随时介入裁定。

## 2. 复核独立性（不轻信自报）
- 不以"codex 说通过"为准：**Claude 亲自构建、运行、并独立重跑本阶段的反例/关键用例**。
- 对抗性验证：主动尝试破坏不变量（缺类却判合格、越权访问、并发生成证书、非法证件号、自由填学科、未登录取预签名…），确认被正确拦截。
- 对照规格原文逐项核对（`plan.md` / `docs/phase-NN` / 附录），尤其"逐字一致"的字典与校验规则。

## 3. 复核维度（逐条留证据）
| 编号 | 维度 | 检查要点 |
|---|---|---|
| D1 | 需求符合性 | 字段/规则/状态/流程与 plan + docs/phase-NN 一致；字典值逐字一致（抽查） |
| D2 | 验收清单 | docs/phase-NN 每条 `- [ ]` 由 Claude 复跑通过 |
| D3 | AT 验收 | 本阶段相关 AT-01~14（见 §6）有证据通过 |
| D4 | 红线合规 | AGENTS §1：文本化/不硬编码/留痕/逻辑删除/Flyway/数据范围/后端校验/权限点/不擅改需求 |
| D5 | 代码质量 | 分层/命名/统一 Result/异常/事务/注释/无魔法值（可跑 `/code-review`） |
| D6 | 安全 | 脱敏/鉴权/无越权明文/无密钥入库/文件视频访问受控（可跑 `/security-review`） |
| D7 | 构建与运行 | `mvn package` 绿、应用可在外部终端/复核环境启动、前端 `vite build` 绿、本阶段冒烟通过；不得让 Codex/headless exec 启动常驻服务 |
| D8 | 测试 | 单元 + **反例** 齐备且通过；核心规则覆盖；CI 绿 |
| D9 | 数据库 | Flyway 幂等、版本号递增不复用、表/索引/注释、种子正确 |
| D10 | 回归 | 重跑既有阶段冒烟（健康/登录/既有接口），未破坏 |
| D11 | 文档/进度 | PROGRESS/DEVLOG/Swagger 更新；DoD（AGENTS §12）逐条满足 |

## 4. 复核方法（建议步骤）
1. **取增量**：`git -C <repo> log --oneline`；`git diff <上阶段末commit>..HEAD --stat` 看改动面。
2. **构建**：`mvn -B -ntp -DskipTests package`；`npm --prefix frontend run build`（命令见 `HANDOFF.md` §3）。
3. **起依赖+应用**：`docker compose -f docker-compose.dev.yml up -d`；如需启动后端/前端常驻服务，必须在外部终端或复核 harness 中用 `scripts/dev-serve.*` 启动并重定向日志，禁止由 Codex/headless exec 执行 `java -jar`、`npm run dev`、`vite dev` 或包装启动脚本；随后跑本阶段冒烟 + **反例**（§6）。
4. **数据核验**：`docker exec tcp-mysql mysql -uroot -proot123 teacher_cert -e "..."` 查表/数据。
5. **读码**：对 D1/D4/D5/D6 抽查关键类（校验器、状态机、证书编号、导入导出、切面、数据权限）。
6. **专项 skill**：`/code-review`（质量/缺陷）、`/security-review`（安全）对本阶段 diff。
7. **写报告**（§5），判定，更新 `PROGRESS.md`。

## 5. 判定与分级
- **PASS**：D1~D11 无 Blocker/Major；Minor 记 backlog 不阻断。
- **退回（CHANGES REQUESTED）**：存在以下任一即退回，逐条给「问题-证据-期望-定位」：
  - **Blocker**：违反红线 / 相关 AT 失败 / 构建或运行失败 / 关键反例未被拦截 / 数据错误。
  - **Major**：规格偏差 / 缺反例测试 / 安全隐患 / 留痕缺失 / 数据范围漏挂 / 文本化破坏。
  - **Minor**：命名/注释/轻微重复/优化项（登记 backlog，不阻断放行）。

## 6. 各阶段复核重点（必过 AT + 必跑反例）
| Phase | 必过 AT | 必跑反例 / 关键核验 |
|---|---|---|
| 1 字典 | AT-05(基础) | 中职专业课**类别节点不可选**；字典值逐字一致(抽身份类型5/实习地点5含括号/测试结论4)；区划拼接完整文本；改字典后缓存刷新 |
| 2 权限 | AT-13 | §15.1 矩阵逐格；学院A 查不到学院B；学生只见本人；无权限接口 403 |
| 3 基本信息 | AT-03, AT-01(部分) | 四类证件正/反例；身份证出生日期不一致**被拦**；港澳通行证/台胞证不做日期校验；前导零/证件号文本不丢；锁定后禁改 |
| 4 培养信息 | AT-04, AT-05 | 教育类研究生专业代码非 0401/0451/0453 **报错**；任教学段变→学科联动且**禁自由填**；企业/海外实习地点限制 |
| 5 材料 | AT-06 | **四类缺一不能"合格"**；某类不通过不能合格；通过后直接替换被拒 |
| 6 免考 | AT-07 | 多科并存不同结论(通过/退回/不通过)互不影响；漏佐证不能提交；通过科目移出应考；不覆盖过程性 |
| 7 视频 | AT-08 | 教师A提交后B**接口层看不到**A的分；分差>阈值→需复评；一合格一不合格→需复评；未登录预签名链接不可播放 |
| 8 测试结果 | (证书前置) | 免考通过科目不在应考；确认后锁定；结论=待确认时 Phase9 阻断 |
| 9 证书 | AT-09/10/11 | 前置缺项**阻断生成**并列缺项；18位编号示例可复现；**并发生成不重号/不空号**；有效期上/下半年；锁定后禁改；作废重开留痕 |
| 10 导入导出 | AT-01/02/14 | 导出 A–Z 26列且 **H列="身份证件号码"**；导出在 WPS/Excel 打开**无科学计数/日期序列/前导零丢失**；V-01~V-13 **逐条**反例命中并定位；异常报告含行号/字段/错误值/原因/建议；导入回滚恢复 |
| 11 统计 | (口径) | 统计数与明细对账一致；数据范围生效 |
| 12 通知 | (触发) | 提交/退回/评审/导出完成 四类触发到正确接收人 |
| 13 系统/审计 | AT-12 | 初/复审/退回/确认/作废/重开/导出**全留痕**(操作人/时间/前后状态/IP)；普通管理员不能删日志；改参数热生效 |
| 14 收口 | 全 AT 复验 | 主流程 E2E 贯通；WPS/Excel 兼容；docker 一键起站 |

## 7. 闭环
退回 → codex 仅修 Blocker/Major → 重新提交并在 DEVLOG 记修复 → Claude **只复核增量 + 必要回归** → 直至 PASS。Minor 进 backlog。

> **Phase 0** 由 Claude 直接实现并运行验证，视为已复核（self-verified），见 commit `6948682..08c638f`，无需补复核报告。
