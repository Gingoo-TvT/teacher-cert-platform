# Phase 44 独立复核报告

- 复核日期：2026-07-23
- 复核者：Codex（用户授权独立复核）
- 结论：**CHANGES REQUESTED**
- 提交：`130ac74`、`649afa9`、`8d24489`、`86a733c`、`130625d`、`234c07e`、`424646a`
- 范围：N+1、通知扇出、SXSSF、缓存、统计 SQL 聚合、真分页与锁等待修复

## 独立证据

- 当前全量回归 265/265，分页/数据范围、统计和导出测试通过；前端 type-check/build 通过。
- 对最终代码与阶段完成声明做反向核对：44f 已把 44a 的通知批量插入回退为逐行 insert；缓存逐出仍有提交前旧值重填窗口。

## D1–D11

| 维度 | 结论 | 说明 |
|---|---|---|
| D1–D4 规格/验收/红线 | 不通过 | P1-5 被标为已完成，但最终实现不再批量写；缓存热生效存在并发窗口。分页/统计/导出/N+1 通过。 |
| D5–D6 质量/安全 | 部分通过 | 44f 选择正确性优先合理，DataScope 授权缓存也被诚实推迟；完成状态需同步。 |
| D7–D10 构建/测试/回归 | 通过（现有断言） | 全量绿，既有锁等待已消失；缺缓存提交交错和大扇出性能门禁。 |
| D11 文档 | 不通过 | `launch-readiness-plan` 仍把通知批量插入记为闭环。 |

## Major

1. 将 P1-5 改为“诚实推迟”，或实现使用同一事务连接的真正 multi-values insert 并回归锁等待。
2. 将字典/参数缓存逐出移动到事务提交后，并补并发重填反例。

完整证据见 PG-M3/PG-M4。该退回不否定真分页、统计下推、SXSSF 和 N+1 子项已经通过。
