# WS-3 独立复核记录 — MinIO 预签名直传

| 项 | 值 |
|---|---|
| 复核日期 | 2026-07-23 |
| 复核者 | Codex（Claude 不可用，经用户明确授权替代复核） |
| 增量 | `acfc3b6..338bc91` |
| 判定 | **CHANGES REQUESTED（复核退回）** |
| Blocker / Major | 0 / 3（按统一审计口径为 High） |

## 独立性与范围

复核覆盖浏览器 S3 multipart、内部/公开端点、CORS、预签名 TTL、会话状态、ETag/分片约束、定稿、秒传、材料与视频集成、前端并发上传和仓库 CI。复核者使用真实 MinIO 重跑测试并读生产数据流，没有把 265/265 直接等同为业务安全 PASS。

## 已通过部分

- 内部服务端点与浏览器公开 presigner 分离，CORS 可配置，预签名 TTL 有 3600 秒上限。
- 对象键、桶、owner/context metadata、part 数量/大小/连续性/ETag、最终大小/content-type 均有后端约束；状态迁移使用 CAS，活动会话有数据库唯一性约束。
- 浏览器端并发 5 路分片、Worker 指纹、刷新 URL、恢复与终止流程完整；材料/视频真实 MinIO IT、过期 URL、CORS、越权与恢复路径通过。
- 前端 type-check/build 通过；全新 schema `mvn clean verify` **265/265**。

## 退回问题

1. **Major / High：真实媒体未校验。** `VideoReviewServiceImpl:1128-1137,1438-1468` 只检查对象元数据与客户端时长；`Phase7VideoReviewIT:1643-1655` 用伪字节也能作为成功 MP4。必须以可信媒体探测验证实际容器和时长，失败关闭。
2. **Major / High：跨用户秒传缺持有证明。** `initUpload:153-218` 直接以客户端 64 位声明跨用户查重，登记路径不重算内容指纹，`toVO:1862` 还返回指纹。修复前应关闭跨 uploader/student 秒传，随后引入服务端 checksum/possession proof。
3. **Major / High：CI 门禁不可复现。** `.github/workflows/ci.yml:12-44` 只有 MySQL/Redis，没有 Phase 5/7 所需 MinIO；前端 `:56-60` 只 build 不 type-check。必须补齐后重跑零状态 CI。

完整证据、风险场景和修复建议见 `current-ws-chain-audit-2026-07-23.md`。

## D1–D11 摘要

| 维度 | 结论 | 说明 |
|---|---|---|
| D1–D4 规格/红线 | FAIL | Phase 7 要求合并后校验真实 MP4/时长，当前只信声明 |
| D5 代码质量 | PASS（有阻断边界缺口） | 上传状态机和约束清晰；可信验证职责缺失 |
| D6 安全 | FAIL | 跨用户秒传以未验证指纹作为持有证明 |
| D7 运行 | PASS | 真实 MinIO、本地构建、Compose 均通过 |
| D8 测试/CI | FAIL | 关键反例缺失且 CI 无 MinIO/type-check |
| D9 数据库 | PASS | V28 与会话唯一/CAS 在全新 schema 通过 |
| D10 回归 | PASS | 265/265，但不足以覆盖上述缺口 |
| D11 文档/进度 | FAIL（待整改） | 当前必须标记复核退回，不可宣称可发布 |

## 重交条件

- 三个 Major 全部修复并补反例。
- 在全新 schema + 真实 MySQL/Redis/MinIO 重跑 WS-3 专项、Phase 5/7 回归与 `mvn clean verify`。
- 前端重跑 type-check/build；GitHub Actions 零状态可复现。
- 复核只检查修复增量 + 必要回归，直至 PASS。
