# Phase 41 独立复核报告

- 复核日期：2026-07-23
- 复核者：Codex（用户授权独立复核）
- 结论：**CHANGES REQUESTED**
- 提交：`43b9837`、`df79418`
- 范围：最小权限数据库账号、Redis/镜像安全、流关闭、应用逻辑备份

## 独立证据

- 备份生成 IT 纳入全量回归并成功生成 gzip/校验和记录。
- 静态对照 `DatabaseBackupService`、Flyway 种子和恢复手册，确认当前恢复命令不可按文档完成。

## D1–D11

| 维度 | 结论 | 说明 |
|---|---|---|
| D1–D4 规格/验收/红线 | 不通过 | “真备份”缺少可执行恢复；仅生成产物不能满足灾备验收。 |
| D5–D6 质量/安全 | 部分通过 | 非 root、Redis 密码和 InputStream 修复有效；MySQL init 仍把变量直接拼进 SQL。 |
| D7–D10 构建/测试/数据库/回归 | 不通过 | 265/265 只验证生成；没有 fresh schema 回放演练。 |
| D11 文档 | 不通过 | 手册要求迁移后回放普通 INSERT，但迁移已写入重叠种子。 |

## Major

备份包含 Flyway 已种子化的系统表并输出普通 `INSERT`；按手册先迁移再回放会重复键失败。必须定义恢复策略并做真实 scratch restore drill。另将 `01-app-user.sh` 的 SQL 插值作为同阶段整改项。详见 PG-H2/PG-M2。
