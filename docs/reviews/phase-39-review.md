# Phase 39 独立复核报告

- 复核日期：2026-07-23
- 复核者：Codex（用户授权独立复核）
- 结论：**CHANGES REQUESTED**
- 提交：`1cf75be`
- 范围：删除父记录的孤儿防护、删除学生后的会话撤销

## 独立证据

- 学生删除后的登录停用/令牌撤销逻辑由当前安全回归覆盖。
- 对学院删除与专业/用户创建做并发交错检查，发现应用层守卫仍为无共享锁的 check-then-delete。

## D1–D11

| 维度 | 结论 | 说明 |
|---|---|---|
| D1–D4 规格/验收/AT/红线 | 不通过 | “不产生静默孤儿”在并发创建路径下不成立；项目又明确不使用 FK。 |
| D5–D6 质量/安全 | 部分通过 | 学生删除撤销访问正确；组织完整性协议缺失。 |
| D7–D10 构建/测试/回归 | 部分通过 | 265/265 绿，但没有 deleteCollege 与 createMajor/createUser/createStudent 的对抗并发 IT。 |
| D11 文档 | 不通过 | 阶段完成声明没有披露该并发窗口。 |

## Major

`OrganizationServiceImpl.deleteCollege` 在 `:108-121` 计数后删除，`createMajor` 在 `:164-177` 读取父记录后插入，双方不锁同一父行；用户/学生创建同样没有统一锁协议。必须修复并补三类并发反例。详见总审计 PG-H1。
